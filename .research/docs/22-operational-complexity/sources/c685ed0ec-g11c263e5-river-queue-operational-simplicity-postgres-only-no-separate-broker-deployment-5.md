[ ![Go](https://pkg.go.dev/static/shared/logo/go-white.svg) ](https://go.dev/)
[ Skip to Main Content ](https://pkg.go.dev/github.com/riverqueue/river#main-content)
![](https://pkg.go.dev/static/shared/icon/search_gm_grey_24dp.svg) ![](https://pkg.go.dev/static/shared/icon/search_gm_grey_24dp.svg)
  * [ Why Go ![submenu dropdown icon](https://pkg.go.dev/static/shared/icon/arrow_drop_down_gm_grey_24dp.svg) ](https://pkg.go.dev/github.com/riverqueue/river)
    * [ Case Studies ](https://go.dev/solutions#case-studies)
Common problems companies solve with Go
    * [ Use Cases ](https://go.dev/solutions#use-cases)
Stories about how and why companies use Go
    * [ Security ](https://go.dev/security/)
How Go can help keep you secure by default
  * [Learn](https://go.dev/learn/)
  * [ Docs ![submenu dropdown icon](https://pkg.go.dev/static/shared/icon/arrow_drop_down_gm_grey_24dp.svg) ](https://pkg.go.dev/github.com/riverqueue/river)
    * [ Effective Go ](https://go.dev/doc/effective_go)
Tips for writing clear, performant, and idiomatic Go code
    * [ Go User Manual ](https://go.dev/doc/)
A complete introduction to building software with Go
    * [ Standard library ](https://pkg.go.dev/std)
Reference documentation for Go's standard library
    * [ Release Notes ](https://go.dev/doc/devel/release)
Learn what's new in each Go release
    * [ API ](https://pkg.go.dev/api)
Reference documentation for the pkg.go.dev API
  * [Packages](https://pkg.go.dev/)
  * [ Community ![submenu dropdown icon](https://pkg.go.dev/static/shared/icon/arrow_drop_down_gm_grey_24dp.svg) ](https://pkg.go.dev/github.com/riverqueue/river)
    * [ Recorded Talks ](https://go.dev/talks/)
Videos from prior events
    * [ Meetups _![](https://pkg.go.dev/static/shared/icon/launch_gm_grey_24dp.svg) _ ](https://www.meetup.com/pro/go)
Meet other local Go developers
    * [ Conferences _![](https://pkg.go.dev/static/shared/icon/launch_gm_grey_24dp.svg) _ ](https://github.com/golang/go/wiki/Conferences)
Learn and network with Go developers from around the world
    * [ Go blog ](https://go.dev/blog)
The Go project's official blog.
    * [ Go project ](https://go.dev/help)
Get help and stay informed from Go
    * Get connected 
[ ![](https://pkg.go.dev/static/shared/logo/social/google-groups.svg) ](https://groups.google.com/g/golang-nuts "Get connected with google-groups \(Opens in new window\)") [ ![](https://pkg.go.dev/static/shared/logo/social/github.svg) ](https://github.com/golang "Get connected with github \(Opens in new window\)") [ ![](https://pkg.go.dev/static/shared/logo/social/twitter.svg) ](https://twitter.com/golang "Get connected with twitter \(Opens in new window\)") [ ![](https://pkg.go.dev/static/shared/logo/social/reddit.svg) ](https://www.reddit.com/r/golang/ "Get connected with reddit \(Opens in new window\)") [ ![](https://pkg.go.dev/static/shared/logo/social/slack.svg) ](https://invite.slack.golangbridge.org/ "Get connected with slack \(Opens in new window\)") [ ![](https://pkg.go.dev/static/shared/logo/social/stack-overflow.svg) ](https://stackoverflow.com/collectives/go)


[ ![Go.](https://pkg.go.dev/static/shared/logo/go-blue.svg) ](https://go.dev/)
  * [ Why Go _![](https://pkg.go.dev/static/shared/icon/navigate_next_gm_grey_24dp.svg) _ ](https://pkg.go.dev/github.com/riverqueue/river)
[ _![](https://pkg.go.dev/static/shared/icon/navigate_before_gm_grey_24dp.svg) _ Why Go ](https://pkg.go.dev/github.com/riverqueue/river)
    * [ Case Studies ](https://go.dev/solutions#case-studies)
    * [ Use Cases ](https://go.dev/solutions#use-cases)
    * [ Security ](https://go.dev/security/)
  * [Learn](https://go.dev/learn/)
  * [ Docs _![](https://pkg.go.dev/static/shared/icon/navigate_next_gm_grey_24dp.svg) _ ](https://pkg.go.dev/github.com/riverqueue/river)
[_![](https://pkg.go.dev/static/shared/icon/navigate_before_gm_grey_24dp.svg) _ Docs ](https://pkg.go.dev/github.com/riverqueue/river)
    * [ Effective Go ](https://go.dev/doc/effective_go)
    * [ Go User Manual ](https://go.dev/doc/)
    * [ Standard library ](https://pkg.go.dev/std)
    * [ Release Notes ](https://go.dev/doc/devel/release)
    * [ API ](https://pkg.go.dev/api)
  * [Packages](https://pkg.go.dev/)
  * [ Community _![](https://pkg.go.dev/static/shared/icon/navigate_next_gm_grey_24dp.svg) _ ](https://pkg.go.dev/github.com/riverqueue/river)
[ _![](https://pkg.go.dev/static/shared/icon/navigate_before_gm_grey_24dp.svg) _ Community ](https://pkg.go.dev/github.com/riverqueue/river)
    * [ Recorded Talks ](https://go.dev/talks/)
    * [ Meetups _![](https://pkg.go.dev/static/shared/icon/launch_gm_grey_24dp.svg) _ ](https://www.meetup.com/pro/go)
    * [ Conferences _![](https://pkg.go.dev/static/shared/icon/launch_gm_grey_24dp.svg) _ ](https://github.com/golang/go/wiki/Conferences)
    * [ Go blog ](https://go.dev/blog)
    * [ Go project ](https://go.dev/help)
    * Get connected
[![](https://pkg.go.dev/static/shared/logo/social/google-groups.svg)](https://groups.google.com/g/golang-nuts) [![](https://pkg.go.dev/static/shared/logo/social/github.svg)](https://github.com/golang) [![](https://pkg.go.dev/static/shared/logo/social/twitter.svg)](https://twitter.com/golang) [![](https://pkg.go.dev/static/shared/logo/social/reddit.svg)](https://www.reddit.com/r/golang/) [![](https://pkg.go.dev/static/shared/logo/social/slack.svg)](https://invite.slack.golangbridge.org/) [![](https://pkg.go.dev/static/shared/logo/social/stack-overflow.svg)](https://stackoverflow.com/collectives/go)


  1. [Discover Packages](https://pkg.go.dev/)
  2. [ github.com/riverqueue/river ](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0) ![](https://pkg.go.dev/static/shared/icon/content_copy_gm_grey_24dp.svg)


[ ![Go](https://pkg.go.dev/static/shared/logo/go-blue.svg) ](https://go.dev/)
# river
package module ![](https://pkg.go.dev/static/shared/icon/content_copy_gm_grey_24dp.svg)
[ Version:  v0.39.0 ](https://pkg.go.dev/github.com/riverqueue/river?tab=versions)
Opens a new window with list of versions in this module. 
Latest Latest  ![Warning](https://pkg.go.dev/static/shared/icon/alert_gm_grey_24dp.svg)
This package is not in the latest version of its module.
[ Go to latest ](https://pkg.go.dev/github.com/riverqueue/river) Published: Jun 3, 2026  License: [MPL-2.0](https://pkg.go.dev/github.com/riverqueue/river?tab=licenses)
Opens a new window with license information. 
[ Imports: 50 ](https://pkg.go.dev/github.com/riverqueue/river?tab=imports)
Opens a new window with list of imports. 
[ Imported by: 172 ](https://pkg.go.dev/github.com/riverqueue/river?tab=importedby)
Opens a new window with list of known importers. 
Main Versions  Licenses  Imports  Imported By 
## Details
  * ![checked](https://pkg.go.dev/static/shared/icon/check_circle_gm_grey_24dp.svg) Valid [go.mod](https://github.com/riverqueue/river/tree/v0.39.0/go.mod) file ![](https://pkg.go.dev/static/shared/icon/help_gm_grey_24dp.svg)
The Go module system was introduced in Go 1.11 and is the official dependency management solution for Go. 
  * ![checked](https://pkg.go.dev/static/shared/icon/check_circle_gm_grey_24dp.svg) Redistributable license ![](https://pkg.go.dev/static/shared/icon/help_gm_grey_24dp.svg)
Redistributable licenses place minimal restrictions on how software can be used, modified, and redistributed. 
  * ![checked](https://pkg.go.dev/static/shared/icon/check_circle_gm_grey_24dp.svg) Tagged version ![](https://pkg.go.dev/static/shared/icon/help_gm_grey_24dp.svg)
Modules with tagged versions give importers more predictable builds.
  * ![unchecked](https://pkg.go.dev/static/shared/icon/cancel_gm_grey_24dp.svg) Stable version ![](https://pkg.go.dev/static/shared/icon/help_gm_grey_24dp.svg)
When a project reaches major version v1 it is considered stable.
  * [Learn more about best practices](https://pkg.go.dev/about#best-practices)


## Repository
[ github.com/riverqueue/river ](https://github.com/riverqueue/river "https://github.com/riverqueue/river")
## Links
  * [ ![Open Source Insights Logo](https://pkg.go.dev/static/shared/icon/depsdev-logo.svg) Open Source Insights ](https://deps.dev/go/github.com%2Friverqueue%2Friver/v0.39.0 "View this module on Open Source Insights")


Jump to ... 
  * [ Documentation ](https://pkg.go.dev/github.com/riverqueue/river#section-documentation)
    * [Overview](https://pkg.go.dev/github.com/riverqueue/river#pkg-overview)
    * [ Index ](https://pkg.go.dev/github.com/riverqueue/river#pkg-index)
    * [ Examples ](https://pkg.go.dev/github.com/riverqueue/river#pkg-examples)
      * [ Package (BatchInsert) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-BatchInsert "Package \(BatchInsert\)")
      * [ Package (CompleteJobWithinTx) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-CompleteJobWithinTx "Package \(CompleteJobWithinTx\)")
      * [ Package (CronJob) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-CronJob "Package \(CronJob\)")
      * [ Package (CustomInsertOpts) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-CustomInsertOpts "Package \(CustomInsertOpts\)")
      * [ Package (ErrorHandler) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-ErrorHandler "Package \(ErrorHandler\)")
      * [ Package (GlobalHooks) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-GlobalHooks "Package \(GlobalHooks\)")
      * [ Package (GlobalMiddleware) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-GlobalMiddleware "Package \(GlobalMiddleware\)")
      * [ Package (GracefulShutdown) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-GracefulShutdown "Package \(GracefulShutdown\)")
      * [ Package (GracefulShutdownStopAndCancel) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-GracefulShutdownStopAndCancel "Package \(GracefulShutdownStopAndCancel\)")
      * [ Package (InsertAndWork) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-InsertAndWork "Package \(InsertAndWork\)")
      * [ Package (JobArgsHooks) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-JobArgsHooks "Package \(JobArgsHooks\)")
      * [ Package (JobCancel) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-JobCancel "Package \(JobCancel\)")
      * [ Package (JobCancelFromClient) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-JobCancelFromClient "Package \(JobCancelFromClient\)")
      * [ Package (JobSnooze) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-JobSnooze "Package \(JobSnooze\)")
      * [ Package (PeriodicJob) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-PeriodicJob "Package \(PeriodicJob\)")
      * [ Package (QueuePause) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-QueuePause "Package \(QueuePause\)")
      * [ Package (Resumable) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-Resumable "Package \(Resumable\)")
      * [ Package (ResumableCursor) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-ResumableCursor "Package \(ResumableCursor\)")
      * [ Package (ResumableSetStepTx) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-ResumableSetStepTx "Package \(ResumableSetStepTx\)")
      * [ Package (ScheduledJob) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-ScheduledJob "Package \(ScheduledJob\)")
      * [ Package (Subscription) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-Subscription "Package \(Subscription\)")
      * [ Package (UniqueJob) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-UniqueJob "Package \(UniqueJob\)")
      * [ Package (WorkFunc) ](https://pkg.go.dev/github.com/riverqueue/river#example-package-WorkFunc "Package \(WorkFunc\)")
      * [ ClientFromContext (Pgx) ](https://pkg.go.dev/github.com/riverqueue/river#example-ClientFromContext-Pgx "ClientFromContext \(Pgx\)")
    * [ Constants ](https://pkg.go.dev/github.com/riverqueue/river#pkg-constants)
    * [ Variables ](https://pkg.go.dev/github.com/riverqueue/river#pkg-variables)
    * [ Functions ](https://pkg.go.dev/github.com/riverqueue/river#pkg-functions)
      * [ AddWorker(workers, worker) ](https://pkg.go.dev/github.com/riverqueue/river#AddWorker "AddWorker\(workers, worker\)")
      * [ AddWorkerArgs(workers, jobArgs, worker) ](https://pkg.go.dev/github.com/riverqueue/river#AddWorkerArgs "AddWorkerArgs\(workers, jobArgs, worker\)")
      * [ AddWorkerSafely(workers, worker) ](https://pkg.go.dev/github.com/riverqueue/river#AddWorkerSafely "AddWorkerSafely\(workers, worker\)")
      * [ JobCancel(err) ](https://pkg.go.dev/github.com/riverqueue/river#JobCancel "JobCancel\(err\)")
      * [ JobSnooze(duration) ](https://pkg.go.dev/github.com/riverqueue/river#JobSnooze "JobSnooze\(duration\)")
      * [ MetadataSet(ctx, key, value) ](https://pkg.go.dev/github.com/riverqueue/river#MetadataSet "MetadataSet\(ctx, key, value\)")
      * [ RecordOutput(ctx, output) ](https://pkg.go.dev/github.com/riverqueue/river#RecordOutput "RecordOutput\(ctx, output\)")
      * [ ReindexerIndexNamesDefault() ](https://pkg.go.dev/github.com/riverqueue/river#ReindexerIndexNamesDefault "ReindexerIndexNamesDefault\(\)")
      * [ ResumableSetCursor(ctx, cursor) ](https://pkg.go.dev/github.com/riverqueue/river#ResumableSetCursor "ResumableSetCursor\(ctx, cursor\)")
      * [ ResumableStep(ctx, name, opts, stepFunc) ](https://pkg.go.dev/github.com/riverqueue/river#ResumableStep "ResumableStep\(ctx, name, opts, stepFunc\)")
      * [ ResumableStepCursor(ctx, name, opts, stepFunc) ](https://pkg.go.dev/github.com/riverqueue/river#ResumableStepCursor "ResumableStepCursor\(ctx, name, opts, stepFunc\)")
    * [ Types ](https://pkg.go.dev/github.com/riverqueue/river#pkg-types)
      * [ type Client ](https://pkg.go.dev/github.com/riverqueue/river#Client "type Client")
        * [ ClientFromContext(ctx) ](https://pkg.go.dev/github.com/riverqueue/river#ClientFromContext "ClientFromContext\(ctx\)")
        * [ ClientFromContextSafely(ctx) ](https://pkg.go.dev/github.com/riverqueue/river#ClientFromContextSafely "ClientFromContextSafely\(ctx\)")
        * [ NewClient(driver, config) ](https://pkg.go.dev/github.com/riverqueue/river#NewClient "NewClient\(driver, config\)")
        * [ (c) Driver() ](https://pkg.go.dev/github.com/riverqueue/river#Client.Driver "\(c\) Driver\(\)")
        * [ (c) ID() ](https://pkg.go.dev/github.com/riverqueue/river#Client.ID "\(c\) ID\(\)")
        * [ (c) Insert(ctx, args, opts) ](https://pkg.go.dev/github.com/riverqueue/river#Client.Insert "\(c\) Insert\(ctx, args, opts\)")
        * [ (c) InsertMany(ctx, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertMany "\(c\) InsertMany\(ctx, params\)")
        * [ (c) InsertManyFast(ctx, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertManyFast "\(c\) InsertManyFast\(ctx, params\)")
        * [ (c) InsertManyFastTx(ctx, tx, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertManyFastTx "\(c\) InsertManyFastTx\(ctx, tx, params\)")
        * [ (c) InsertManyTx(ctx, tx, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertManyTx "\(c\) InsertManyTx\(ctx, tx, params\)")
        * [ (c) InsertTx(ctx, tx, args, opts) ](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertTx "\(c\) InsertTx\(ctx, tx, args, opts\)")
        * [ (c) JobCancel(ctx, jobID) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobCancel "\(c\) JobCancel\(ctx, jobID\)")
        * [ (c) JobCancelTx(ctx, tx, jobID) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobCancelTx "\(c\) JobCancelTx\(ctx, tx, jobID\)")
        * [ (c) JobDelete(ctx, id) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobDelete "\(c\) JobDelete\(ctx, id\)")
        * [ (c) JobDeleteMany(ctx, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobDeleteMany "\(c\) JobDeleteMany\(ctx, params\)")
        * [ (c) JobDeleteManyTx(ctx, tx, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobDeleteManyTx "\(c\) JobDeleteManyTx\(ctx, tx, params\)")
        * [ (c) JobDeleteTx(ctx, tx, id) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobDeleteTx "\(c\) JobDeleteTx\(ctx, tx, id\)")
        * [ (c) JobGet(ctx, id) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobGet "\(c\) JobGet\(ctx, id\)")
        * [ (c) JobGetTx(ctx, tx, id) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobGetTx "\(c\) JobGetTx\(ctx, tx, id\)")
        * [ (c) JobList(ctx, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobList "\(c\) JobList\(ctx, params\)")
        * [ (c) JobListTx(ctx, tx, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobListTx "\(c\) JobListTx\(ctx, tx, params\)")
        * [ (c) JobRetry(ctx, id) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobRetry "\(c\) JobRetry\(ctx, id\)")
        * [ (c) JobRetryTx(ctx, tx, id) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobRetryTx "\(c\) JobRetryTx\(ctx, tx, id\)")
        * [ (c) JobUpdate(ctx, id, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobUpdate "\(c\) JobUpdate\(ctx, id, params\)")
        * [ (c) JobUpdateTx(ctx, tx, id, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.JobUpdateTx "\(c\) JobUpdateTx\(ctx, tx, id, params\)")
        * [ (c) Notify() ](https://pkg.go.dev/github.com/riverqueue/river#Client.Notify "\(c\) Notify\(\)")
        * [ (c) PeriodicJobs() ](https://pkg.go.dev/github.com/riverqueue/river#Client.PeriodicJobs "\(c\) PeriodicJobs\(\)")
        * [ (c) Pilot() ](https://pkg.go.dev/github.com/riverqueue/river#Client.Pilot "\(c\) Pilot\(\)")
        * [ (c) QueueGet(ctx, name) ](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueGet "\(c\) QueueGet\(ctx, name\)")
        * [ (c) QueueGetTx(ctx, tx, name) ](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueGetTx "\(c\) QueueGetTx\(ctx, tx, name\)")
        * [ (c) QueueList(ctx, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueList "\(c\) QueueList\(ctx, params\)")
        * [ (c) QueueListTx(ctx, tx, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueListTx "\(c\) QueueListTx\(ctx, tx, params\)")
        * [ (c) QueuePause(ctx, name, opts) ](https://pkg.go.dev/github.com/riverqueue/river#Client.QueuePause "\(c\) QueuePause\(ctx, name, opts\)")
        * [ (c) QueuePauseTx(ctx, tx, name, opts) ](https://pkg.go.dev/github.com/riverqueue/river#Client.QueuePauseTx "\(c\) QueuePauseTx\(ctx, tx, name, opts\)")
        * [ (c) QueueResume(ctx, name, opts) ](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueResume "\(c\) QueueResume\(ctx, name, opts\)")
        * [ (c) QueueResumeTx(ctx, tx, name, opts) ](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueResumeTx "\(c\) QueueResumeTx\(ctx, tx, name, opts\)")
        * [ (c) QueueUpdate(ctx, name, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueUpdate "\(c\) QueueUpdate\(ctx, name, params\)")
        * [ (c) QueueUpdateTx(ctx, tx, name, params) ](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueUpdateTx "\(c\) QueueUpdateTx\(ctx, tx, name, params\)")
        * [ (c) Queues() ](https://pkg.go.dev/github.com/riverqueue/river#Client.Queues "\(c\) Queues\(\)")
        * [ (c) Schema() ](https://pkg.go.dev/github.com/riverqueue/river#Client.Schema "\(c\) Schema\(\)")
        * [ (c) Start(ctx) ](https://pkg.go.dev/github.com/riverqueue/river#Client.Start "\(c\) Start\(ctx\)")
        * [ (c) Stop(ctx) ](https://pkg.go.dev/github.com/riverqueue/river#Client.Stop "\(c\) Stop\(ctx\)")
        * [ (c) StopAndCancel(ctx) ](https://pkg.go.dev/github.com/riverqueue/river#Client.StopAndCancel "\(c\) StopAndCancel\(ctx\)")
        * [ (c) Stopped() ](https://pkg.go.dev/github.com/riverqueue/river#Client.Stopped "\(c\) Stopped\(\)")
        * [ (c) Subscribe(kinds) ](https://pkg.go.dev/github.com/riverqueue/river#Client.Subscribe "\(c\) Subscribe\(kinds\)")
        * [ (c) SubscribeConfig(config) ](https://pkg.go.dev/github.com/riverqueue/river#Client.SubscribeConfig "\(c\) SubscribeConfig\(config\)")
      * [ type ClientNotifyBundle ](https://pkg.go.dev/github.com/riverqueue/river#ClientNotifyBundle "type ClientNotifyBundle")
        * [ (c) RequestResign(ctx) ](https://pkg.go.dev/github.com/riverqueue/river#ClientNotifyBundle.RequestResign "\(c\) RequestResign\(ctx\)")
        * [ (c) RequestResignTx(ctx, tx) ](https://pkg.go.dev/github.com/riverqueue/river#ClientNotifyBundle.RequestResignTx "\(c\) RequestResignTx\(ctx, tx\)")
      * [ type ClientRetryPolicy ](https://pkg.go.dev/github.com/riverqueue/river#ClientRetryPolicy "type ClientRetryPolicy")
      * [ type Config ](https://pkg.go.dev/github.com/riverqueue/river#Config "type Config")
        * [ (c) WithDefaults() ](https://pkg.go.dev/github.com/riverqueue/river#Config.WithDefaults "\(c\) WithDefaults\(\)")
      * [ type DefaultClientRetryPolicy ](https://pkg.go.dev/github.com/riverqueue/river#DefaultClientRetryPolicy "type DefaultClientRetryPolicy")
        * [ (p) NextRetry(job) ](https://pkg.go.dev/github.com/riverqueue/river#DefaultClientRetryPolicy.NextRetry "\(p\) NextRetry\(job\)")
      * [ type ErrorHandler ](https://pkg.go.dev/github.com/riverqueue/river#ErrorHandler "type ErrorHandler")
      * [ type ErrorHandlerResult ](https://pkg.go.dev/github.com/riverqueue/river#ErrorHandlerResult "type ErrorHandlerResult")
      * [ type Event ](https://pkg.go.dev/github.com/riverqueue/river#Event "type Event")
      * [ type EventKind ](https://pkg.go.dev/github.com/riverqueue/river#EventKind "type EventKind")
      * [ type HookDefaults ](https://pkg.go.dev/github.com/riverqueue/river#HookDefaults "type HookDefaults")
        * [ (d) IsHook() ](https://pkg.go.dev/github.com/riverqueue/river#HookDefaults.IsHook "\(d\) IsHook\(\)")
      * [ type HookInsertBeginFunc ](https://pkg.go.dev/github.com/riverqueue/river#HookInsertBeginFunc "type HookInsertBeginFunc")
        * [ (f) InsertBegin(ctx, params) ](https://pkg.go.dev/github.com/riverqueue/river#HookInsertBeginFunc.InsertBegin "\(f\) InsertBegin\(ctx, params\)")
        * [ (f) IsHook() ](https://pkg.go.dev/github.com/riverqueue/river#HookInsertBeginFunc.IsHook "\(f\) IsHook\(\)")
      * [ type HookPeriodicJobsStartFunc ](https://pkg.go.dev/github.com/riverqueue/river#HookPeriodicJobsStartFunc "type HookPeriodicJobsStartFunc")
        * [ (f) IsHook() ](https://pkg.go.dev/github.com/riverqueue/river#HookPeriodicJobsStartFunc.IsHook "\(f\) IsHook\(\)")
        * [ (f) Start(ctx, params) ](https://pkg.go.dev/github.com/riverqueue/river#HookPeriodicJobsStartFunc.Start "\(f\) Start\(ctx, params\)")
      * [ type HookWorkBeginFunc ](https://pkg.go.dev/github.com/riverqueue/river#HookWorkBeginFunc "type HookWorkBeginFunc")
        * [ (f) IsHook() ](https://pkg.go.dev/github.com/riverqueue/river#HookWorkBeginFunc.IsHook "\(f\) IsHook\(\)")
        * [ (f) WorkBegin(ctx, job) ](https://pkg.go.dev/github.com/riverqueue/river#HookWorkBeginFunc.WorkBegin "\(f\) WorkBegin\(ctx, job\)")
      * [ type HookWorkEndFunc ](https://pkg.go.dev/github.com/riverqueue/river#HookWorkEndFunc "type HookWorkEndFunc")
        * [ (f) IsHook() ](https://pkg.go.dev/github.com/riverqueue/river#HookWorkEndFunc.IsHook "\(f\) IsHook\(\)")
        * [ (f) WorkEnd(ctx, job, err) ](https://pkg.go.dev/github.com/riverqueue/river#HookWorkEndFunc.WorkEnd "\(f\) WorkEnd\(ctx, job, err\)")
      * [ type InsertManyParams ](https://pkg.go.dev/github.com/riverqueue/river#InsertManyParams "type InsertManyParams")
      * [ type InsertOpts ](https://pkg.go.dev/github.com/riverqueue/river#InsertOpts "type InsertOpts")
      * [ type Job ](https://pkg.go.dev/github.com/riverqueue/river#Job "type Job")
        * [ JobCompleteTx(ctx, tx, job) ](https://pkg.go.dev/github.com/riverqueue/river#JobCompleteTx "JobCompleteTx\(ctx, tx, job\)")
        * [ ResumableSetStepCursorTx(ctx, tx, job, cursor) ](https://pkg.go.dev/github.com/riverqueue/river#ResumableSetStepCursorTx "ResumableSetStepCursorTx\(ctx, tx, job, cursor\)")
        * [ ResumableSetStepTx(ctx, tx, job) ](https://pkg.go.dev/github.com/riverqueue/river#ResumableSetStepTx "ResumableSetStepTx\(ctx, tx, job\)")
      * [ type JobArgs ](https://pkg.go.dev/github.com/riverqueue/river#JobArgs "type JobArgs")
      * [ type JobArgsWithHooks ](https://pkg.go.dev/github.com/riverqueue/river#JobArgsWithHooks "type JobArgsWithHooks")
      * [ type JobArgsWithInsertOpts ](https://pkg.go.dev/github.com/riverqueue/river#JobArgsWithInsertOpts "type JobArgsWithInsertOpts")
      * [ type JobArgsWithKindAliases ](https://pkg.go.dev/github.com/riverqueue/river#JobArgsWithKindAliases "type JobArgsWithKindAliases")
      * [ type JobCancelError ](https://pkg.go.dev/github.com/riverqueue/river#JobCancelError "type JobCancelError")
      * [ type JobDeleteManyParams ](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams "type JobDeleteManyParams")
        * [ NewJobDeleteManyParams() ](https://pkg.go.dev/github.com/riverqueue/river#NewJobDeleteManyParams "NewJobDeleteManyParams\(\)")
        * [ (p) First(count) ](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.First "\(p\) First\(count\)")
        * [ (p) IDs(ids) ](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.IDs "\(p\) IDs\(ids\)")
        * [ (p) Kinds(kinds) ](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.Kinds "\(p\) Kinds\(kinds\)")
        * [ (p) Priorities(priorities) ](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.Priorities "\(p\) Priorities\(priorities\)")
        * [ (p) Queues(queues) ](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.Queues "\(p\) Queues\(queues\)")
        * [ (p) States(states) ](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.States "\(p\) States\(states\)")
        * [ (p) UnsafeAll() ](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.UnsafeAll "\(p\) UnsafeAll\(\)")
      * [ type JobDeleteManyResult ](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyResult "type JobDeleteManyResult")
      * [ type JobInsertMiddlewareDefaults ](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareDefaults "type JobInsertMiddlewareDefaults")
        * [ (d) InsertMany(ctx, manyParams, doInner) ](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareDefaults.InsertMany "\(d\) InsertMany\(ctx, manyParams, doInner\)")
      * [ type JobInsertMiddlewareFunc ](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareFunc "type JobInsertMiddlewareFunc")
        * [ (f) InsertMany(ctx, manyParams, doInner) ](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareFunc.InsertMany "\(f\) InsertMany\(ctx, manyParams, doInner\)")
        * [ (f) IsMiddleware() ](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareFunc.IsMiddleware "\(f\) IsMiddleware\(\)")
      * [ type JobListCursor ](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor "type JobListCursor")
        * [ JobListCursorFromJob(job) ](https://pkg.go.dev/github.com/riverqueue/river#JobListCursorFromJob "JobListCursorFromJob\(job\)")
        * [ (c) MarshalText() ](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor.MarshalText "\(c\) MarshalText\(\)")
        * [ (c) UnmarshalText(text) ](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor.UnmarshalText "\(c\) UnmarshalText\(text\)")
      * [ type JobListOrderByField ](https://pkg.go.dev/github.com/riverqueue/river#JobListOrderByField "type JobListOrderByField")
      * [ type JobListParams ](https://pkg.go.dev/github.com/riverqueue/river#JobListParams "type JobListParams")
        * [ NewJobListParams() ](https://pkg.go.dev/github.com/riverqueue/river#NewJobListParams "NewJobListParams\(\)")
        * [ (p) After(cursor) ](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.After "\(p\) After\(cursor\)")
        * [ (p) First(count) ](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.First "\(p\) First\(count\)")
        * [ (p) IDs(ids) ](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.IDs "\(p\) IDs\(ids\)")
        * [ (p) Kinds(kinds) ](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Kinds "\(p\) Kinds\(kinds\)")
        * [ (p) Metadata(json) ](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Metadata "\(p\) Metadata\(json\)")
        * [ (p) OrderBy(field, direction) ](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.OrderBy "\(p\) OrderBy\(field, direction\)")
        * [ (p) Priorities(priorities) ](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Priorities "\(p\) Priorities\(priorities\)")
        * [ (p) Queues(queues) ](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Queues "\(p\) Queues\(queues\)")
        * [ (p) States(states) ](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.States "\(p\) States\(states\)")
        * [ (p) Where(sql, namedArgsMany) ](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Where "\(p\) Where\(sql, namedArgsMany\)")
      * [ type JobListResult ](https://pkg.go.dev/github.com/riverqueue/river#JobListResult "type JobListResult")
      * [ type JobSnoozeError ](https://pkg.go.dev/github.com/riverqueue/river#JobSnoozeError "type JobSnoozeError")
      * [ type JobStatistics ](https://pkg.go.dev/github.com/riverqueue/river#JobStatistics "type JobStatistics")
      * [ type JobUpdateParams ](https://pkg.go.dev/github.com/riverqueue/river#JobUpdateParams "type JobUpdateParams")
      * [ type MiddlewareDefaults ](https://pkg.go.dev/github.com/riverqueue/river#MiddlewareDefaults "type MiddlewareDefaults")
        * [ (d) IsMiddleware() ](https://pkg.go.dev/github.com/riverqueue/river#MiddlewareDefaults.IsMiddleware "\(d\) IsMiddleware\(\)")
      * [ type NamedArgs ](https://pkg.go.dev/github.com/riverqueue/river#NamedArgs "type NamedArgs")
      * [ type PeriodicJob ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJob "type PeriodicJob")
        * [ NewPeriodicJob(scheduleFunc, constructorFunc, opts) ](https://pkg.go.dev/github.com/riverqueue/river#NewPeriodicJob "NewPeriodicJob\(scheduleFunc, constructorFunc, opts\)")
      * [ type PeriodicJobBundle ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle "type PeriodicJobBundle")
        * [ (b) Add(periodicJob) ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.Add "\(b\) Add\(periodicJob\)")
        * [ (b) AddMany(periodicJobs) ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.AddMany "\(b\) AddMany\(periodicJobs\)")
        * [ (b) AddManySafely(periodicJobs) ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.AddManySafely "\(b\) AddManySafely\(periodicJobs\)")
        * [ (b) AddSafely(periodicJob) ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.AddSafely "\(b\) AddSafely\(periodicJob\)")
        * [ (b) Clear() ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.Clear "\(b\) Clear\(\)")
        * [ (b) Remove(periodicJobHandle) ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.Remove "\(b\) Remove\(periodicJobHandle\)")
        * [ (b) RemoveByID(id) ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.RemoveByID "\(b\) RemoveByID\(id\)")
        * [ (b) RemoveMany(periodicJobHandles) ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.RemoveMany "\(b\) RemoveMany\(periodicJobHandles\)")
        * [ (b) RemoveManyByID(ids) ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.RemoveManyByID "\(b\) RemoveManyByID\(ids\)")
      * [ type PeriodicJobConstructor ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobConstructor "type PeriodicJobConstructor")
      * [ type PeriodicJobOpts ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobOpts "type PeriodicJobOpts")
      * [ type PeriodicSchedule ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicSchedule "type PeriodicSchedule")
        * [ NeverSchedule() ](https://pkg.go.dev/github.com/riverqueue/river#NeverSchedule "NeverSchedule\(\)")
        * [ PeriodicInterval(interval) ](https://pkg.go.dev/github.com/riverqueue/river#PeriodicInterval "PeriodicInterval\(interval\)")
      * [ type QueueAlreadyAddedError ](https://pkg.go.dev/github.com/riverqueue/river#QueueAlreadyAddedError "type QueueAlreadyAddedError")
        * [ (e) Error() ](https://pkg.go.dev/github.com/riverqueue/river#QueueAlreadyAddedError.Error "\(e\) Error\(\)")
        * [ (e) Is(target) ](https://pkg.go.dev/github.com/riverqueue/river#QueueAlreadyAddedError.Is "\(e\) Is\(target\)")
      * [ type QueueBundle ](https://pkg.go.dev/github.com/riverqueue/river#QueueBundle "type QueueBundle")
        * [ (b) Add(queueName, queueConfig) ](https://pkg.go.dev/github.com/riverqueue/river#QueueBundle.Add "\(b\) Add\(queueName, queueConfig\)")
        * [ (b) Remove(ctx, queueName) ](https://pkg.go.dev/github.com/riverqueue/river#QueueBundle.Remove "\(b\) Remove\(ctx, queueName\)")
      * [ type QueueConfig ](https://pkg.go.dev/github.com/riverqueue/river#QueueConfig "type QueueConfig")
      * [ type QueueListParams ](https://pkg.go.dev/github.com/riverqueue/river#QueueListParams "type QueueListParams")
        * [ NewQueueListParams() ](https://pkg.go.dev/github.com/riverqueue/river#NewQueueListParams "NewQueueListParams\(\)")
        * [ (p) First(count) ](https://pkg.go.dev/github.com/riverqueue/river#QueueListParams.First "\(p\) First\(count\)")
      * [ type QueueListResult ](https://pkg.go.dev/github.com/riverqueue/river#QueueListResult "type QueueListResult")
      * [ type QueueNotFoundError ](https://pkg.go.dev/github.com/riverqueue/river#QueueNotFoundError "type QueueNotFoundError")
        * [ (e) Error() ](https://pkg.go.dev/github.com/riverqueue/river#QueueNotFoundError.Error "\(e\) Error\(\)")
        * [ (e) Is(target) ](https://pkg.go.dev/github.com/riverqueue/river#QueueNotFoundError.Is "\(e\) Is\(target\)")
      * [ type QueuePauseOpts ](https://pkg.go.dev/github.com/riverqueue/river#QueuePauseOpts "type QueuePauseOpts")
      * [ type QueueUpdateParams ](https://pkg.go.dev/github.com/riverqueue/river#QueueUpdateParams "type QueueUpdateParams")
      * [ type SortOrder ](https://pkg.go.dev/github.com/riverqueue/river#SortOrder "type SortOrder")
      * [ type StepOpts ](https://pkg.go.dev/github.com/riverqueue/river#StepOpts "type StepOpts")
      * [ type SubscribeConfig ](https://pkg.go.dev/github.com/riverqueue/river#SubscribeConfig "type SubscribeConfig")
      * [ type TestConfig ](https://pkg.go.dev/github.com/riverqueue/river#TestConfig "type TestConfig")
      * [ type UniqueOpts ](https://pkg.go.dev/github.com/riverqueue/river#UniqueOpts "type UniqueOpts")
      * [ type UnknownJobKindError ](https://pkg.go.dev/github.com/riverqueue/river#UnknownJobKindError "type UnknownJobKindError")
      * [ type Worker ](https://pkg.go.dev/github.com/riverqueue/river#Worker "type Worker")
        * [ WorkFunc(f) ](https://pkg.go.dev/github.com/riverqueue/river#WorkFunc "WorkFunc\(f\)")
      * [ type WorkerDefaults ](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults "type WorkerDefaults")
        * [ (w) Middleware() ](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults.Middleware "\(w\) Middleware\(\)")
        * [ (w) NextRetry() ](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults.NextRetry "\(w\) NextRetry\(\)")
        * [ (w) Timeout() ](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults.Timeout "\(w\) Timeout\(\)")
      * [ type WorkerMiddlewareDefaults ](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareDefaults "type WorkerMiddlewareDefaults")
        * [ (d) Work(ctx, job, doInner) ](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareDefaults.Work "\(d\) Work\(ctx, job, doInner\)")
      * [ type WorkerMiddlewareFunc ](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareFunc "type WorkerMiddlewareFunc")
        * [ (f) IsMiddleware() ](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareFunc.IsMiddleware "\(f\) IsMiddleware\(\)")
        * [ (f) Work(ctx, job, doInner) ](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareFunc.Work "\(f\) Work\(ctx, job, doInner\)")
      * [ type Workers ](https://pkg.go.dev/github.com/riverqueue/river#Workers "type Workers")
        * [ NewWorkers() ](https://pkg.go.dev/github.com/riverqueue/river#NewWorkers "NewWorkers\(\)")
  * [ Source Files ](https://pkg.go.dev/github.com/riverqueue/river#section-sourcefiles)
  * [ Directories ](https://pkg.go.dev/github.com/riverqueue/river#section-directories)


Documentation Source Files Directories Overview Index Examples Constants Variables Functions Types Package (BatchInsert) Package (CompleteJobWithinTx) Package (CronJob) Package (CustomInsertOpts) Package (ErrorHandler) Package (GlobalHooks) Package (GlobalMiddleware) Package (GracefulShutdown) Package (GracefulShutdownStopAndCancel) Package (InsertAndWork) Package (JobArgsHooks) Package (JobCancel) Package (JobCancelFromClient) Package (JobSnooze) Package (PeriodicJob) Package (QueuePause) Package (Resumable) Package (ResumableCursor) Package (ResumableSetStepTx) Package (ScheduledJob) Package (Subscription) Package (UniqueJob) Package (WorkFunc) ClientFromContext (Pgx) AddWorker(workers, worker) AddWorkerArgs(workers, jobArgs, worker) AddWorkerSafely(workers, worker) JobCancel(err) JobSnooze(duration) MetadataSet(ctx, key, value) RecordOutput(ctx, output) ReindexerIndexNamesDefault() ResumableSetCursor(ctx, cursor) ResumableStep(ctx, name, opts, stepFunc) ResumableStepCursor(ctx, name, opts, stepFunc) type Client type ClientNotifyBundle type ClientRetryPolicy type Config type DefaultClientRetryPolicy type ErrorHandler type ErrorHandlerResult type Event type EventKind type HookDefaults type HookInsertBeginFunc type HookPeriodicJobsStartFunc type HookWorkBeginFunc type HookWorkEndFunc type InsertManyParams type InsertOpts type Job type JobArgs type JobArgsWithHooks type JobArgsWithInsertOpts type JobArgsWithKindAliases type JobCancelError type JobDeleteManyParams type JobDeleteManyResult type JobInsertMiddlewareDefaults type JobInsertMiddlewareFunc type JobListCursor type JobListOrderByField type JobListParams type JobListResult type JobSnoozeError type JobStatistics type JobUpdateParams type MiddlewareDefaults type NamedArgs type PeriodicJob type PeriodicJobBundle type PeriodicJobConstructor type PeriodicJobOpts type PeriodicSchedule type QueueAlreadyAddedError type QueueBundle type QueueConfig type QueueListParams type QueueListResult type QueueNotFoundError type QueuePauseOpts type QueueUpdateParams type SortOrder type StepOpts type SubscribeConfig type TestConfig type UniqueOpts type UnknownJobKindError type Worker type WorkerDefaults type WorkerMiddlewareDefaults type WorkerMiddlewareFunc type Workers ClientFromContext(ctx) ClientFromContextSafely(ctx) NewClient(driver, config) (c) Driver() (c) ID() (c) Insert(ctx, args, opts) (c) InsertMany(ctx, params) (c) InsertManyFast(ctx, params) (c) InsertManyFastTx(ctx, tx, params) (c) InsertManyTx(ctx, tx, params) (c) InsertTx(ctx, tx, args, opts) (c) JobCancel(ctx, jobID) (c) JobCancelTx(ctx, tx, jobID) (c) JobDelete(ctx, id) (c) JobDeleteMany(ctx, params) (c) JobDeleteManyTx(ctx, tx, params) (c) JobDeleteTx(ctx, tx, id) (c) JobGet(ctx, id) (c) JobGetTx(ctx, tx, id) (c) JobList(ctx, params) (c) JobListTx(ctx, tx, params) (c) JobRetry(ctx, id) (c) JobRetryTx(ctx, tx, id) (c) JobUpdate(ctx, id, params) (c) JobUpdateTx(ctx, tx, id, params) (c) Notify() (c) PeriodicJobs() (c) Pilot() (c) QueueGet(ctx, name) (c) QueueGetTx(ctx, tx, name) (c) QueueList(ctx, params) (c) QueueListTx(ctx, tx, params) (c) QueuePause(ctx, name, opts) (c) QueuePauseTx(ctx, tx, name, opts) (c) QueueResume(ctx, name, opts) (c) QueueResumeTx(ctx, tx, name, opts) (c) QueueUpdate(ctx, name, params) (c) QueueUpdateTx(ctx, tx, name, params) (c) Queues() (c) Schema() (c) Start(ctx) (c) Stop(ctx) (c) StopAndCancel(ctx) (c) Stopped() (c) Subscribe(kinds) (c) SubscribeConfig(config) (c) RequestResign(ctx) (c) RequestResignTx(ctx, tx) (c) WithDefaults() (p) NextRetry(job) (d) IsHook() (f) InsertBegin(ctx, params) (f) IsHook() (f) IsHook() (f) Start(ctx, params) (f) IsHook() (f) WorkBegin(ctx, job) (f) IsHook() (f) WorkEnd(ctx, job, err) JobCompleteTx(ctx, tx, job) ResumableSetStepCursorTx(ctx, tx, job, cursor) ResumableSetStepTx(ctx, tx, job) NewJobDeleteManyParams() (p) First(count) (p) IDs(ids) (p) Kinds(kinds) (p) Priorities(priorities) (p) Queues(queues) (p) States(states) (p) UnsafeAll() (d) InsertMany(ctx, manyParams, doInner) (f) InsertMany(ctx, manyParams, doInner) (f) IsMiddleware() JobListCursorFromJob(job) (c) MarshalText() (c) UnmarshalText(text) NewJobListParams() (p) After(cursor) (p) First(count) (p) IDs(ids) (p) Kinds(kinds) (p) Metadata(json) (p) OrderBy(field, direction) (p) Priorities(priorities) (p) Queues(queues) (p) States(states) (p) Where(sql, namedArgsMany) (d) IsMiddleware() NewPeriodicJob(scheduleFunc, constructorFunc, opts) (b) Add(periodicJob) (b) AddMany(periodicJobs) (b) AddManySafely(periodicJobs) (b) AddSafely(periodicJob) (b) Clear() (b) Remove(periodicJobHandle) (b) RemoveByID(id) (b) RemoveMany(periodicJobHandles) (b) RemoveManyByID(ids) NeverSchedule() PeriodicInterval(interval) (e) Error() (e) Is(target) (b) Add(queueName, queueConfig) (b) Remove(ctx, queueName) NewQueueListParams() (p) First(count) (e) Error() (e) Is(target) WorkFunc(f) (w) Middleware() (w) NextRetry() (w) Timeout() (d) Work(ctx, job, doInner) (f) IsMiddleware() (f) Work(ctx, job, doInner) NewWorkers()
##  ![](https://pkg.go.dev/static/shared/icon/code_gm_grey_24dp.svg) Documentation [¶](https://pkg.go.dev/github.com/riverqueue/river#section-documentation "Go to Documentation")
### Overview [¶](https://pkg.go.dev/github.com/riverqueue/river#pkg-overview "Go to Overview")
  * [Job args and workers](https://pkg.go.dev/github.com/riverqueue/river#hdr-Job_args_and_workers)
  * [Registering workers](https://pkg.go.dev/github.com/riverqueue/river#hdr-Registering_workers)
  * [Starting a client](https://pkg.go.dev/github.com/riverqueue/river#hdr-Starting_a_client)
  * [Inserting jobs](https://pkg.go.dev/github.com/riverqueue/river#hdr-Inserting_jobs)
  * [Other features](https://pkg.go.dev/github.com/riverqueue/river#hdr-Other_features)
  * [Development](https://pkg.go.dev/github.com/riverqueue/river#hdr-Development)


Package river is a robust high-performance job processing system for Go and Postgres. 
See [homepage](https://riverqueue.com), [docs](https://riverqueue.com/docs), and [godoc](https://pkg.go.dev/github.com/riverqueue/river), as well as the [River UI](https://github.com/riverqueue/riverui). 
Being built for Postgres, River encourages the use of the same database for application data and job queue. By enqueueing jobs transactionally along with other database changes, whole classes of distributed systems problems are avoided. Jobs are guaranteed to be enqueued if their transaction commits, are removed if their transaction rolls back, and aren't visible for work _until_ commit. See [transactional enqueueing](https://riverqueue.com/docs/transactional-enqueueing) for more background on this philosophy. 
#### Job args and workers [¶](https://pkg.go.dev/github.com/riverqueue/river#hdr-Job_args_and_workers "Go to Job args and workers")
Jobs are defined in struct pairs, with an implementation of [`JobArgs`](https://pkg.go.dev/github.com/riverqueue/river#JobArgs) and one of [`Worker`](https://pkg.go.dev/github.com/riverqueue/river#Worker). 
Job args contain `json` annotations and define how jobs are serialized to and from the database, along with a "kind", a stable string that uniquely identifies the job. 

```
type SortArgs struct {
	// Strings is a slice of strings to sort.
	Strings []string `json:"strings"`
}

func (SortArgs) Kind() string { return "sort" }

```

Workers expose a `Work` function that dictates how jobs run. 

```
type SortWorker struct {
    // An embedded WorkerDefaults sets up default methods to fulfill the rest of
    // the Worker interface:
    river.WorkerDefaults[SortArgs]
}

func (w *SortWorker) Work(ctx context.Context, job *river.Job[SortArgs]) error {
    sort.Strings(job.Args.Strings)
    fmt.Printf("Sorted strings: %+v\n", job.Args.Strings)
    return nil
}

```

#### Registering workers [¶](https://pkg.go.dev/github.com/riverqueue/river#hdr-Registering_workers "Go to Registering workers")
Jobs are uniquely identified by their "kind" string. Workers are registered on start up so that River knows how to assign jobs to workers: 

```
workers := river.NewWorkers()
// AddWorker panics if the worker is already registered or invalid:
river.AddWorker(workers, &SortWorker{})

```

#### Starting a client [¶](https://pkg.go.dev/github.com/riverqueue/river#hdr-Starting_a_client "Go to Starting a client")
A River [`Client`](https://pkg.go.dev/github.com/riverqueue/river#Client) provides an interface for job insertion and manages job processing and [maintenance services](https://riverqueue.com/docs/maintenance-services). A client's created with a database pool, [driver](https://riverqueue.com/docs/database-drivers), and config struct containing a `Workers` bundle and other settings. Here's a client `Client` working one queue (`"default"`) with up to 100 worker goroutines at a time: 

```
riverClient, err := river.NewClient(riverpgxv5.New(dbPool), &river.Config{
    Queues: map[string]river.QueueConfig{
        river.QueueDefault: {MaxWorkers: 100},
    },
    Workers: workers,
})
if err != nil {
    panic(err)
}

// Run the client inline. All executed jobs will inherit from ctx:
if err := riverClient.Start(ctx); err != nil {
    panic(err)
}

```

## Insert-only clients 
It's often desirable to have a client that'll be used for inserting jobs, but not working them. This is possible by omitting the `Queues` configuration, and skipping the call to `Start`: 

```
riverClient, err := river.NewClient(riverpgxv5.New(dbPool), &river.Config{
    Workers: workers,
})
if err != nil {
    panic(err)
}

```

`Workers` can also be omitted, but it's better to include it so River can check that inserted job kinds have a worker that can run them. 
## Stopping 
The client should also be stopped on program shutdown: 

```
// Stop fetching new work and wait for active jobs to finish.
if err := riverClient.Stop(ctx); err != nil {
    panic(err)
}

```

There are some complexities around ensuring clients stop cleanly, but also in a timely manner. See [graceful shutdown](https://riverqueue.com/docs/graceful-shutdown) for more details on River's stop modes. 
#### Inserting jobs [¶](https://pkg.go.dev/github.com/riverqueue/river#hdr-Inserting_jobs "Go to Inserting jobs")
[`Client.InsertTx`](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertTx) is used in conjunction with an instance of job args to insert a job to work on a transaction: 

```
_, err = riverClient.InsertTx(ctx, tx, SortArgs{
    Strings: []string{
        "whale", "tiger", "bear",
    },
}, nil)

if err != nil {
    panic(err)
}

```

See the [`InsertAndWork` example](https://pkg.go.dev/github.com/riverqueue/river#example-package-InsertAndWork) for complete code. 
#### Other features [¶](https://pkg.go.dev/github.com/riverqueue/river#hdr-Other_features "Go to Other features")
  * [Batch job insertion](https://riverqueue.com/docs/batch-job-insertion) for efficiently inserting many jobs at once using Postgres `COPY FROM`. 
  * [Cancelling jobs](https://riverqueue.com/docs/cancelling-jobs) from inside a work function. 
  * [Error and panic handling](https://riverqueue.com/docs/error-handling). 
  * [Multiple queues](https://riverqueue.com/docs/multiple-queues) to better guarantee job throughput, worker availability, and isolation between components. 
  * [Periodic and cron jobs](https://riverqueue.com/docs/periodic-jobs). 
  * [Scheduled jobs](https://riverqueue.com/docs/scheduled-jobs) that run automatically at their scheduled time in the future. 
  * [Snoozing jobs](https://riverqueue.com/docs/snoozing-jobs) from inside a work function. 
  * [Subscriptions](https://riverqueue.com/docs/subscriptions) to queue activity and statistics, providing easy hooks for telemetry like logging and metrics. 
  * [Test helpers](https://riverqueue.com/docs/testing) to verify that jobs are inserted as expected. 
  * [Transactional job completion](https://riverqueue.com/docs/transactional-job-completion) to guarantee job completion commits with other changes in a transaction. 
  * [Unique jobs](https://riverqueue.com/docs/unique-jobs) by args, period, queue, and state. 
  * [Web UI](https://github.com/riverqueue/riverui) for inspecting and interacting with jobs and queues. 
  * [Work functions](https://riverqueue.com/docs/work-functions) for simplified worker implementation. 


## Cross language enqueueing 
River supports inserting jobs in some non-Go languages which are then worked by Go implementations. This may be desirable in performance sensitive cases so that jobs can take advantage of Go's fast runtime. 
  * [Inserting jobs from Python](https://riverqueue.com/docs/python).
  * [Inserting jobs from Ruby](https://riverqueue.com/docs/ruby).


#### Development [¶](https://pkg.go.dev/github.com/riverqueue/river#hdr-Development "Go to Development")
See [developing River](https://github.com/riverqueue/river/blob/master/docs/development.md). 
Example (BatchInsert) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-BatchInsert "Go to Example \(BatchInsert\)")
Example_batchInsert demonstrates how many jobs can be inserted for work as part of a single operation. 

```
Output:
Inserted 5 jobs
Worked a job
Worked a job
Worked a job
Worked a job
Worked a job

```

Share Format Run
Example (CompleteJobWithinTx) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-CompleteJobWithinTx "Go to Example \(CompleteJobWithinTx\)")
Example_completeJobWithinTx demonstrates how to transactionally complete a job alongside other database changes being made. 

```
Output:
Transitioned TransactionalWorker job from "running" to "completed"

```

Share Format Run
Example (CronJob) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-CronJob "Go to Example \(CronJob\)")
Example_cronJob demonstrates how to create a cron job with a more complex schedule using a third party cron package to parse more elaborate crontab syntax. 

```
Output:
This job will run once immediately then every hour on the half hour

```

Share Format Run
Example (CustomInsertOpts) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-CustomInsertOpts "Go to Example \(CustomInsertOpts\)")
Example_customInsertOpts demonstrates the use of a job with custom job-specific insertion options. 

```
Output:
Ran in queue: high_priority
Ran in queue: high_priority

```

Share Format Run
Example (ErrorHandler) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-ErrorHandler "Go to Example \(ErrorHandler\)")
Example_errorHandler demonstrates how to use the ErrorHandler interface for custom application telemetry. 

```
Output:
Job errored with: this job errored
Job panicked with: this job panicked

```

Share Format Run
Example (GlobalHooks) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-GlobalHooks "Go to Example \(GlobalHooks\)")
Example_globalHooks demonstrates the use of hooks to modify River behavior which are global to a River client. 

```
Output:
BothInsertAndWorkBeginHook.InsertBegin ran
InsertBeginHook.InsertBegin ran
BothInsertAndWorkBeginHook.WorkBegin ran
WorkBeginHook.WorkBegin ran
NoOpWorker.Work ran

```

Share Format Run
Example (GlobalMiddleware) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-GlobalMiddleware "Go to Example \(GlobalMiddleware\)")
Example_globalMiddleware demonstrates the use of middleware to modify River behavior which are global to a River client. 

```
Output:
JobBothInsertAndWorkMiddleware.InsertMany ran
JobInsertMiddleware.InsertMany ran
JobBothInsertAndWorkMiddleware.Work ran
WorkerMiddleware.Work ran
NoOpWorker.Work ran

```

Share Format Run
Example (GracefulShutdown) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-GracefulShutdown "Go to Example \(GracefulShutdown\)")
Example_gracefulShutdown demonstrates graceful stop using SoftStopTimeout. When a SIGINT/SIGTERM arrives, the start context is cancelled, which initiates a soft stop. If running jobs don't finish within the configured SoftStopTimeout, their contexts are automatically cancelled (hard stop). 

```
Output:
Working job that doesn't finish until cancelled
Received SIGINT/SIGTERM; initiating soft stop (waiting for cancelled jobs to finish)
Job cancelled

```

Share Format Run
Example (GracefulShutdownStopAndCancel) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-GracefulShutdownStopAndCancel "Go to Example \(GracefulShutdownStopAndCancel\)")
Example_gracefulShutdownStopCancel demonstrates graceful stop with explicit fallback to StopAndCancel. When a SIGINT/SIGTERM arrives, Stop initiates a soft stop. If running jobs don't finish before the soft stop context expires, StopAndCancel cancels their contexts (hard stop). This example is intended to demonstrate advanced use of StopAndCancel. Generally, prefer the method shown in Example_gracefulShutdown over the one here. 

```
Output:
Working job that doesn't finish until cancelled
Received SIGINT/SIGTERM; initiating soft stop (waiting for jobs to finish)
Soft stop timeout; cancelling remaining jobs
Job cancelled

```

Example (InsertAndWork) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-InsertAndWork "Go to Example \(InsertAndWork\)")
Example_insertAndWork demonstrates how to register job workers, start a client, and insert a job on it to be worked. 

```
Output:
Sorted strings: [bear tiger whale]

```

Share Format Run
Example (JobArgsHooks) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-JobArgsHooks "Go to Example \(JobArgsHooks\)")
Example_jobArgsHooks demonstrates the use of hooks to modify River behavior. 

```
Output:
JobWithHooksInsertAndWorkBeginHook.InsertBegin ran
JobWithHooksInsertBeginHook.InsertBegin ran
JobWithHooksInsertAndWorkBeginHook.WorkBegin ran
JobWithHooksWorkBeginHook.WorkBegin ran
JobWithHooksWorker.Work ran

```

Share Format Run
Example (JobCancel) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-JobCancel "Go to Example \(JobCancel\)")
Example_jobCancel demonstrates how to permanently cancel a job from within Work using JobCancel. 

```
Output:
cancelling job

```

Share Format Run
Example (JobCancelFromClient) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-JobCancelFromClient "Go to Example \(JobCancelFromClient\)")
Example_jobCancelFromClient demonstrates how to permanently cancel a job from any Client using JobCancel. 

```
Output:
msg="jobexecutor.JobExecutor: job cancelled remotely"

```

Share Format Run
Example (JobSnooze) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-JobSnooze "Go to Example \(JobSnooze\)")
Example_jobSnooze demonstrates how to snooze a job from within Work using JobSnooze. The job will be run again after 5 minutes and the snooze attempt will decrement the job's attempt count, ensuring that one can snooze as many times as desired without being impacted by the max attempts. 

```
Output:
snoozing job for 5 minutes

```

Share Format Run
Example (PeriodicJob) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-PeriodicJob "Go to Example \(PeriodicJob\)")
Example_periodicJob demonstrates the use of a periodic job. 

```
Output:
This job will run once immediately then approximately once every 15 minutes

```

Share Format Run
Example (QueuePause) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-QueuePause "Go to Example \(QueuePause\)")
Example_queuePause demonstrates how to pause queues to prevent them from working new jobs, and later resume them. 

```
Output:
Pausing unreliable_external_service queue
Inserting one job each into unreliable and reliable queues
Job worked on reliable_jobs queue
Resuming unreliable_external_service queue
Job worked on unreliable_external_service queue

```

Share Format Run
Example (Resumable) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-Resumable "Go to Example \(Resumable\)")
Example_resumable demonstrates the use of a "resumable job", a job that has multiple steps, and which can be resumed after each one. 

```
Output:
Step 1
Step 2
Step 3

```

Share Format Run
Example (ResumableCursor) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-ResumableCursor "Go to Example \(ResumableCursor\)")
Example_resumableCursor demonstrates the use of a resumable cursor step, a step that can store arbitrary JSON state to resume a partially completed loop. 

```
Output:
Processed 1
Processed 2
Processed 3

```

Share Format Run
Example (ResumableSetStepTx) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-ResumableSetStepTx "Go to Example \(ResumableSetStepTx\)")
Example_resumableSetStepTx demonstrates how to transactionally persist a resumable step so it survives a failed attempt. 

```
Output:
Persisted resumable step: durable_step

```

Share Format Run
Example (ScheduledJob) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-ScheduledJob "Go to Example \(ScheduledJob\)")
Example_scheduledJob demonstrates how to schedule a job to be worked in the future. 

```
Output:

```

Share Format Run
Example (Subscription) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-Subscription "Go to Example \(Subscription\)")
Example_subscription demonstrates the use of client subscriptions to receive events containing information about worked jobs. 

```
Output:
Got job with state: completed
Got job with state: available
Got job with state: cancelled
Client stopped
Channel is closed
Channel is closed
Channel is closed

```

Share Format Run
Example (UniqueJob) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-UniqueJob "Go to Example \(UniqueJob\)")
Example_uniqueJob demonstrates the use of a job with custom job-specific insertion options. 

```
Output:
Reconciled account 1; new total: 900
Reconciled account 2; new total: 1

```

Share Format Run
Example (WorkFunc) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-package-WorkFunc "Go to Example \(WorkFunc\)")
Example_workFunc demonstrates the use of river.WorkFunc, which can be used to easily add a worker with only a function instead of having to implement a full worker struct. 

```
Output:
Message: hello from a function!

```

Share Format Run
### Index [¶](https://pkg.go.dev/github.com/riverqueue/river#pkg-index "Go to Index")
  * [Constants](https://pkg.go.dev/github.com/riverqueue/river#pkg-constants)
  * [Variables](https://pkg.go.dev/github.com/riverqueue/river#pkg-variables)
  * [func AddWorker[T JobArgs](workers *Workers, worker Worker[T])](https://pkg.go.dev/github.com/riverqueue/river#AddWorker)
  * [func AddWorkerArgs[T JobArgs](workers *Workers, jobArgs T, worker Worker[T])](https://pkg.go.dev/github.com/riverqueue/river#AddWorkerArgs)
  * [func AddWorkerSafely[T JobArgs](workers *Workers, worker Worker[T]) error](https://pkg.go.dev/github.com/riverqueue/river#AddWorkerSafely)
  * [func JobCancel(err error) error](https://pkg.go.dev/github.com/riverqueue/river#JobCancel)
  * [func JobSnooze(duration time.Duration) error](https://pkg.go.dev/github.com/riverqueue/river#JobSnooze)
  * [func MetadataSet(ctx context.Context, key string, value any) error](https://pkg.go.dev/github.com/riverqueue/river#MetadataSet)
  * [func RecordOutput(ctx context.Context, output any) error](https://pkg.go.dev/github.com/riverqueue/river#RecordOutput)
  * [func ReindexerIndexNamesDefault() []string](https://pkg.go.dev/github.com/riverqueue/river#ReindexerIndexNamesDefault)
  * [func ResumableSetCursor[TCursor any](ctx context.Context, cursor TCursor) error](https://pkg.go.dev/github.com/riverqueue/river#ResumableSetCursor)
  * [func ResumableStep(ctx context.Context, name string, opts *StepOpts, ...)](https://pkg.go.dev/github.com/riverqueue/river#ResumableStep)
  * [func ResumableStepCursor[TCursor any](ctx context.Context, name string, opts *StepOpts, ...)](https://pkg.go.dev/github.com/riverqueue/river#ResumableStepCursor)
  * [type Client](https://pkg.go.dev/github.com/riverqueue/river#Client)
  *     * [func ClientFromContext[TTx any](ctx context.Context) *Client[TTx]](https://pkg.go.dev/github.com/riverqueue/river#ClientFromContext)
    * [func ClientFromContextSafely[TTx any](ctx context.Context) (*Client[TTx], error)](https://pkg.go.dev/github.com/riverqueue/river#ClientFromContextSafely)
    * [func NewClient[TTx any](driver riverdriver.Driver[TTx], config *Config) (*Client[TTx], error)](https://pkg.go.dev/github.com/riverqueue/river#NewClient)
  *     * [func (c *Client[TTx]) Driver() riverdriver.Driver[TTx]](https://pkg.go.dev/github.com/riverqueue/river#Client.Driver)
    * [func (c *Client[TTx]) ID() string](https://pkg.go.dev/github.com/riverqueue/river#Client.ID)
    * [func (c *Client[TTx]) Insert(ctx context.Context, args JobArgs, opts *InsertOpts) (*rivertype.JobInsertResult, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.Insert)
    * [func (c *Client[TTx]) InsertMany(ctx context.Context, params []InsertManyParams) ([]*rivertype.JobInsertResult, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertMany)
    * [func (c *Client[TTx]) InsertManyFast(ctx context.Context, params []InsertManyParams) (int, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertManyFast)
    * [func (c *Client[TTx]) InsertManyFastTx(ctx context.Context, tx TTx, params []InsertManyParams) (int, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertManyFastTx)
    * [func (c *Client[TTx]) InsertManyTx(ctx context.Context, tx TTx, params []InsertManyParams) ([]*rivertype.JobInsertResult, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertManyTx)
    * [func (c *Client[TTx]) InsertTx(ctx context.Context, tx TTx, args JobArgs, opts *InsertOpts) (*rivertype.JobInsertResult, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertTx)
    * [func (c *Client[TTx]) JobCancel(ctx context.Context, jobID int64) (*rivertype.JobRow, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobCancel)
    * [func (c *Client[TTx]) JobCancelTx(ctx context.Context, tx TTx, jobID int64) (*rivertype.JobRow, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobCancelTx)
    * [func (c *Client[TTx]) JobDelete(ctx context.Context, id int64) (*rivertype.JobRow, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobDelete)
    * [func (c *Client[TTx]) JobDeleteMany(ctx context.Context, params *JobDeleteManyParams) (*JobDeleteManyResult, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobDeleteMany)
    * [func (c *Client[TTx]) JobDeleteManyTx(ctx context.Context, tx TTx, params *JobDeleteManyParams) (*JobDeleteManyResult, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobDeleteManyTx)
    * [func (c *Client[TTx]) JobDeleteTx(ctx context.Context, tx TTx, id int64) (*rivertype.JobRow, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobDeleteTx)
    * [func (c *Client[TTx]) JobGet(ctx context.Context, id int64) (*rivertype.JobRow, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobGet)
    * [func (c *Client[TTx]) JobGetTx(ctx context.Context, tx TTx, id int64) (*rivertype.JobRow, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobGetTx)
    * [func (c *Client[TTx]) JobList(ctx context.Context, params *JobListParams) (*JobListResult, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobList)
    * [func (c *Client[TTx]) JobListTx(ctx context.Context, tx TTx, params *JobListParams) (*JobListResult, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobListTx)
    * [func (c *Client[TTx]) JobRetry(ctx context.Context, id int64) (*rivertype.JobRow, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobRetry)
    * [func (c *Client[TTx]) JobRetryTx(ctx context.Context, tx TTx, id int64) (*rivertype.JobRow, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobRetryTx)
    * [func (c *Client[TTx]) JobUpdate(ctx context.Context, id int64, params *JobUpdateParams) (*rivertype.JobRow, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobUpdate)
    * [func (c *Client[TTx]) JobUpdateTx(ctx context.Context, tx TTx, id int64, params *JobUpdateParams) (*rivertype.JobRow, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.JobUpdateTx)
    * [func (c *Client[TTx]) Notify() *ClientNotifyBundle[TTx]](https://pkg.go.dev/github.com/riverqueue/river#Client.Notify)
    * [func (c *Client[TTx]) PeriodicJobs() *PeriodicJobBundle](https://pkg.go.dev/github.com/riverqueue/river#Client.PeriodicJobs)
    * [func (c *Client[TTx]) Pilot() riverpilot.Pilot](https://pkg.go.dev/github.com/riverqueue/river#Client.Pilot)
    * [func (c *Client[TTx]) QueueGet(ctx context.Context, name string) (*rivertype.Queue, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueGet)
    * [func (c *Client[TTx]) QueueGetTx(ctx context.Context, tx TTx, name string) (*rivertype.Queue, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueGetTx)
    * [func (c *Client[TTx]) QueueList(ctx context.Context, params *QueueListParams) (*QueueListResult, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueList)
    * [func (c *Client[TTx]) QueueListTx(ctx context.Context, tx TTx, params *QueueListParams) (*QueueListResult, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueListTx)
    * [func (c *Client[TTx]) QueuePause(ctx context.Context, name string, opts *QueuePauseOpts) error](https://pkg.go.dev/github.com/riverqueue/river#Client.QueuePause)
    * [func (c *Client[TTx]) QueuePauseTx(ctx context.Context, tx TTx, name string, opts *QueuePauseOpts) error](https://pkg.go.dev/github.com/riverqueue/river#Client.QueuePauseTx)
    * [func (c *Client[TTx]) QueueResume(ctx context.Context, name string, opts *QueuePauseOpts) error](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueResume)
    * [func (c *Client[TTx]) QueueResumeTx(ctx context.Context, tx TTx, name string, opts *QueuePauseOpts) error](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueResumeTx)
    * [func (c *Client[TTx]) QueueUpdate(ctx context.Context, name string, params *QueueUpdateParams) (*rivertype.Queue, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueUpdate)
    * [func (c *Client[TTx]) QueueUpdateTx(ctx context.Context, tx TTx, name string, params *QueueUpdateParams) (*rivertype.Queue, error)](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueUpdateTx)
    * [func (c *Client[TTx]) Queues() *QueueBundle](https://pkg.go.dev/github.com/riverqueue/river#Client.Queues)
    * [func (c *Client[TTx]) Schema() string](https://pkg.go.dev/github.com/riverqueue/river#Client.Schema)
    * [func (c *Client[TTx]) Start(ctx context.Context) error](https://pkg.go.dev/github.com/riverqueue/river#Client.Start)
    * [func (c *Client[TTx]) Stop(ctx context.Context) error](https://pkg.go.dev/github.com/riverqueue/river#Client.Stop)
    * [func (c *Client[TTx]) StopAndCancel(ctx context.Context) error](https://pkg.go.dev/github.com/riverqueue/river#Client.StopAndCancel)
    * [func (c *Client[TTx]) Stopped() <-chan struct{}](https://pkg.go.dev/github.com/riverqueue/river#Client.Stopped)
    * [func (c *Client[TTx]) Subscribe(kinds ...EventKind) (<-chan *Event, func())](https://pkg.go.dev/github.com/riverqueue/river#Client.Subscribe)
    * [func (c *Client[TTx]) SubscribeConfig(config *SubscribeConfig) (<-chan *Event, func())](https://pkg.go.dev/github.com/riverqueue/river#Client.SubscribeConfig)
  * [type ClientNotifyBundle](https://pkg.go.dev/github.com/riverqueue/river#ClientNotifyBundle)
  *     * [func (c *ClientNotifyBundle[TTx]) RequestResign(ctx context.Context) error](https://pkg.go.dev/github.com/riverqueue/river#ClientNotifyBundle.RequestResign)
    * [func (c *ClientNotifyBundle[TTx]) RequestResignTx(ctx context.Context, tx TTx) error](https://pkg.go.dev/github.com/riverqueue/river#ClientNotifyBundle.RequestResignTx)
  * [type ClientRetryPolicy](https://pkg.go.dev/github.com/riverqueue/river#ClientRetryPolicy)
  * [type Config](https://pkg.go.dev/github.com/riverqueue/river#Config)
  *     * [func (c *Config) WithDefaults() *Config](https://pkg.go.dev/github.com/riverqueue/river#Config.WithDefaults)
  * [type DefaultClientRetryPolicy](https://pkg.go.dev/github.com/riverqueue/river#DefaultClientRetryPolicy)
  *     * [func (p *DefaultClientRetryPolicy) NextRetry(job *rivertype.JobRow) time.Time](https://pkg.go.dev/github.com/riverqueue/river#DefaultClientRetryPolicy.NextRetry)
  * [type ErrorHandler](https://pkg.go.dev/github.com/riverqueue/river#ErrorHandler)
  * [type ErrorHandlerResult](https://pkg.go.dev/github.com/riverqueue/river#ErrorHandlerResult)
  * [type Event](https://pkg.go.dev/github.com/riverqueue/river#Event)
  * [type EventKind](https://pkg.go.dev/github.com/riverqueue/river#EventKind)
  * [type HookDefaults](https://pkg.go.dev/github.com/riverqueue/river#HookDefaults)
  *     * [func (d *HookDefaults) IsHook() bool](https://pkg.go.dev/github.com/riverqueue/river#HookDefaults.IsHook)
  * [type HookInsertBeginFunc](https://pkg.go.dev/github.com/riverqueue/river#HookInsertBeginFunc)
  *     * [func (f HookInsertBeginFunc) InsertBegin(ctx context.Context, params *rivertype.JobInsertParams) error](https://pkg.go.dev/github.com/riverqueue/river#HookInsertBeginFunc.InsertBegin)
    * [func (f HookInsertBeginFunc) IsHook() bool](https://pkg.go.dev/github.com/riverqueue/river#HookInsertBeginFunc.IsHook)
  * [type HookPeriodicJobsStartFunc](https://pkg.go.dev/github.com/riverqueue/river#HookPeriodicJobsStartFunc)
  *     * [func (f HookPeriodicJobsStartFunc) IsHook() bool](https://pkg.go.dev/github.com/riverqueue/river#HookPeriodicJobsStartFunc.IsHook)
    * [func (f HookPeriodicJobsStartFunc) Start(ctx context.Context, params *rivertype.HookPeriodicJobsStartParams) error](https://pkg.go.dev/github.com/riverqueue/river#HookPeriodicJobsStartFunc.Start)
  * [type HookWorkBeginFunc](https://pkg.go.dev/github.com/riverqueue/river#HookWorkBeginFunc)
  *     * [func (f HookWorkBeginFunc) IsHook() bool](https://pkg.go.dev/github.com/riverqueue/river#HookWorkBeginFunc.IsHook)
    * [func (f HookWorkBeginFunc) WorkBegin(ctx context.Context, job *rivertype.JobRow) error](https://pkg.go.dev/github.com/riverqueue/river#HookWorkBeginFunc.WorkBegin)
  * [type HookWorkEndFunc](https://pkg.go.dev/github.com/riverqueue/river#HookWorkEndFunc)
  *     * [func (f HookWorkEndFunc) IsHook() bool](https://pkg.go.dev/github.com/riverqueue/river#HookWorkEndFunc.IsHook)
    * [func (f HookWorkEndFunc) WorkEnd(ctx context.Context, job *rivertype.JobRow, err error) error](https://pkg.go.dev/github.com/riverqueue/river#HookWorkEndFunc.WorkEnd)
  * [type InsertManyParams](https://pkg.go.dev/github.com/riverqueue/river#InsertManyParams)
  * [type InsertOpts](https://pkg.go.dev/github.com/riverqueue/river#InsertOpts)
  * [type Job](https://pkg.go.dev/github.com/riverqueue/river#Job)
  *     * [func JobCompleteTx[TDriver riverdriver.Driver[TTx], TTx any, TArgs JobArgs](ctx context.Context, tx TTx, job *Job[TArgs]) (*Job[TArgs], error)](https://pkg.go.dev/github.com/riverqueue/river#JobCompleteTx)
    * [func ResumableSetStepCursorTx[TDriver riverdriver.Driver[TTx], TTx any, TArgs JobArgs, TCursor any](ctx context.Context, tx TTx, job *Job[TArgs], cursor TCursor) (*Job[TArgs], error)](https://pkg.go.dev/github.com/riverqueue/river#ResumableSetStepCursorTx)
    * [func ResumableSetStepTx[TDriver riverdriver.Driver[TTx], TTx any, TArgs JobArgs](ctx context.Context, tx TTx, job *Job[TArgs]) (*Job[TArgs], error)](https://pkg.go.dev/github.com/riverqueue/river#ResumableSetStepTx)
  * [type JobArgs](https://pkg.go.dev/github.com/riverqueue/river#JobArgs)
  * [type JobArgsWithHooks](https://pkg.go.dev/github.com/riverqueue/river#JobArgsWithHooks)
  * [type JobArgsWithInsertOpts](https://pkg.go.dev/github.com/riverqueue/river#JobArgsWithInsertOpts)
  * [type JobArgsWithKindAliases](https://pkg.go.dev/github.com/riverqueue/river#JobArgsWithKindAliases)
  * [type JobCancelError](https://pkg.go.dev/github.com/riverqueue/river#JobCancelError)
  * [type JobDeleteManyParams](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)
  *     * [func NewJobDeleteManyParams() *JobDeleteManyParams](https://pkg.go.dev/github.com/riverqueue/river#NewJobDeleteManyParams)
  *     * [func (p *JobDeleteManyParams) First(count int) *JobDeleteManyParams](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.First)
    * [func (p *JobDeleteManyParams) IDs(ids ...int64) *JobDeleteManyParams](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.IDs)
    * [func (p *JobDeleteManyParams) Kinds(kinds ...string) *JobDeleteManyParams](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.Kinds)
    * [func (p *JobDeleteManyParams) Priorities(priorities ...int16) *JobDeleteManyParams](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.Priorities)
    * [func (p *JobDeleteManyParams) Queues(queues ...string) *JobDeleteManyParams](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.Queues)
    * [func (p *JobDeleteManyParams) States(states ...rivertype.JobState) *JobDeleteManyParams](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.States)
    * [func (p *JobDeleteManyParams) UnsafeAll() *JobDeleteManyParams](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.UnsafeAll)
  * [type JobDeleteManyResult](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyResult)
  * [type JobInsertMiddlewareDefaults](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareDefaults)deprecated
  *     * [func (d *JobInsertMiddlewareDefaults) InsertMany(ctx context.Context, manyParams []*rivertype.JobInsertParams, ...) ([]*rivertype.JobInsertResult, error)](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareDefaults.InsertMany)
  * [type JobInsertMiddlewareFunc](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareFunc)
  *     * [func (f JobInsertMiddlewareFunc) InsertMany(ctx context.Context, manyParams []*rivertype.JobInsertParams, ...) ([]*rivertype.JobInsertResult, error)](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareFunc.InsertMany)
    * [func (f JobInsertMiddlewareFunc) IsMiddleware() bool](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareFunc.IsMiddleware)
  * [type JobListCursor](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor)
  *     * [func JobListCursorFromJob(job *rivertype.JobRow) *JobListCursor](https://pkg.go.dev/github.com/riverqueue/river#JobListCursorFromJob)
  *     * [func (c JobListCursor) MarshalText() ([]byte, error)](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor.MarshalText)
    * [func (c *JobListCursor) UnmarshalText(text []byte) error](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor.UnmarshalText)
  * [type JobListOrderByField](https://pkg.go.dev/github.com/riverqueue/river#JobListOrderByField)
  * [type JobListParams](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)
  *     * [func NewJobListParams() *JobListParams](https://pkg.go.dev/github.com/riverqueue/river#NewJobListParams)
  *     * [func (p *JobListParams) After(cursor *JobListCursor) *JobListParams](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.After)
    * [func (p *JobListParams) First(count int) *JobListParams](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.First)
    * [func (p *JobListParams) IDs(ids ...int64) *JobListParams](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.IDs)
    * [func (p *JobListParams) Kinds(kinds ...string) *JobListParams](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Kinds)
    * [func (p *JobListParams) Metadata(json string) *JobListParams](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Metadata)
    * [func (p *JobListParams) OrderBy(field JobListOrderByField, direction SortOrder) *JobListParams](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.OrderBy)
    * [func (p *JobListParams) Priorities(priorities ...int16) *JobListParams](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Priorities)
    * [func (p *JobListParams) Queues(queues ...string) *JobListParams](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Queues)
    * [func (p *JobListParams) States(states ...rivertype.JobState) *JobListParams](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.States)
    * [func (p *JobListParams) Where(sql string, namedArgsMany ...NamedArgs) *JobListParams](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Where)
  * [type JobListResult](https://pkg.go.dev/github.com/riverqueue/river#JobListResult)
  * [type JobSnoozeError](https://pkg.go.dev/github.com/riverqueue/river#JobSnoozeError)
  * [type JobStatistics](https://pkg.go.dev/github.com/riverqueue/river#JobStatistics)
  * [type JobUpdateParams](https://pkg.go.dev/github.com/riverqueue/river#JobUpdateParams)
  * [type MiddlewareDefaults](https://pkg.go.dev/github.com/riverqueue/river#MiddlewareDefaults)
  *     * [func (d *MiddlewareDefaults) IsMiddleware() bool](https://pkg.go.dev/github.com/riverqueue/river#MiddlewareDefaults.IsMiddleware)
  * [type NamedArgs](https://pkg.go.dev/github.com/riverqueue/river#NamedArgs)
  * [type PeriodicJob](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJob)
  *     * [func NewPeriodicJob(scheduleFunc PeriodicSchedule, constructorFunc PeriodicJobConstructor, ...) *PeriodicJob](https://pkg.go.dev/github.com/riverqueue/river#NewPeriodicJob)
  * [type PeriodicJobBundle](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle)
  *     * [func (b *PeriodicJobBundle) Add(periodicJob *PeriodicJob) rivertype.PeriodicJobHandle](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.Add)
    * [func (b *PeriodicJobBundle) AddMany(periodicJobs []*PeriodicJob) []rivertype.PeriodicJobHandle](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.AddMany)
    * [func (b *PeriodicJobBundle) AddManySafely(periodicJobs []*PeriodicJob) ([]rivertype.PeriodicJobHandle, error)](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.AddManySafely)
    * [func (b *PeriodicJobBundle) AddSafely(periodicJob *PeriodicJob) (rivertype.PeriodicJobHandle, error)](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.AddSafely)
    * [func (b *PeriodicJobBundle) Clear()](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.Clear)
    * [func (b *PeriodicJobBundle) Remove(periodicJobHandle rivertype.PeriodicJobHandle)](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.Remove)
    * [func (b *PeriodicJobBundle) RemoveByID(id string) bool](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.RemoveByID)
    * [func (b *PeriodicJobBundle) RemoveMany(periodicJobHandles []rivertype.PeriodicJobHandle)](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.RemoveMany)
    * [func (b *PeriodicJobBundle) RemoveManyByID(ids []string)](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.RemoveManyByID)
  * [type PeriodicJobConstructor](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobConstructor)
  * [type PeriodicJobOpts](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobOpts)
  * [type PeriodicSchedule](https://pkg.go.dev/github.com/riverqueue/river#PeriodicSchedule)
  *     * [func NeverSchedule() PeriodicSchedule](https://pkg.go.dev/github.com/riverqueue/river#NeverSchedule)
    * [func PeriodicInterval(interval time.Duration) PeriodicSchedule](https://pkg.go.dev/github.com/riverqueue/river#PeriodicInterval)
  * [type QueueAlreadyAddedError](https://pkg.go.dev/github.com/riverqueue/river#QueueAlreadyAddedError)
  *     * [func (e *QueueAlreadyAddedError) Error() string](https://pkg.go.dev/github.com/riverqueue/river#QueueAlreadyAddedError.Error)
    * [func (e *QueueAlreadyAddedError) Is(target error) bool](https://pkg.go.dev/github.com/riverqueue/river#QueueAlreadyAddedError.Is)
  * [type QueueBundle](https://pkg.go.dev/github.com/riverqueue/river#QueueBundle)
  *     * [func (b *QueueBundle) Add(queueName string, queueConfig QueueConfig) error](https://pkg.go.dev/github.com/riverqueue/river#QueueBundle.Add)
    * [func (b *QueueBundle) Remove(ctx context.Context, queueName string) error](https://pkg.go.dev/github.com/riverqueue/river#QueueBundle.Remove)
  * [type QueueConfig](https://pkg.go.dev/github.com/riverqueue/river#QueueConfig)
  * [type QueueListParams](https://pkg.go.dev/github.com/riverqueue/river#QueueListParams)
  *     * [func NewQueueListParams() *QueueListParams](https://pkg.go.dev/github.com/riverqueue/river#NewQueueListParams)
  *     * [func (p *QueueListParams) First(count int) *QueueListParams](https://pkg.go.dev/github.com/riverqueue/river#QueueListParams.First)
  * [type QueueListResult](https://pkg.go.dev/github.com/riverqueue/river#QueueListResult)
  * [type QueueNotFoundError](https://pkg.go.dev/github.com/riverqueue/river#QueueNotFoundError)
  *     * [func (e *QueueNotFoundError) Error() string](https://pkg.go.dev/github.com/riverqueue/river#QueueNotFoundError.Error)
    * [func (e *QueueNotFoundError) Is(target error) bool](https://pkg.go.dev/github.com/riverqueue/river#QueueNotFoundError.Is)
  * [type QueuePauseOpts](https://pkg.go.dev/github.com/riverqueue/river#QueuePauseOpts)
  * [type QueueUpdateParams](https://pkg.go.dev/github.com/riverqueue/river#QueueUpdateParams)
  * [type SortOrder](https://pkg.go.dev/github.com/riverqueue/river#SortOrder)
  * [type StepOpts](https://pkg.go.dev/github.com/riverqueue/river#StepOpts)
  * [type SubscribeConfig](https://pkg.go.dev/github.com/riverqueue/river#SubscribeConfig)
  * [type TestConfig](https://pkg.go.dev/github.com/riverqueue/river#TestConfig)
  * [type UniqueOpts](https://pkg.go.dev/github.com/riverqueue/river#UniqueOpts)
  * [type UnknownJobKindError](https://pkg.go.dev/github.com/riverqueue/river#UnknownJobKindError)
  * [type Worker](https://pkg.go.dev/github.com/riverqueue/river#Worker)
  *     * [func WorkFunc[T JobArgs](f func(context.Context, *Job[T]) error) Worker[T]](https://pkg.go.dev/github.com/riverqueue/river#WorkFunc)
  * [type WorkerDefaults](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults)
  *     * [func (w WorkerDefaults[T]) Middleware(*rivertype.JobRow) []rivertype.WorkerMiddleware](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults.Middleware)
    * [func (w WorkerDefaults[T]) NextRetry(*Job[T]) time.Time](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults.NextRetry)
    * [func (w WorkerDefaults[T]) Timeout(*Job[T]) time.Duration](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults.Timeout)
  * [type WorkerMiddlewareDefaults](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareDefaults)deprecated
  *     * [func (d *WorkerMiddlewareDefaults) Work(ctx context.Context, job *rivertype.JobRow, ...) error](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareDefaults.Work)
  * [type WorkerMiddlewareFunc](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareFunc)
  *     * [func (f WorkerMiddlewareFunc) IsMiddleware() bool](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareFunc.IsMiddleware)
    * [func (f WorkerMiddlewareFunc) Work(ctx context.Context, job *rivertype.JobRow, ...) error](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareFunc.Work)
  * [type Workers](https://pkg.go.dev/github.com/riverqueue/river#Workers)
  *     * [func NewWorkers() *Workers](https://pkg.go.dev/github.com/riverqueue/river#NewWorkers)


### Examples [¶](https://pkg.go.dev/github.com/riverqueue/river#pkg-examples "Go to Examples")
  * [Package (BatchInsert)](https://pkg.go.dev/github.com/riverqueue/river#example-package-BatchInsert)
  * [Package (CompleteJobWithinTx)](https://pkg.go.dev/github.com/riverqueue/river#example-package-CompleteJobWithinTx)
  * [Package (CronJob)](https://pkg.go.dev/github.com/riverqueue/river#example-package-CronJob)
  * [Package (CustomInsertOpts)](https://pkg.go.dev/github.com/riverqueue/river#example-package-CustomInsertOpts)
  * [Package (ErrorHandler)](https://pkg.go.dev/github.com/riverqueue/river#example-package-ErrorHandler)
  * [Package (GlobalHooks)](https://pkg.go.dev/github.com/riverqueue/river#example-package-GlobalHooks)
  * [Package (GlobalMiddleware)](https://pkg.go.dev/github.com/riverqueue/river#example-package-GlobalMiddleware)
  * [Package (GracefulShutdown)](https://pkg.go.dev/github.com/riverqueue/river#example-package-GracefulShutdown)
  * [Package (GracefulShutdownStopAndCancel)](https://pkg.go.dev/github.com/riverqueue/river#example-package-GracefulShutdownStopAndCancel)
  * [Package (InsertAndWork)](https://pkg.go.dev/github.com/riverqueue/river#example-package-InsertAndWork)
  * [Package (JobArgsHooks)](https://pkg.go.dev/github.com/riverqueue/river#example-package-JobArgsHooks)
  * [Package (JobCancel)](https://pkg.go.dev/github.com/riverqueue/river#example-package-JobCancel)
  * [Package (JobCancelFromClient)](https://pkg.go.dev/github.com/riverqueue/river#example-package-JobCancelFromClient)
  * [Package (JobSnooze)](https://pkg.go.dev/github.com/riverqueue/river#example-package-JobSnooze)
  * [Package (PeriodicJob)](https://pkg.go.dev/github.com/riverqueue/river#example-package-PeriodicJob)
  * [Package (QueuePause)](https://pkg.go.dev/github.com/riverqueue/river#example-package-QueuePause)
  * [Package (Resumable)](https://pkg.go.dev/github.com/riverqueue/river#example-package-Resumable)
  * [Package (ResumableCursor)](https://pkg.go.dev/github.com/riverqueue/river#example-package-ResumableCursor)
  * [Package (ResumableSetStepTx)](https://pkg.go.dev/github.com/riverqueue/river#example-package-ResumableSetStepTx)
  * [Package (ScheduledJob)](https://pkg.go.dev/github.com/riverqueue/river#example-package-ScheduledJob)
  * [Package (Subscription)](https://pkg.go.dev/github.com/riverqueue/river#example-package-Subscription)
  * [Package (UniqueJob)](https://pkg.go.dev/github.com/riverqueue/river#example-package-UniqueJob)
  * [Package (WorkFunc)](https://pkg.go.dev/github.com/riverqueue/river#example-package-WorkFunc)
  * [ClientFromContext (Pgx)](https://pkg.go.dev/github.com/riverqueue/river#example-ClientFromContext-Pgx)


### Constants [¶](https://pkg.go.dev/github.com/riverqueue/river#pkg-constants "Go to Constants")
[View Source](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L42) 
```
const (
	FetchCooldownDefault = 100 * time[](https://pkg.go.dev/time).Millisecond[](https://pkg.go.dev/time#Millisecond)
	FetchCooldownMin     = 1 * time[](https://pkg.go.dev/time).Millisecond[](https://pkg.go.dev/time#Millisecond)

	FetchPollIntervalDefault = 1 * time[](https://pkg.go.dev/time).Second[](https://pkg.go.dev/time#Second)
	FetchPollIntervalMin     = 1 * time[](https://pkg.go.dev/time).Millisecond[](https://pkg.go.dev/time#Millisecond)

	JobTimeoutDefault  = 1 * time[](https://pkg.go.dev/time).Minute[](https://pkg.go.dev/time#Minute)
	MaxAttemptsDefault = rivercommon[](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/rivercommon).MaxAttemptsDefault[](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/rivercommon#MaxAttemptsDefault)
	PriorityDefault    = rivercommon[](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/rivercommon).PriorityDefault[](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/rivercommon#PriorityDefault)
	QueueDefault       = rivercommon[](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/rivercommon).QueueDefault[](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/rivercommon#QueueDefault)
	QueueNumWorkersMax = 10_000
)
```

### Variables [¶](https://pkg.go.dev/github.com/riverqueue/river#pkg-variables "Go to Variables")
[View Source](https://github.com/riverqueue/river/blob/v0.39.0/error.go#L11) 
```
var ErrJobCancelledRemotely = rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).ErrJobCancelledRemotely[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#ErrJobCancelledRemotely)
```

ErrJobCancelledRemotely is a sentinel error indicating that the job was cancelled remotely. 
[View Source](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L716) 
```
var (
	// ErrNotFound is returned when a query by ID does not match any existing
	// rows. For example, attempting to cancel a job that doesn't exist will
	// return this error.
	ErrNotFound = rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).ErrNotFound[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#ErrNotFound)
)
```

### Functions [¶](https://pkg.go.dev/github.com/riverqueue/river#pkg-functions "Go to Functions")
####  func [AddWorker](https://github.com/riverqueue/river/blob/v0.39.0/worker.go#L108) [¶](https://pkg.go.dev/github.com/riverqueue/river#AddWorker "Go to AddWorker")

```
func AddWorker[T JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs)](workers *Workers[](https://pkg.go.dev/github.com/riverqueue/river#Workers), worker Worker[](https://pkg.go.dev/github.com/riverqueue/river#Worker)[T])
```

AddWorker registers a Worker on the provided Workers bundle. Each Worker must be registered so that the Client knows it should handle a specific kind of job (as returned by its `Kind()` method). 
Use by explicitly specifying a JobArgs type and then passing an instance of a worker for the same type: 

```
river.AddWorker(workers, &SortWorker{})

```

Note that AddWorker can panic in some situations, such as if the worker is already registered or if its configuration is otherwise invalid. This default probably makes sense for most applications because you wouldn't want to start an application with invalid hardcoded runtime configuration. If you want to avoid panics, use AddWorkerSafely instead. 
####  func [AddWorkerArgs](https://github.com/riverqueue/river/blob/v0.39.0/worker.go#L119) [¶](https://pkg.go.dev/github.com/riverqueue/river#AddWorkerArgs "Go to AddWorkerArgs") added in v0.24.0

```
func AddWorkerArgs[T JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs)](workers *Workers[](https://pkg.go.dev/github.com/riverqueue/river#Workers), jobArgs T, worker Worker[](https://pkg.go.dev/github.com/riverqueue/river#Worker)[T])
```

AddWorkerArgs is the same as AddWorker except that it lets args be passed explicitly rather than being instantiated implicitly. We don't know of any use for this function beyond exercising some args-related edge cases in tests are difficult/impossible to exercise otherwise, and its use should be considered internal only. 
####  func [AddWorkerSafely](https://github.com/riverqueue/river/blob/v0.39.0/worker.go#L133) [¶](https://pkg.go.dev/github.com/riverqueue/river#AddWorkerSafely "Go to AddWorkerSafely")

```
func AddWorkerSafely[T JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs)](workers *Workers[](https://pkg.go.dev/github.com/riverqueue/river#Workers), worker Worker[](https://pkg.go.dev/github.com/riverqueue/river#Worker)[T]) error[](https://pkg.go.dev/builtin#error)
```

AddWorkerSafely registers a worker on the provided Workers bundle. Unlike AddWorker, AddWorkerSafely does not panic and instead returns an error if the worker is already registered or if its configuration is invalid. 
Use by explicitly specifying a JobArgs type and then passing an instance of a worker for the same type: 

```
river.AddWorkerSafely[SortArgs](workers, &SortWorker{}).

```

####  func [JobCancel](https://github.com/riverqueue/river/blob/v0.39.0/error.go#L21) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobCancel "Go to JobCancel")

```
func JobCancel(err error[](https://pkg.go.dev/builtin#error)) error[](https://pkg.go.dev/builtin#error)
```

JobCancel wraps err and can be returned from a Worker's Work method to cancel the job at the end of execution. Regardless of whether or not the job has any remaining attempts, this will ensure the job does not execute again. 
####  func [JobSnooze](https://github.com/riverqueue/river/blob/v0.39.0/error.go#L43) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobSnooze "Go to JobSnooze")

```
func JobSnooze(duration time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)) error[](https://pkg.go.dev/builtin#error)
```

JobSnooze can be returned from a Worker's Work method to cause the job to be tried again after the specified duration. This will not increment the job's Attempt count, meaning that jobs can be repeatedly snoozed without ever being discarded. 
A special duration of zero can be used to make the job immediately available to be reworked. This may be useful in cases like where a long-running job is being interrupted on shutdown. Instead of returning a context cancelled error that'd schedule a retry for the future and count towards maximum attempts, the work function can return JobSnooze(0) and the job will be retried immediately the next time a client starts up. 
Panics if duration is < 0. 
####  func [MetadataSet](https://github.com/riverqueue/river/blob/v0.39.0/metadata.go#L26) [¶](https://pkg.go.dev/github.com/riverqueue/river#MetadataSet "Go to MetadataSet") added in v0.39.0

```
func MetadataSet(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), key string[](https://pkg.go.dev/builtin#string), value any[](https://pkg.go.dev/builtin#any)) error[](https://pkg.go.dev/builtin#error)
```

MetadataSet records a metadata value to be merged into the job's metadata when the current work attempt finishes. 
This function is only valid from a worker, worker middleware, or work hook like rivertype.HookWorkBegin or rivertype.HookWorkEnd. 
Metadata updates are stored on the work context and merged into the job row when the current work attempt finishes, whether the attempt succeeds or errors. Values must be JSON marshalable because metadata is stored in a jsonb column, and setting a key replaces any existing value at that key. 
Keys prefixed with `river:` are reserved for internal use and may not be set by user code. 
####  func [RecordOutput](https://github.com/riverqueue/river/blob/v0.39.0/recorded_output.go#L53) [¶](https://pkg.go.dev/github.com/riverqueue/river#RecordOutput "Go to RecordOutput") added in v0.18.0

```
func RecordOutput(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), output any[](https://pkg.go.dev/builtin#any)) error[](https://pkg.go.dev/builtin#error)
```

RecordOutput records output JSON from a job. The "output" can be any JSON-encodable value and will be stored in the database on the job row after the current execution attempt completes. Output may be useful for debugging, or for storing the result of a job temporarily without needing to create a dedicated table to keep it in. 
For example, with workflows, it's common for subsequent task to depend on something done in an earlier dependency task. Consider the creation of an external resource in another API or in an database—it will typically have a unique ID that must be used to reference the resource later. A later step may require that info in order to complete its work, and the output can be a convenient way to store that info. 
Output is stored in the job's metadata under the `"output"` key ([github.com/riverqueue/river/rivertype.MetadataKeyOutput](https://pkg.go.dev/github.com/riverqueue/river/rivertype#MetadataKeyOutput)). This function must be called within an Worker's Work function. It returns an error if called anywhere else. As with any stored value, care should be taken to ensure that the payload size is not too large. Output is limited to 32MB in size for safety, but should be kept much smaller than this. 
Only one output can be stored per job. If this function is called more than once, the output will be overwritten with the latest value. The output also must be recorded _before_ the job finishes executing so that it can be stored when the job's row is updated. 
Once recorded, the output is stored regardless of the outcome of the execution attempt (success, error, panic, etc.). 
RecordOutput always stores output lazily as a job is being completed (whether that's completion to success or failure). Client.JobUpdate and JobUpdateTx are available to store output eagerly at any time, including from inside a work function as the job is being executed. 
The output is marshalled to JSON as part of this function and it will return an error if the output is not JSON-encodable. 
####  func [ReindexerIndexNamesDefault](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L422) [¶](https://pkg.go.dev/github.com/riverqueue/river#ReindexerIndexNamesDefault "Go to ReindexerIndexNamesDefault") added in v0.34.0

```
func ReindexerIndexNamesDefault() []string[](https://pkg.go.dev/builtin#string)
```

ReindexerIndexNamesDefault returns the default set of indexes reindexed by River. 
####  func [ResumableSetCursor](https://github.com/riverqueue/river/blob/v0.39.0/resumable.go#L24) [¶](https://pkg.go.dev/github.com/riverqueue/river#ResumableSetCursor "Go to ResumableSetCursor") added in v0.37.0

```
func ResumableSetCursor[TCursor any[](https://pkg.go.dev/builtin#any)](ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), cursor TCursor) error[](https://pkg.go.dev/builtin#error)
```

ResumableSetCursor records a cursor for the current resumable cursor step. The cursor is stored only if the job attempt ends in an error, allowing a later retry to resume the same step from the recorded position. 
Alternatively, ResumableSetStepCursorTx is available to persist a step and cursor immediately as part of a transaction, guaranteeing that it's stored durably. 
####  func [ResumableStep](https://github.com/riverqueue/river/blob/v0.39.0/resumable.go#L55) [¶](https://pkg.go.dev/github.com/riverqueue/river#ResumableStep "Go to ResumableStep") added in v0.37.0

```
func ResumableStep(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), name string[](https://pkg.go.dev/builtin#string), opts *StepOpts[](https://pkg.go.dev/github.com/riverqueue/river#StepOpts), stepFunc func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context)) error[](https://pkg.go.dev/builtin#error))
```

ResumableStep runs a resumable step, skipping the step on a later retry if an earlier attempt already completed it successfully. 
After a step returns an error, no subsequent steps will be run and the overall job will be marked as failed with that error. Be careful to put all executable code in steps, because any code outside of them will be run, even if a step returned an error. 
opts may be nil. 
####  func [ResumableStepCursor](https://github.com/riverqueue/river/blob/v0.39.0/resumable.go#L100) [¶](https://pkg.go.dev/github.com/riverqueue/river#ResumableStepCursor "Go to ResumableStepCursor") added in v0.37.0

```
func ResumableStepCursor[TCursor any[](https://pkg.go.dev/builtin#any)](ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), name string[](https://pkg.go.dev/builtin#string), opts *StepOpts[](https://pkg.go.dev/github.com/riverqueue/river#StepOpts), stepFunc func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), cursor TCursor) error[](https://pkg.go.dev/builtin#error))
```

ResumableStepCursor runs a resumable step that also receives a persisted cursor value from an earlier failed attempt, if one was recorded with ResumableSetCursor. 
The cursor type T is user-specified. It may be a primitive value like an integer ID, or a more complex type like a struct with multiple fields. It's stored in a job's metadata, so it needs to be marshable and unmarshable to and from JSON. 
Notably, it's the responsibility of the step function to call ResumableSetCursor with an updated cursor value as progress is made, and to check the cursor value before running to determine where to resume from. 
After a step returns an error, no subsequent steps will be run and the overall job will be marked as failed with that error. Be careful to put all executable code in steps, because any code outside of them will be run, even if a step returned an error. 
opts may be nil. 
### Types [¶](https://pkg.go.dev/github.com/riverqueue/river#pkg-types "Go to Types")
####  type [Client](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L648) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client "Go to Client")

```
type Client[TTx any[](https://pkg.go.dev/builtin#any)] struct {
	// contains filtered or unexported fields
}
```

Client is a single isolated instance of River. Your application may use multiple instances operating on different databases or Postgres schemas within a single database. 
####  func [ClientFromContext](https://github.com/riverqueue/river/blob/v0.39.0/client_context.go#L28) [¶](https://pkg.go.dev/github.com/riverqueue/river#ClientFromContext "Go to ClientFromContext") added in v0.0.17

```
func ClientFromContext[TTx any[](https://pkg.go.dev/builtin#any)](ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context)) *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]
```

ClientFromContext returns the Client from the context. This function can only be used within a Worker's Work() method because that is the only place River sets the Client on the context. 
It panics if the context does not contain a Client, which will never happen from the context provided to a Worker's Work() method. 
When testing JobArgs.Work implementations, it might be useful to use rivertest.WorkContext to initialize a context that has an available client. 
The type parameter TTx is the transaction type used by the [Client](https://pkg.go.dev/github.com/riverqueue/river#Client), pgx.Tx for the pgx driver, and *sql.Tx for the [database/sql](https://pkg.go.dev/database/sql) driver. 
Example (Pgx) [¶](https://pkg.go.dev/github.com/riverqueue/river#example-ClientFromContext-Pgx "Go to Example \(Pgx\)")
ExampleClientFromContext_pgx demonstrates how to extract the River client from the worker context when using the pgx/v5 driver. ([github.com/riverqueue/river/riverdriver/riverpgxv5](https://pkg.go.dev/github.com/riverqueue/river/riverdriver/riverpgxv5)). 

```
Output:
client found in context, id=ClientFromContextClient

```

Share Format Run
####  func [ClientFromContextSafely](https://github.com/riverqueue/river/blob/v0.39.0/client_context.go#L48) [¶](https://pkg.go.dev/github.com/riverqueue/river#ClientFromContextSafely "Go to ClientFromContextSafely") added in v0.0.17

```
func ClientFromContextSafely[TTx any[](https://pkg.go.dev/builtin#any)](ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context)) (*Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx], error[](https://pkg.go.dev/builtin#error))
```

ClientFromContextSafely returns the Client from the context. This function can only be used within a Worker's Work() method because that is the only place River sets the Client on the context. 
It returns an error if the context does not contain a Client, which will never happen from the context provided to a Worker's Work() method. 
When testing JobArgs.Work implementations, it might be useful to use rivertest.WorkContext to initialize a context that has an available client. 
See the examples for [ClientFromContext](https://pkg.go.dev/github.com/riverqueue/river#ClientFromContext) to understand how to use this function. 
####  func [NewClient](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L754) [¶](https://pkg.go.dev/github.com/riverqueue/river#NewClient "Go to NewClient")

```
func NewClient[TTx any[](https://pkg.go.dev/builtin#any)](driver riverdriver[](https://pkg.go.dev/github.com/riverqueue/river/riverdriver).Driver[](https://pkg.go.dev/github.com/riverqueue/river/riverdriver#Driver)[TTx], config *Config[](https://pkg.go.dev/github.com/riverqueue/river#Config)) (*Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx], error[](https://pkg.go.dev/builtin#error))
```

NewClient creates a new Client with the given database driver and configuration. 
Currently only one driver is supported, which is Pgx v5. See package riverpgxv5. 
The function takes a generic parameter TTx representing a transaction type, but it can be omitted because it'll generally always be inferred from the driver. For example: 

```
import "github.com/riverqueue/river"
import "github.com/riverqueue/river/riverdriver/riverpgxv5"

...

dbPool, err := pgxpool.New(ctx, os.Getenv("DATABASE_URL"))
if err != nil {
	// handle error
}
defer dbPool.Close()

riverClient, err := river.NewClient(riverpgxv5.New(dbPool), &river.Config{
	...
})
if err != nil {
	// handle error
}

```

####  func (*Client[TTx]) [Driver](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1385) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.Driver "Go to Client.Driver") added in v0.11.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) Driver() riverdriver[](https://pkg.go.dev/github.com/riverqueue/river/riverdriver).Driver[](https://pkg.go.dev/github.com/riverqueue/river/riverdriver#Driver)[TTx]
```

Driver exposes the underlying driver used by the client. 
API is not stable. DO NOT USE. 
####  func (*Client[TTx]) [ID](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1667) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.ID "Go to Client.ID") added in v0.0.21

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) ID() string[](https://pkg.go.dev/builtin#string)
```

ID returns the unique ID of this client as set in its config or auto-generated if not specified. 
####  func (*Client[TTx]) [Insert](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1789) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.Insert "Go to Client.Insert")

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) Insert(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), args JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs), opts *InsertOpts[](https://pkg.go.dev/github.com/riverqueue/river#InsertOpts)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertResult[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertResult), error[](https://pkg.go.dev/builtin#error))
```

Insert inserts a new job with the provided args. Job opts can be used to override any defaults that may have been provided by an implementation of JobArgsWithInsertOpts.InsertOpts, as well as any global defaults. The provided context is used for the underlying Postgres insert and can be used to cancel the operation or apply a timeout. 

```
jobRow, err := client.Insert(insertCtx, MyArgs{}, nil)
if err != nil {
	// handle error
}

```

####  func (*Client[TTx]) [InsertMany](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1855) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertMany "Go to Client.InsertMany")

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) InsertMany(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), params []InsertManyParams[](https://pkg.go.dev/github.com/riverqueue/river#InsertManyParams)) ([]*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertResult[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertResult), error[](https://pkg.go.dev/builtin#error))
```

InsertMany inserts many jobs at once. Each job is inserted as an InsertManyParams tuple, which takes job args along with an optional set of insert options, which override insert options provided by an JobArgsWithInsertOpts.InsertOpts implementation or any client-level defaults. The provided context is used for the underlying Postgres inserts and can be used to cancel the operation or apply a timeout. 

```
count, err := client.InsertMany(ctx, []river.InsertManyParams{
	{Args: BatchInsertArgs{}},
	{Args: BatchInsertArgs{}, InsertOpts: &river.InsertOpts{Priority: 3}},
})
if err != nil {
	// handle error
}

```

####  func (*Client[TTx]) [InsertManyFast](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2077) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertManyFast "Go to Client.InsertManyFast") added in v0.12.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) InsertManyFast(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), params []InsertManyParams[](https://pkg.go.dev/github.com/riverqueue/river#InsertManyParams)) (int[](https://pkg.go.dev/builtin#int), error[](https://pkg.go.dev/builtin#error))
```

InsertManyFast inserts many jobs at once using Postgres' `COPY FROM` mechanism, making the operation quite fast and memory efficient. Each job is inserted as an InsertManyParams tuple, which takes job args along with an optional set of insert options, which override insert options provided by an JobArgsWithInsertOpts.InsertOpts implementation or any client-level defaults. The provided context is used for the underlying Postgres inserts and can be used to cancel the operation or apply a timeout. 

```
count, err := client.InsertMany(ctx, []river.InsertManyParams{
	{Args: BatchInsertArgs{}},
	{Args: BatchInsertArgs{}, InsertOpts: &river.InsertOpts{Priority: 3}},
})
if err != nil {
	// handle error
}

```

Unlike with `InsertMany`, unique conflicts cannot be handled gracefully. If a unique constraint is violated, the operation will fail and no jobs will be inserted. 
####  func (*Client[TTx]) [InsertManyFastTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2118) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertManyFastTx "Go to Client.InsertManyFastTx") added in v0.12.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) InsertManyFastTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, params []InsertManyParams[](https://pkg.go.dev/github.com/riverqueue/river#InsertManyParams)) (int[](https://pkg.go.dev/builtin#int), error[](https://pkg.go.dev/builtin#error))
```

InsertManyFastTx inserts many jobs at once using Postgres' `COPY FROM` mechanism, making the operation quite fast and memory efficient. Each job is inserted as an InsertManyParams tuple, which takes job args along with an optional set of insert options, which override insert options provided by an JobArgsWithInsertOpts.InsertOpts implementation or any client-level defaults. The provided context is used for the underlying Postgres inserts and can be used to cancel the operation or apply a timeout. 

```
count, err := client.InsertManyTx(ctx, tx, []river.InsertManyParams{
	{Args: BatchInsertArgs{}},
	{Args: BatchInsertArgs{}, InsertOpts: &river.InsertOpts{Priority: 3}},
})
if err != nil {
	// handle error
}

```

This variant lets a caller insert jobs atomically alongside other database changes. An inserted job isn't visible to be worked until the transaction commits, and if the transaction rolls back, so too is the inserted job. 
Unlike with `InsertManyTx`, unique conflicts cannot be handled gracefully. If a unique constraint is violated, the operation will fail and no jobs will be inserted. 
####  func (*Client[TTx]) [InsertManyTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1890) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertManyTx "Go to Client.InsertManyTx")

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) InsertManyTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, params []InsertManyParams[](https://pkg.go.dev/github.com/riverqueue/river#InsertManyParams)) ([]*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertResult[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertResult), error[](https://pkg.go.dev/builtin#error))
```

InsertManyTx inserts many jobs at once. Each job is inserted as an InsertManyParams tuple, which takes job args along with an optional set of insert options, which override insert options provided by an JobArgsWithInsertOpts.InsertOpts implementation or any client-level defaults. The provided context is used for the underlying Postgres inserts and can be used to cancel the operation or apply a timeout. 

```
count, err := client.InsertManyTx(ctx, tx, []river.InsertManyParams{
	{Args: BatchInsertArgs{}},
	{Args: BatchInsertArgs{}, InsertOpts: &river.InsertOpts{Priority: 3}},
})
if err != nil {
	// handle error
}

```

This variant lets a caller insert jobs atomically alongside other database changes. An inserted job isn't visible to be worked until the transaction commits, and if the transaction rolls back, so too is the inserted job. 
####  func (*Client[TTx]) [InsertTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1823) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.InsertTx "Go to Client.InsertTx")

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) InsertTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, args JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs), opts *InsertOpts[](https://pkg.go.dev/github.com/riverqueue/river#InsertOpts)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertResult[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertResult), error[](https://pkg.go.dev/builtin#error))
```

InsertTx inserts a new job with the provided args on the given transaction. Job opts can be used to override any defaults that may have been provided by an implementation of JobArgsWithInsertOpts.InsertOpts, as well as any global defaults. The provided context is used for the underlying Postgres insert and can be used to cancel the operation or apply a timeout. 

```
jobRow, err := client.InsertTx(insertCtx, tx, MyArgs{}, nil)
if err != nil {
	// handle error
}

```

This variant lets a caller insert jobs atomically alongside other database changes. It's also possible to insert a job outside a transaction, but this usage is recommended to ensure that all data a job needs to run is available by the time it starts. Because of snapshot visibility guarantees across transactions, the job will not be worked until the transaction has committed, and if the transaction rolls back, so too is the inserted job. 
####  func (*Client[TTx]) [JobCancel](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1425) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobCancel "Go to Client.JobCancel") added in v0.0.17

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobCancel(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), jobID int64[](https://pkg.go.dev/builtin#int64)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), error[](https://pkg.go.dev/builtin#error))
```

JobCancel cancels the job with the given ID. If possible, the job is cancelled immediately and will not be retried. The provided context is used for the underlying Postgres update and can be used to cancel the operation or apply a timeout. 
If the job is still in the queue (available, scheduled, or retryable), it is immediately marked as cancelled and will not be retried. 
If the job is already finalized (cancelled, completed, or discarded), no changes are made. 
If the job is currently running, it is not immediately cancelled, but is instead marked for cancellation. The client running the job will also be notified (via LISTEN/NOTIFY) to cancel the running job's context. Although the job's context will be cancelled, since Go does not provide a mechanism to interrupt a running goroutine the job will continue running until it returns. As always, it is important for workers to respect context cancellation and return promptly when the job context is done. 
Once the cancellation signal is received by the client running the job, any error returned by that job will result in it being cancelled permanently and not retried. However if the job returns no error, it will be completed as usual. 
In the event the running job finishes executing _before_ the cancellation signal is received but _after_ this update was made, the behavior depends on which state the job is being transitioned into (based on its return error): 
  * If the job completed successfully, was cancelled from within, or was discarded due to exceeding its max attempts, the job will be updated as usual.
  * If the job was snoozed to run again later or encountered a retryable error, the job will be marked as cancelled and will not be attempted again.


Returns the up-to-date JobRow for the specified jobID if it exists. Returns ErrNotFound if the job doesn't exist. 
####  func (*Client[TTx]) [JobCancelTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1481) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobCancelTx "Go to Client.JobCancelTx") added in v0.0.17

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobCancelTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, jobID int64[](https://pkg.go.dev/builtin#int64)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), error[](https://pkg.go.dev/builtin#error))
```

JobCancelTx cancels the job with the given ID within the specified transaction. This variant lets a caller cancel a job atomically alongside other database changes. A cancelled job doesn't take effect until the transaction commits, and if the transaction rolls back, so too is the cancelled job. 
If possible, the job is cancelled immediately and will not be retried. The provided context is used for the underlying Postgres update and can be used to cancel the operation or apply a timeout. 
If the job is still in the queue (available, scheduled, or retryable), it is immediately marked as cancelled and will not be retried. 
If the job is already finalized (cancelled, completed, or discarded), no changes are made. 
If the job is currently running, it is not immediately cancelled, but is instead marked for cancellation. The client running the job will also be notified (via LISTEN/NOTIFY) to cancel the running job's context. Although the job's context will be cancelled, since Go does not provide a mechanism to interrupt a running goroutine the job will continue running until it returns. As always, it is important for workers to respect context cancellation and return promptly when the job context is done. 
Once the cancellation signal is received by the client running the job, any error returned by that job will result in it being cancelled permanently and not retried. However if the job returns no error, it will be completed as usual. 
In the event the running job finishes executing _before_ the cancellation signal is received but _after_ this update was made, the behavior depends on which state the job is being transitioned into (based on its return error): 
  * If the job completed successfully, was cancelled from within, or was discarded due to exceeding its max attempts, the job will be updated as usual.
  * If the job was snoozed to run again later or encountered a retryable error, the job will be marked as cancelled and will not be attempted again.


Returns the up-to-date JobRow for the specified jobID if it exists. Returns ErrNotFound if the job doesn't exist. 
####  func (*Client[TTx]) [JobDelete](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1498) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobDelete "Go to Client.JobDelete") added in v0.7.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobDelete(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), id int64[](https://pkg.go.dev/builtin#int64)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), error[](https://pkg.go.dev/builtin#error))
```

JobDelete deletes the job with the given ID from the database, returning the deleted row if it was deleted. Jobs in the running state are not deleted, instead returning rivertype.ErrJobRunning. 
####  func (*Client[TTx]) [JobDeleteMany](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2328) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobDeleteMany "Go to Client.JobDeleteMany") added in v0.24.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobDeleteMany(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), params *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)) (*JobDeleteManyResult[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyResult), error[](https://pkg.go.dev/builtin#error))
```

JobDeleteMany deletes many jobs at once based on the conditions defined by JobDeleteManyParams. Running jobs are always ignored. 

```
params := river.NewJobDeleteManyParams().First(10).State(rivertype.JobStateCompleted)
jobRows, err := client.JobDeleteMany(ctx, params)
if err != nil {
	// handle error
}

```

####  func (*Client[TTx]) [JobDeleteManyTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2344) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobDeleteManyTx "Go to Client.JobDeleteManyTx") added in v0.24.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobDeleteManyTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, params *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)) (*JobDeleteManyResult[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyResult), error[](https://pkg.go.dev/builtin#error))
```

JobDeleteManyTx deletes many jobs at once based on the conditions defined by JobDeleteManyParams. Running jobs are always ignored. 

```
params := river.NewJobDeleteManyParams().First(10).States(river.JobStateCompleted)
jobRows, err := client.JobDeleteManyTx(ctx, tx, params)
if err != nil {
	// handle error
}

```

####  func (*Client[TTx]) [JobDeleteTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1511) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobDeleteTx "Go to Client.JobDeleteTx") added in v0.7.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobDeleteTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, id int64[](https://pkg.go.dev/builtin#int64)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), error[](https://pkg.go.dev/builtin#error))
```

JobDeleteTx deletes the job with the given ID from the database, returning the deleted row if it was deleted. Jobs in the running state are not deleted, instead returning rivertype.ErrJobRunning. This variant lets a caller retry a job atomically alongside other database changes. A deleted job isn't deleted until the transaction commits, and if the transaction rolls back, so too is the deleted job. 
####  func (*Client[TTx]) [JobGet](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1520) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobGet "Go to Client.JobGet") added in v0.0.19

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobGet(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), id int64[](https://pkg.go.dev/builtin#int64)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), error[](https://pkg.go.dev/builtin#error))
```

JobGet fetches a single job by its ID. Returns the up-to-date JobRow for the specified jobID if it exists. Returns ErrNotFound if the job doesn't exist. 
####  func (*Client[TTx]) [JobGetTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1530) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobGetTx "Go to Client.JobGetTx") added in v0.0.19

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobGetTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, id int64[](https://pkg.go.dev/builtin#int64)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), error[](https://pkg.go.dev/builtin#error))
```

JobGetTx fetches a single job by its ID, within a transaction. Returns the up-to-date JobRow for the specified jobID if it exists. Returns ErrNotFound if the job doesn't exist. 
####  func (*Client[TTx]) [JobList](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2394) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobList "Go to Client.JobList") added in v0.0.17

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobList(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), params *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)) (*JobListResult[](https://pkg.go.dev/github.com/riverqueue/river#JobListResult), error[](https://pkg.go.dev/builtin#error))
```

JobList returns a paginated list of jobs matching the provided filters. The provided context is used for the underlying Postgres query and can be used to cancel the operation or apply a timeout. 

```
params := river.NewJobListParams().First(10).State(rivertype.JobStateCompleted)
jobRows, err := client.JobList(ctx, params)
if err != nil {
	// handle error
}

```

####  func (*Client[TTx]) [JobListTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2439) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobListTx "Go to Client.JobListTx") added in v0.0.17

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobListTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, params *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)) (*JobListResult[](https://pkg.go.dev/github.com/riverqueue/river#JobListResult), error[](https://pkg.go.dev/builtin#error))
```

JobListTx returns a paginated list of jobs matching the provided filters. The provided context is used for the underlying Postgres query and can be used to cancel the operation or apply a timeout. 

```
params := river.NewJobListParams().First(10).States(river.JobStateCompleted)
jobRows, err := client.JobListTx(ctx, tx, params)
if err != nil {
	// handle error
}

```

####  func (*Client[TTx]) [JobRetry](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1545) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobRetry "Go to Client.JobRetry") added in v0.0.19

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobRetry(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), id int64[](https://pkg.go.dev/builtin#int64)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), error[](https://pkg.go.dev/builtin#error))
```

JobRetry updates the job with the given ID to make it immediately available to be retried. Jobs in the running state are not touched, while jobs in any other state are made available. To prevent jobs already waiting in the queue from being set back in line, the job's scheduled_at field is set to the current time only if it's not already in the past. 
MaxAttempts is also incremented by one if the job has already exhausted its max attempts. 
####  func (*Client[TTx]) [JobRetryTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1562) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobRetryTx "Go to Client.JobRetryTx") added in v0.0.19

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobRetryTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, id int64[](https://pkg.go.dev/builtin#int64)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), error[](https://pkg.go.dev/builtin#error))
```

JobRetryTx updates the job with the given ID to make it immediately available to be retried, within the specified transaction. This variant lets a caller retry a job atomically alongside other database changes. A retried job isn't visible to be worked until the transaction commits, and if the transaction rolls back, so too is the retried job. 
Jobs in the running state are not touched, while jobs in any other state are made available. To prevent jobs already waiting in the queue from being set back in line, the job's scheduled_at field is set to the current time only if it's not already in the past. 
MaxAttempts is also incremented by one if the job has already exhausted its max attempts. 
####  func (*Client[TTx]) [JobUpdate](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1588) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobUpdate "Go to Client.JobUpdate") added in v0.29.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobUpdate(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), id int64[](https://pkg.go.dev/builtin#int64), params *JobUpdateParams[](https://pkg.go.dev/github.com/riverqueue/river#JobUpdateParams)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), error[](https://pkg.go.dev/builtin#error))
```

JobUpdate updates the job with the given ID. 
If JobUpdateParams.Output is not set, this function may be used inside a job work function to set a job's output based on output recorded so far using RecordOutput. 
####  func (*Client[TTx]) [JobUpdateTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1599) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.JobUpdateTx "Go to Client.JobUpdateTx") added in v0.29.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) JobUpdateTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, id int64[](https://pkg.go.dev/builtin#int64), params *JobUpdateParams[](https://pkg.go.dev/github.com/riverqueue/river#JobUpdateParams)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), error[](https://pkg.go.dev/builtin#error))
```

JobUpdateTx updates the job with the given ID. 
If JobUpdateParams.Output is not set, this function may be used inside a job work function to set a job's output based on output recorded so far using RecordOutput. 
This variant updates the job inside of a transaction. 
####  func (*Client[TTx]) [Notify](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2473) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.Notify "Go to Client.Notify") added in v0.29.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) Notify() *ClientNotifyBundle[](https://pkg.go.dev/github.com/riverqueue/river#ClientNotifyBundle)[TTx]
```

Notify retrieves a notification bundle for the client (in the sense of Postgres listen/notify) used to send notifications of various kinds. 
####  func (*Client[TTx]) [PeriodicJobs](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2529) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.PeriodicJobs "Go to Client.PeriodicJobs") added in v0.2.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) PeriodicJobs() *PeriodicJobBundle[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle)
```

PeriodicJobs returns the currently configured set of periodic jobs for the client, and can be used to add new or remove existing ones. 
This function should only be invoked on clients capable of running perioidc jobs. Running periodic jobs requires that the client be electable as leader to run maintenance services, and being electable as leader requires that a client be started. To be startable, a client must have Queues and Workers configured. Invoking this function will panic if these conditions aren't met. 
####  func (*Client[TTx]) [Pilot](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2541) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.Pilot "Go to Client.Pilot") added in v0.13.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) Pilot() riverpilot[](https://pkg.go.dev/github.com/riverqueue/river/rivershared/riverpilot).Pilot[](https://pkg.go.dev/github.com/riverqueue/river/rivershared/riverpilot#Pilot)
```

Pilot returns the pilot in use by the pilot. If not configured, this is often simply StandardPilot. 
API is not stable. DO NOT USE. 
####  func (*Client[TTx]) [QueueGet](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2554) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueGet "Go to Client.QueueGet") added in v0.5.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) QueueGet(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), name string[](https://pkg.go.dev/builtin#string)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).Queue[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#Queue), error[](https://pkg.go.dev/builtin#error))
```

QueueGet returns the queue with the given name. If the queue has not recently been active or does not exist, returns ErrNotFound. 
The provided context is used for the underlying Postgres query and can be used to cancel the operation or apply a timeout. 
####  func (*Client[TTx]) [QueueGetTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2566) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueGetTx "Go to Client.QueueGetTx") added in v0.8.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) QueueGetTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, name string[](https://pkg.go.dev/builtin#string)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).Queue[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#Queue), error[](https://pkg.go.dev/builtin#error))
```

QueueGetTx returns the queue with the given name. If the queue has not recently been active or does not exist, returns ErrNotFound. 
The provided context is used for the underlying Postgres query and can be used to cancel the operation or apply a timeout. 
####  func (*Client[TTx]) [QueueList](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2591) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueList "Go to Client.QueueList") added in v0.5.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) QueueList(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), params *QueueListParams[](https://pkg.go.dev/github.com/riverqueue/river#QueueListParams)) (*QueueListResult[](https://pkg.go.dev/github.com/riverqueue/river#QueueListResult), error[](https://pkg.go.dev/builtin#error))
```

QueueList returns a list of all queues that are currently active or were recently active. Limit and offset can be used to paginate the results. 
The provided context is used for the underlying Postgres query and can be used to cancel the operation or apply a timeout. 

```
params := river.NewQueueListParams().First(10)
queueRows, err := client.QueueListTx(ctx, tx, params)
if err != nil {
	// handle error
}

```

####  func (*Client[TTx]) [QueueListTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2618) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueListTx "Go to Client.QueueListTx") added in v0.8.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) QueueListTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, params *QueueListParams[](https://pkg.go.dev/github.com/riverqueue/river#QueueListParams)) (*QueueListResult[](https://pkg.go.dev/github.com/riverqueue/river#QueueListResult), error[](https://pkg.go.dev/builtin#error))
```

QueueListTx returns a list of all queues that are currently active or were recently active. Limit and offset can be used to paginate the results. 
The provided context is used for the underlying Postgres query and can be used to cancel the operation or apply a timeout. 

```
params := river.NewQueueListParams().First(10)
queueRows, err := client.QueueListTx(ctx, tx, params)
if err != nil {
	// handle error
}

```

####  func (*Client[TTx]) [QueuePause](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2645) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.QueuePause "Go to Client.QueuePause") added in v0.5.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) QueuePause(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), name string[](https://pkg.go.dev/builtin#string), opts *QueuePauseOpts[](https://pkg.go.dev/github.com/riverqueue/river#QueuePauseOpts)) error[](https://pkg.go.dev/builtin#error)
```

QueuePause pauses the queue with the given name. When a queue is paused, clients will not fetch any more jobs for that particular queue. To pause all queues at once, use the special queue name "*". 
Clients with a configured notifier should receive a notification about the paused queue(s) within a few milliseconds of the transaction commit. Clients in poll-only mode will pause after their next poll for queue configuration. 
The provided context is used for the underlying Postgres update and can be used to cancel the operation or apply a timeout. The opts are reserved for future functionality. 
####  func (*Client[TTx]) [QueuePauseTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2685) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.QueuePauseTx "Go to Client.QueuePauseTx") added in v0.8.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) QueuePauseTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, name string[](https://pkg.go.dev/builtin#string), opts *QueuePauseOpts[](https://pkg.go.dev/github.com/riverqueue/river#QueuePauseOpts)) error[](https://pkg.go.dev/builtin#error)
```

QueuePauseTx pauses the queue with the given name. When a queue is paused, clients will not fetch any more jobs for that particular queue. To pause all queues at once, use the special queue name "*". 
Clients with a configured notifier should receive a notification about the paused queue(s) within a few milliseconds of the transaction commit. Clients in poll-only mode will pause after their next poll for queue configuration. 
The provided context is used for the underlying Postgres update and can be used to cancel the operation or apply a timeout. The opts are reserved for future functionality. 
####  func (*Client[TTx]) [QueueResume](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2715) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueResume "Go to Client.QueueResume") added in v0.5.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) QueueResume(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), name string[](https://pkg.go.dev/builtin#string), opts *QueuePauseOpts[](https://pkg.go.dev/github.com/riverqueue/river#QueuePauseOpts)) error[](https://pkg.go.dev/builtin#error)
```

QueueResume resumes the queue with the given name. If the queue was previously paused, any clients configured to work that queue will resume fetching additional jobs. To resume all queues at once, use the special queue name "*". 
Clients with a configured notifier should receive a notification about the resumed queue(s) within a few milliseconds of the transaction commit. Clients in poll-only mode will resume after their next poll for queue configuration. 
The provided context is used for the underlying Postgres update and can be used to cancel the operation or apply a timeout. The opts are reserved for future functionality. 
####  func (*Client[TTx]) [QueueResumeTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2756) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueResumeTx "Go to Client.QueueResumeTx") added in v0.8.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) QueueResumeTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, name string[](https://pkg.go.dev/builtin#string), opts *QueuePauseOpts[](https://pkg.go.dev/github.com/riverqueue/river#QueuePauseOpts)) error[](https://pkg.go.dev/builtin#error)
```

QueueResumeTx resumes the queue with the given name. If the queue was previously paused, any clients configured to work that queue will resume fetching additional jobs. To resume all queues at once, use the special queue name "*". 
Clients with a configured notifier should receive a notification about the resumed queue(s) within a few milliseconds of the transaction commit. Clients in poll-only mode will resume after their next poll for queue configuration. 
The provided context is used for the underlying Postgres update and can be used to cancel the operation or apply a timeout. The opts are reserved for future functionality. 
####  func (*Client[TTx]) [QueueUpdate](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2783) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueUpdate "Go to Client.QueueUpdate") added in v0.20.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) QueueUpdate(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), name string[](https://pkg.go.dev/builtin#string), params *QueueUpdateParams[](https://pkg.go.dev/github.com/riverqueue/river#QueueUpdateParams)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).Queue[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#Queue), error[](https://pkg.go.dev/builtin#error))
```

QueueUpdate updates a queue's settings in the database. These settings override the settings in the client (if applied). 
####  func (*Client[TTx]) [QueueUpdateTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2806) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.QueueUpdateTx "Go to Client.QueueUpdateTx") added in v0.20.2

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) QueueUpdateTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, name string[](https://pkg.go.dev/builtin#string), params *QueueUpdateParams[](https://pkg.go.dev/github.com/riverqueue/river#QueueUpdateParams)) (*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).Queue[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#Queue), error[](https://pkg.go.dev/builtin#error))
```

QueueUpdateTx updates a queue's settings in the database. These settings override the settings in the client (if applied). 
####  func (*Client[TTx]) [Queues](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2547) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.Queues "Go to Client.Queues") added in v0.10.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) Queues() *QueueBundle[](https://pkg.go.dev/github.com/riverqueue/river#QueueBundle)
```

Queues returns the currently configured set of queues for the client, and can be used to add new ones. 
####  func (*Client[TTx]) [Schema](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2880) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.Schema "Go to Client.Schema") added in v0.24.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) Schema() string[](https://pkg.go.dev/builtin#string)
```

Schema returns the configured schema for the client. 
####  func (*Client[TTx]) [Start](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1048) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.Start "Go to Client.Start")

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) Start(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context)) error[](https://pkg.go.dev/builtin#error)
```

Start starts the client's job fetching and working loops. Once this is called, the client will run in a background goroutine until stopped. All jobs are run with a context inheriting from the provided context, but with a timeout deadline applied based on the job's settings. 
A graceful shutdown stops fetching new jobs but allows any previously fetched jobs to complete. This can be initiated with the Stop method. 
A more abrupt shutdown can be achieved by either cancelling the provided context or by calling StopAndCancel. This will not only stop fetching new jobs, but will also cancel the context for any currently-running jobs. If using StopAndCancel, there's no need to also call Stop. 
####  func (*Client[TTx]) [Stop](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1240) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.Stop "Go to Client.Stop")

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) Stop(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context)) error[](https://pkg.go.dev/builtin#error)
```

Stop performs a graceful shutdown of the Client. It signals all producers to stop fetching new jobs and waits for any fetched or in-progress jobs to complete before exiting. If the provided context is done before shutdown has completed, Stop will return immediately with the context's error. 
If SoftStopTimeout is configured, running job contexts will be automatically cancelled after the timeout elapses, escalating to a hard stop. This also applies when stop is initiated by cancelling the context passed to Start. 
There's no need to call this method if a hard stop has already been initiated by cancelling the context passed to Start or by calling StopAndCancel. 
####  func (*Client[TTx]) [StopAndCancel](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1272) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.StopAndCancel "Go to Client.StopAndCancel")

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) StopAndCancel(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context)) error[](https://pkg.go.dev/builtin#error)
```

StopAndCancel shuts down the client and cancels all work in progress. It is a more aggressive stop than Stop because the contexts for any in-progress jobs are cancelled. However, it still waits for jobs to complete before returning, even though their contexts are cancelled. If the provided context is done before shutdown has completed, StopAndCancel will return immediately with the context's error. 
This can also be initiated by cancelling the context passed to Start. There is no need to call this method if the context passed to Start is cancelled instead. 
In most cases, using Stop with SoftStopTimeout configured is preferable to calling StopAndCancel directly. SoftStopTimeout gives running jobs a chance to finish before automatically escalating to context cancellation, providing graceful stop semantics without requiring manual orchestration of Stop and StopAndCancel. 
####  func (*Client[TTx]) [Stopped](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1295) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.Stopped "Go to Client.Stopped") added in v0.0.11

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) Stopped() <-chan struct{}
```

Stopped returns a channel that will be closed when the Client has stopped. It can be used to wait for a graceful shutdown to complete. 
It is not affected by any contexts passed to Stop or StopAndCancel. 
####  func (*Client[TTx]) [Subscribe](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1316) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.Subscribe "Go to Client.Subscribe")

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) Subscribe(kinds ...EventKind[](https://pkg.go.dev/github.com/riverqueue/river#EventKind)) (<-chan *Event[](https://pkg.go.dev/github.com/riverqueue/river#Event), func())
```

Subscribe subscribes to the provided kinds of events that occur within the client, like EventKindJobCompleted for when a job completes. 
Returns a channel over which to receive events along with a cancel function that can be used to cancel and tear down resources associated with the subscription. It's recommended but not necessary to invoke the cancel function. Resources will be freed when the client stops in case it's not. 
The event channel is buffered and sends on it are non-blocking. Consumers must process events in a timely manner or it's possible for events to be dropped. Any slow operations performed in a response to a receipt (e.g. persisting to a database) should be made asynchronous to avoid event loss. 
Callers must specify the kinds of events they're interested in. This allows for forward compatibility in case new kinds of events are added in future versions. If new event kinds are added, callers will have to explicitly add them to their requested list and ensure they can be handled correctly. 
####  func (*Client[TTx]) [SubscribeConfig](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1344) [¶](https://pkg.go.dev/github.com/riverqueue/river#Client.SubscribeConfig "Go to Client.SubscribeConfig") added in v0.1.0

```
func (c *Client[](https://pkg.go.dev/github.com/riverqueue/river#Client)[TTx]) SubscribeConfig(config *SubscribeConfig[](https://pkg.go.dev/github.com/riverqueue/river#SubscribeConfig)) (<-chan *Event[](https://pkg.go.dev/github.com/riverqueue/river#Event), func())
```

SubscribeConfig is a special internal variant of Subscribe that lets us inject an overridden channel size. 
####  type [ClientNotifyBundle](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2480) [¶](https://pkg.go.dev/github.com/riverqueue/river#ClientNotifyBundle "Go to ClientNotifyBundle") added in v0.29.0

```
type ClientNotifyBundle[TTx any[](https://pkg.go.dev/builtin#any)] struct {
	// contains filtered or unexported fields
}
```

ClientNotifyBundle sends various notifications for a client (in the sense of Postgres listen/notify). Functions are on this bundle struct instead of the top-level client to keep them grouped together and better organized. 
####  func (*ClientNotifyBundle[TTx]) [RequestResign](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2488) [¶](https://pkg.go.dev/github.com/riverqueue/river#ClientNotifyBundle.RequestResign "Go to ClientNotifyBundle.RequestResign") added in v0.29.0

```
func (c *ClientNotifyBundle[](https://pkg.go.dev/github.com/riverqueue/river#ClientNotifyBundle)[TTx]) RequestResign(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context)) error[](https://pkg.go.dev/builtin#error)
```

RequestResign sends a notification requesting that the current leader resign. This usually causes the resignation of the current leader, but may have no effect if no leader is currently elected. 
####  func (*ClientNotifyBundle[TTx]) [RequestResignTx](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2500) [¶](https://pkg.go.dev/github.com/riverqueue/river#ClientNotifyBundle.RequestResignTx "Go to ClientNotifyBundle.RequestResignTx") added in v0.29.0

```
func (c *ClientNotifyBundle[](https://pkg.go.dev/github.com/riverqueue/river#ClientNotifyBundle)[TTx]) RequestResignTx(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx) error[](https://pkg.go.dev/builtin#error)
```

RequestResignTx sends a notification requesting that the current leader resign. This usually causes the resignation of the current leader, but may have no effect if no leader is currently elected. 
This variant sends a notification in a transaction, which means that no notification is sent until the transaction commits. 
####  type [ClientRetryPolicy](https://github.com/riverqueue/river/blob/v0.39.0/retry_policy.go#L20) [¶](https://pkg.go.dev/github.com/riverqueue/river#ClientRetryPolicy "Go to ClientRetryPolicy")

```
type ClientRetryPolicy interface {
	// NextRetry calculates when the next retry for a failed job should take place
	// given when it was last attempted and its number of attempts, or any other
	// of the job's properties a user-configured retry policy might want to
	// consider.
	NextRetry(job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow)) time[](https://pkg.go.dev/time).Time[](https://pkg.go.dev/time#Time)
}
```

ClientRetryPolicy is an interface that can be implemented to provide a retry policy for how River deals with failed jobs at the client level (when a worker does not define an override for `NextRetry`). Jobs are scheduled to be retried in the future up until they've reached the job's max attempts, at which pointed they're set as discarded. 
The ClientRetryPolicy does not have access to generics and operates on the raw JobRow struct with encoded args. 
####  type [Config](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L92) [¶](https://pkg.go.dev/github.com/riverqueue/river#Config "Go to Config")

```
type Config struct {
	// AdvisoryLockPrefix is a configurable 32-bit prefix that River will use
	// when generating any key to acquire a Postgres advisory lock. All advisory
	// locks share the same 64-bit number space, so this allows a calling
	// application to guarantee that a River advisory lock will never conflict
	// with one of its own by cordoning each type to its own prefix.
	//
	// If this value isn't set, River defaults to generating key hashes across
	// the entire 64-bit advisory lock number space, which is large enough that
	// conflicts are exceedingly unlikely. If callers don't strictly need this
	// option then it's recommended to leave it unset because the prefix leaves
	// only 32 bits of number space for advisory lock hashes, so it makes
	// internally conflicting River-generated keys more likely.
	//
	// Advisory locks are currently only used for the deprecated fallback/slow
	// path of unique job insertion when pending, scheduled, available, or running
	// are omitted from a customized ByState configuration.
	AdvisoryLockPrefix int32[](https://pkg.go.dev/builtin#int32)

	// CancelledJobRetentionPeriod is the amount of time to keep cancelled jobs
	// around before they're removed permanently.
	//
	// The special value -1 disables deletion of cancelled jobs.
	//
	// Defaults to 24 hours.
	CancelledJobRetentionPeriod time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// CompletedJobRetentionPeriod is the amount of time to keep completed jobs
	// around before they're removed permanently.
	//
	// The special value -1 disables deletion of completed jobs.
	//
	// Defaults to 24 hours.
	CompletedJobRetentionPeriod time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// DiscardedJobRetentionPeriod is the amount of time to keep discarded jobs
	// around before they're removed permanently.
	//
	// The special value -1 disables deletion of discarded jobs.
	//
	// Defaults to 7 days.
	DiscardedJobRetentionPeriod time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// ErrorHandler can be configured to be invoked in case of an error or panic
	// occurring in a job. This is often useful for logging and exception
	// tracking, but can also be used to customize retry behavior.
	ErrorHandler ErrorHandler[](https://pkg.go.dev/github.com/riverqueue/river#ErrorHandler)

	// FetchCooldown is the minimum amount of time to wait between fetches of new
	// jobs. Jobs will only be fetched *at most* this often, but if no new jobs
	// are coming in via LISTEN/NOTIFY then fetches may be delayed as long as
	// FetchPollInterval.
	//
	// Throughput is limited by this value.
	//
	// Individual QueueConfig structs may override this for a specific queue.
	//
	// Defaults to 100 ms.
	FetchCooldown time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// FetchPollInterval is the amount of time between periodic fetches for new
	// jobs. Typically new jobs will be picked up ~immediately after insert via
	// LISTEN/NOTIFY, but this provides a fallback.
	//
	// Individual QueueConfig structs may override this for a specific queue.
	//
	// Defaults to 1 second.
	FetchPollInterval time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// ID is the unique identifier for this client. If not set, a random
	// identifier will be generated.
	//
	// This is used to identify the client in job attempts and for leader election.
	// This value must be unique across all clients in the same database and
	// schema and there must not be more than one process running with the same
	// ID at the same time.
	//
	// A client ID should differ between different programs and must be unique
	// across all clients in the same database and schema. There must not be
	// more than one process running with the same ID at the same time.
	// Duplicate IDs between processes will lead to facilities like leader
	// election or client statistics to fail in novel ways. However, the client
	// ID is shared by all executors within any given client. (i.e.  different
	// Go processes have different IDs, but IDs are shared within any given
	// process.)
	//
	// If in doubt, leave this property empty.
	ID string[](https://pkg.go.dev/builtin#string)

	// JobCleanerTimeout is the timeout of the individual queries within the job
	// cleaner.
	//
	// Defaults to 30 seconds, which should be more than enough time for most
	// deployments.
	JobCleanerTimeout time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// JobInsertMiddleware are optional functions that can be called around job
	// insertion.
	//
	// Deprecated: Prefer the use of Middleware instead (which may contain
	// instances of rivertype.JobInsertMiddleware).
	JobInsertMiddleware []rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertMiddleware[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertMiddleware)

	// JobTimeout is the maximum amount of time a job is allowed to run before its
	// context is cancelled. A timeout of zero means JobTimeoutDefault will be
	// used, whereas a value of -1 means the job's context will not be cancelled
	// unless the Client is shutting down.
	//
	// Defaults to 1 minute.
	JobTimeout time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// Hooks are functions that may activate at certain points during a job's
	// lifecycle (see rivertype.Hook), installed globally.
	//
	// The effect of hooks in this list will depend on the specific hook
	// interfaces they implement, so for example implementing
	// rivertype.HookInsertBegin will cause the hook to be invoked before a job
	// is inserted, or implementing rivertype.HookWorkBegin will cause it to be
	// invoked before a job is worked. Hook structs may implement multiple hook
	// interfaces.
	//
	// Order in this list is significant. A hook that appears first will be
	// entered before a hook that appears later. For any particular phase, order
	// is relevant only for hooks that will run for that phase. For example, if
	// two rivertype.HookInsertBegin are separated by a rivertype.HookWorkBegin,
	// during job insertion those two outer hooks will run one after another,
	// and the work hook between them will not run. When a job is worked, the
	// work hook runs and the insertion hooks on either side of it are skipped.
	//
	// Jobs may have their own specific hooks by implementing JobArgsWithHooks.
	Hooks []rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).Hook[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#Hook)

	// Logger is the structured logger to use for logging purposes. If none is
	// specified, logs will be emitted to STDOUT with messages at warn level
	// or higher.
	Logger *slog[](https://pkg.go.dev/log/slog).Logger[](https://pkg.go.dev/log/slog#Logger)

	// MaxAttempts is the default number of times a job will be retried before
	// being discarded. This value is applied to all jobs by default, and can be
	// overridden on individual job types on the JobArgs or on a per-job basis at
	// insertion time.
	//
	// If not specified, defaults to 25 (MaxAttemptsDefault).
	MaxAttempts int[](https://pkg.go.dev/builtin#int)

	// Middleware contains middleware that may activate at certain points during
	// a job's lifecycle (see rivertype.Middleware), installed globally.
	//
	// The effect of middleware in this list will depend on the specific
	// middleware interfaces they implement, so for example implementing
	// rivertype.JobInsertMiddleware will cause the middleware to be invoked
	// when jobs are inserted, and implementing rivertype.WorkerMiddleware will
	// cause it to be invoked when a job is worked. Middleware structs may
	// implement multiple middleware interfaces.
	//
	// Order in this list is significant. Middleware that appears first will be
	// entered before middleware that appears later. For any particular phase,
	// order is relevant only for middlewares that will run for that phase. For
	// example, if two rivertype.JobInsertMiddleware are separated by a
	// rivertype.WorkerMiddleware, during job insertion those two outer
	// middlewares will run one after another, and the work middleware between
	// them will not run. When a job is worked, the work middleware runs and the
	// insertion middlewares on either side of it are skipped.
	Middleware []rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).Middleware[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#Middleware)

	// PeriodicJobs are a set of periodic jobs to run at the specified intervals
	// in the client.
	PeriodicJobs []*PeriodicJob[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJob)

	// PollOnly starts the client in "poll only" mode, which avoids issuing
	// `LISTEN` statements to wait for events like a leadership resignation or
	// new job available. The program instead polls periodically to look for
	// changes (checking for new jobs on the period in FetchPollInterval).
	//
	// The downside of this mode of operation is that events will usually be
	// noticed less quickly. A new job in the queue may have to wait up to
	// FetchPollInterval to be locked for work. When a leader resigns, it will
	// be up to five seconds before a new one elects itself.
	//
	// The upside is that it makes River compatible with systems where
	// listen/notify isn't available. For example, PgBouncer in transaction
	// pooling mode.
	PollOnly bool[](https://pkg.go.dev/builtin#bool)

	// Queues is a list of queue names for this client to operate on along with
	// configuration for the queue like the maximum number of workers to run for
	// each queue.
	//
	// This field may be omitted for a program that's only queueing jobs rather
	// than working them. If it's specified, then Workers must also be given.
	Queues map[string[](https://pkg.go.dev/builtin#string)]QueueConfig[](https://pkg.go.dev/github.com/riverqueue/river#QueueConfig)

	// ReindexerSchedule is the schedule for running the reindexer. If nil, the
	// reindexer will run at midnight UTC every day.
	ReindexerSchedule PeriodicSchedule[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicSchedule)

	// ReindexerIndexNames customizes which indexes River periodically reindexes.
	// If nil, River uses [ReindexerIndexNamesDefault]. If non-nil, the provided
	// slice is used as the exact list.
	ReindexerIndexNames []string[](https://pkg.go.dev/builtin#string)

	// ReindexerTimeout is the amount of time to wait for the reindexer to run a
	// single reindex operation before cancelling it via context. Set to -1 to
	// disable the timeout.
	//
	// Defaults to 1 minute.
	ReindexerTimeout time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// RescueStuckJobsAfter is the amount of time a job can be running before it
	// is considered stuck. A stuck job which has not yet reached its max attempts
	// will be scheduled for a retry, while one which has exhausted its attempts
	// will be discarded.  This prevents jobs from being stuck forever if a worker
	// crashes or is killed.
	//
	// Note that this can result in repeat or duplicate execution of a job that is
	// not actually stuck but is still working. The value should be set higher
	// than the maximum duration you expect your jobs to run. Setting a value too
	// low will result in more duplicate executions, whereas too high of a value
	// will result in jobs being stuck for longer than necessary before they are
	// retried.
	//
	// RescueStuckJobsAfter must be greater than JobTimeout. Otherwise, jobs
	// would become eligible for rescue while they're still running.
	//
	// Defaults to 1 hour, or in cases where JobTimeout has been configured and
	// is greater than 1 hour, JobTimeout + 1 hour.
	RescueStuckJobsAfter time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// RetryPolicy is a configurable retry policy for the client.
	//
	// Defaults to DefaultRetryPolicy.
	RetryPolicy ClientRetryPolicy[](https://pkg.go.dev/github.com/riverqueue/river#ClientRetryPolicy)

	// Schema is a non-standard Schema where River tables are located. All table
	// references in database queries will use this value as a prefix.
	//
	// Defaults to empty, which causes the client to look for tables using the
	// setting of Postgres `search_path`.
	Schema string[](https://pkg.go.dev/builtin#string)

	// SoftStopTimeout is the maximum amount of time that the client will wait
	// for running jobs to finish during a stop before their contexts are
	// cancelled. After the timeout elapses, the client escalates to a hard stop
	// by cancelling the context of all running jobs. This applies regardless of
	// how stop is initiated — whether by calling Stop, StopAndCancel, or by
	// cancelling the context passed to Start.
	//
	// In combination with signal.NotifyContext on the context passed to Start,
	// this can simplify graceful stop to:
	//
	//	ctx, stop := signal.NotifyContext(ctx, syscall.SIGINT, syscall.SIGTERM)
	//	defer stop()
	//
	//	if err := client.Start(ctx); err != nil { ... }
	//	<-client.Stopped()
	//
	// The signal cancels the Start context, which initiates a soft stop. If
	// running jobs haven't finished after SoftStopTimeout, their contexts are
	// automatically cancelled to trigger a hard stop.
	//
	// StopAndCancel bypasses the timeout entirely and cancels job contexts
	// immediately.
	//
	// Defaults to no timeout (wait indefinitely for jobs to finish).
	SoftStopTimeout time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// SkipJobKindValidation causes the job kind format validation check to be
	// skipped. This is available as an interim stopgap for users that have
	// invalid job kind names, but would rather disable the check rather than
	// fix them immediately.
	//
	// Deprecated: This option will be removed in a future versions so that job
	// kinds will always have to have a valid format.
	SkipJobKindValidation bool[](https://pkg.go.dev/builtin#bool)

	// SkipUnknownJobCheck is a flag to control whether the client should skip
	// checking to see if a registered worker exists in the client's worker bundle
	// for a job arg prior to insertion.
	//
	// This can be set to true to allow a client to insert jobs which are
	// intended to be worked by a different client which effectively makes
	// the client's insertion behavior mimic that of an insert-only client.
	//
	// Defaults to false.
	SkipUnknownJobCheck bool[](https://pkg.go.dev/builtin#bool)

	// Test holds configuration specific to test environments.
	Test TestConfig[](https://pkg.go.dev/github.com/riverqueue/river#TestConfig)

	// TestOnly can be set to true to disable certain features that are useful
	// in production, but which may be harmful to tests, in ways like having the
	// effect of making them slower. It should not be used outside of test
	// suites.
	//
	// For example, queue maintenance services normally stagger their startup
	// with a random jittered sleep so they don't all try to work at the same
	// time. This is nice in production, but makes starting and stopping the
	// client in a test case slower.
	TestOnly bool[](https://pkg.go.dev/builtin#bool)

	// Workers is a bundle of registered job workers.
	//
	// This field may be omitted for a program that's only enqueueing jobs
	// rather than working them, but if it is configured the client can validate
	// ahead of time that a worker is properly registered for an inserted job.
	// (i.e.  That it wasn't forgotten by accident.)
	Workers *Workers[](https://pkg.go.dev/github.com/riverqueue/river#Workers)

	// WorkerMiddleware are optional functions that can be called around
	// all job executions.
	//
	// Deprecated: Prefer the use of Middleware instead (which may contain
	// instances of rivertype.WorkerMiddleware).
	WorkerMiddleware []rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).WorkerMiddleware[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#WorkerMiddleware)
	// contains filtered or unexported fields
}
```

Config is the configuration for a Client. 
Both Queues and Workers are required for a client to work jobs, but an insert-only client can be initialized by omitting Queues, and not calling Start for the client. Workers can also be omitted, but it's better to include it so River can check that inserted job kinds have a worker that can run them. 
####  func (*Config) [WithDefaults](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L430) [¶](https://pkg.go.dev/github.com/riverqueue/river#Config.WithDefaults "Go to Config.WithDefaults") added in v0.17.0

```
func (c *Config[](https://pkg.go.dev/github.com/riverqueue/river#Config)) WithDefaults() *Config[](https://pkg.go.dev/github.com/riverqueue/river#Config)
```

WithDefaults returns a copy of the Config with all default values applied. 
####  type [DefaultClientRetryPolicy](https://github.com/riverqueue/river/blob/v0.39.0/retry_policy.go#L29) [¶](https://pkg.go.dev/github.com/riverqueue/river#DefaultClientRetryPolicy "Go to DefaultClientRetryPolicy")

```
type DefaultClientRetryPolicy struct {
	// contains filtered or unexported fields
}
```

DefaultClientRetryPolicy is River's default retry policy. 
####  func (*DefaultClientRetryPolicy) [NextRetry](https://github.com/riverqueue/river/blob/v0.39.0/retry_policy.go#L49) [¶](https://pkg.go.dev/github.com/riverqueue/river#DefaultClientRetryPolicy.NextRetry "Go to DefaultClientRetryPolicy.NextRetry")

```
func (p *DefaultClientRetryPolicy[](https://pkg.go.dev/github.com/riverqueue/river#DefaultClientRetryPolicy)) NextRetry(job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow)) time[](https://pkg.go.dev/time).Time[](https://pkg.go.dev/time#Time)
```

NextRetry gets the next retry given for the given job, accounting for when it was last attempted and what attempt number that was. Reschedules using a basic exponential backoff of `ATTEMPT^4`, so after the first failure a new try will be scheduled in 1 seconds, 16 seconds after the second, 1 minute and 21 seconds after the third, etc. 
Snoozes do not count as attempts and do not influence retry behavior. Earlier versions of River would allow the attempt to increment each time a job was snoozed. Although this has been changed and snoozes now decrement the attempt count, we can maintain the same retry schedule even for pre-existing jobs by using the number of errors instead of the attempt count. This ensures consistent behavior across River versions. 
At degenerately high retry counts (>= 310) the policy starts adding the equivalent of the maximum of time.Duration to each retry, about 292 years. The schedule is no longer exponential past this point. 
####  type [ErrorHandler](https://github.com/riverqueue/river/blob/v0.39.0/error_handler.go#L12) [¶](https://pkg.go.dev/github.com/riverqueue/river#ErrorHandler "Go to ErrorHandler")

```
type ErrorHandler interface {
	// HandleError is invoked in case of an error occurring in a job.
	//
	// Context is descended from the one used to start the River client that
	// worked the job. Errors are handled above all middleware, so changes made
	// to context by a middleware are not available in the context.
	HandleError(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), err error[](https://pkg.go.dev/builtin#error)) *ErrorHandlerResult[](https://pkg.go.dev/github.com/riverqueue/river#ErrorHandlerResult)

	// HandlePanic is invoked in case of a panic occurring in a job.
	//
	// Context is descended from the one used to start the River client that
	// worked the job. Panics are handled above all middleware, so changes made
	// to context by a middleware are not available in the context (however,
	// panics can be recovered from in any middleware where middleware context
	// is available).
	HandlePanic(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), panicVal any[](https://pkg.go.dev/builtin#any), trace string[](https://pkg.go.dev/builtin#string)) *ErrorHandlerResult[](https://pkg.go.dev/github.com/riverqueue/river#ErrorHandlerResult)
}
```

ErrorHandler provides an interface that will be invoked in case of an error or panic occurring in the job. This is often useful for logging and exception tracking, but can also be used to customize retry behavior. 
####  type [ErrorHandlerResult](https://github.com/riverqueue/river/blob/v0.39.0/error_handler.go#L30) [¶](https://pkg.go.dev/github.com/riverqueue/river#ErrorHandlerResult "Go to ErrorHandlerResult")

```
type ErrorHandlerResult struct {
	// SetCancelled can be set to true to fail the job immediately and
	// permanently. By default it'll continue to follow the configured retry
	// schedule.
	SetCancelled bool[](https://pkg.go.dev/builtin#bool)
}
```

####  type [Event](https://github.com/riverqueue/river/blob/v0.39.0/event.go#L50) [¶](https://pkg.go.dev/github.com/riverqueue/river#Event "Go to Event")

```
type Event struct {
	// Kind is the kind of event. Receivers should read this field and respond
	// accordingly. Subscriptions will only receive event kinds that they
	// requested when creating a subscription with Subscribe.
	Kind EventKind[](https://pkg.go.dev/github.com/riverqueue/river#EventKind)

	// Job contains job-related information.
	Job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow)

	// JobStats are statistics about the run of a job.
	JobStats *JobStatistics[](https://pkg.go.dev/github.com/riverqueue/river#JobStatistics)

	// Queue contains queue-related information.
	Queue *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).Queue[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#Queue)
}
```

Event wraps an event that occurred within a River client, like a job being completed. 
####  type [EventKind](https://github.com/riverqueue/river/blob/v0.39.0/event.go#L11) [¶](https://pkg.go.dev/github.com/riverqueue/river#EventKind "Go to EventKind")

```
type EventKind string[](https://pkg.go.dev/builtin#string)
```

EventKind is a kind of event to subscribe to from a client. 

```
const (
	// EventKindJobCancelled occurs when a job is cancelled.
	EventKindJobCancelled EventKind[](https://pkg.go.dev/github.com/riverqueue/river#EventKind) = "job_cancelled"

	// EventKindJobCompleted occurs when a job is completed.
	EventKindJobCompleted EventKind[](https://pkg.go.dev/github.com/riverqueue/river#EventKind) = "job_completed"

	// EventKindJobFailed occurs when a job fails. Occurs both when a job fails
	// and will be retried and when a job fails for the last time and will be
	// discarded. Callers can use job fields like `Attempt` and `State` to
	// differentiate each type of occurrence.
	EventKindJobFailed EventKind[](https://pkg.go.dev/github.com/riverqueue/river#EventKind) = "job_failed"

	// EventKindJobSnoozed occurs when a job is snoozed.
	EventKindJobSnoozed EventKind[](https://pkg.go.dev/github.com/riverqueue/river#EventKind) = "job_snoozed"

	// EventKindQueuePaused occurs when a queue is paused.
	EventKindQueuePaused EventKind[](https://pkg.go.dev/github.com/riverqueue/river#EventKind) = "queue_paused"

	// EventKindQueueResumed occurs when a queue is resumed.
	EventKindQueueResumed EventKind[](https://pkg.go.dev/github.com/riverqueue/river#EventKind) = "queue_resumed"
)
```

####  type [HookDefaults](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L12) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookDefaults "Go to HookDefaults") added in v0.19.0

```
type HookDefaults struct{}
```

HookDefaults should be embedded on any hooks implementation. It helps identify a struct as hooks, and guarantee forward compatibility in case additions are necessary to the rivertype.Hook interface. 
####  func (*HookDefaults) [IsHook](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L14) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookDefaults.IsHook "Go to HookDefaults.IsHook") added in v0.19.0

```
func (d *HookDefaults[](https://pkg.go.dev/github.com/riverqueue/river#HookDefaults)) IsHook() bool[](https://pkg.go.dev/builtin#bool)
```

####  type [HookInsertBeginFunc](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L18) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookInsertBeginFunc "Go to HookInsertBeginFunc") added in v0.19.0

```
type HookInsertBeginFunc func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), params *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertParams[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertParams)) error[](https://pkg.go.dev/builtin#error)
```

HookInsertBeginFunc is a convenience helper for implementing rivertype.HookInsertBegin using a simple function instead of a struct. 
####  func (HookInsertBeginFunc) [InsertBegin](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L20) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookInsertBeginFunc.InsertBegin "Go to HookInsertBeginFunc.InsertBegin") added in v0.19.0

```
func (f HookInsertBeginFunc[](https://pkg.go.dev/github.com/riverqueue/river#HookInsertBeginFunc)) InsertBegin(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), params *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertParams[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertParams)) error[](https://pkg.go.dev/builtin#error)
```

####  func (HookInsertBeginFunc) [IsHook](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L24) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookInsertBeginFunc.IsHook "Go to HookInsertBeginFunc.IsHook") added in v0.19.0

```
func (f HookInsertBeginFunc[](https://pkg.go.dev/github.com/riverqueue/river#HookInsertBeginFunc)) IsHook() bool[](https://pkg.go.dev/builtin#bool)
```

####  type [HookPeriodicJobsStartFunc](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L28) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookPeriodicJobsStartFunc "Go to HookPeriodicJobsStartFunc") added in v0.29.0

```
type HookPeriodicJobsStartFunc func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), params *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).HookPeriodicJobsStartParams[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#HookPeriodicJobsStartParams)) error[](https://pkg.go.dev/builtin#error)
```

HookPeriodicJobsStartFunc is a convenience helper for implementing rivertype.HookPeriodicJobsStart using a simple function instead of a struct. 
####  func (HookPeriodicJobsStartFunc) [IsHook](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L30) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookPeriodicJobsStartFunc.IsHook "Go to HookPeriodicJobsStartFunc.IsHook") added in v0.29.0

```
func (f HookPeriodicJobsStartFunc[](https://pkg.go.dev/github.com/riverqueue/river#HookPeriodicJobsStartFunc)) IsHook() bool[](https://pkg.go.dev/builtin#bool)
```

####  func (HookPeriodicJobsStartFunc) [Start](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L32) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookPeriodicJobsStartFunc.Start "Go to HookPeriodicJobsStartFunc.Start") added in v0.29.0

```
func (f HookPeriodicJobsStartFunc[](https://pkg.go.dev/github.com/riverqueue/river#HookPeriodicJobsStartFunc)) Start(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), params *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).HookPeriodicJobsStartParams[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#HookPeriodicJobsStartParams)) error[](https://pkg.go.dev/builtin#error)
```

####  type [HookWorkBeginFunc](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L38) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookWorkBeginFunc "Go to HookWorkBeginFunc") added in v0.19.0

```
type HookWorkBeginFunc func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow)) error[](https://pkg.go.dev/builtin#error)
```

HookWorkBeginFunc is a convenience helper for implementing rivertype.HookWorkBegin using a simple function instead of a struct. 
####  func (HookWorkBeginFunc) [IsHook](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L44) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookWorkBeginFunc.IsHook "Go to HookWorkBeginFunc.IsHook") added in v0.19.0

```
func (f HookWorkBeginFunc[](https://pkg.go.dev/github.com/riverqueue/river#HookWorkBeginFunc)) IsHook() bool[](https://pkg.go.dev/builtin#bool)
```

####  func (HookWorkBeginFunc) [WorkBegin](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L40) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookWorkBeginFunc.WorkBegin "Go to HookWorkBeginFunc.WorkBegin") added in v0.19.0

```
func (f HookWorkBeginFunc[](https://pkg.go.dev/github.com/riverqueue/river#HookWorkBeginFunc)) WorkBegin(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow)) error[](https://pkg.go.dev/builtin#error)
```

####  type [HookWorkEndFunc](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L48) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookWorkEndFunc "Go to HookWorkEndFunc") added in v0.21.0

```
type HookWorkEndFunc func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), err error[](https://pkg.go.dev/builtin#error)) error[](https://pkg.go.dev/builtin#error)
```

HookWorkEndFunc is a convenience helper for implementing rivertype.HookWorkEnd using a simple function instead of a struct. 
####  func (HookWorkEndFunc) [IsHook](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L54) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookWorkEndFunc.IsHook "Go to HookWorkEndFunc.IsHook") added in v0.21.0

```
func (f HookWorkEndFunc[](https://pkg.go.dev/github.com/riverqueue/river#HookWorkEndFunc)) IsHook() bool[](https://pkg.go.dev/builtin#bool)
```

####  func (HookWorkEndFunc) [WorkEnd](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go#L50) [¶](https://pkg.go.dev/github.com/riverqueue/river#HookWorkEndFunc.WorkEnd "Go to HookWorkEndFunc.WorkEnd") added in v0.21.0

```
func (f HookWorkEndFunc[](https://pkg.go.dev/github.com/riverqueue/river#HookWorkEndFunc)) WorkEnd(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), err error[](https://pkg.go.dev/builtin#error)) error[](https://pkg.go.dev/builtin#error)
```

####  type [InsertManyParams](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1833) [¶](https://pkg.go.dev/github.com/riverqueue/river#InsertManyParams "Go to InsertManyParams")

```
type InsertManyParams struct {
	// Args are the arguments of the job to insert.
	Args JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs)

	// InsertOpts are insertion options for this job.
	InsertOpts *InsertOpts[](https://pkg.go.dev/github.com/riverqueue/river#InsertOpts)
}
```

InsertManyParams encapsulates a single job combined with insert options for use with batch insertion. 
####  type [InsertOpts](https://github.com/riverqueue/river/blob/v0.39.0/insert_opts.go#L25) [¶](https://pkg.go.dev/github.com/riverqueue/river#InsertOpts "Go to InsertOpts")

```
type InsertOpts struct {
	// MaxAttempts is the maximum number of total attempts (including both the
	// original run and all retries) before a job is abandoned and set as
	// discarded.
	MaxAttempts int[](https://pkg.go.dev/builtin#int)

	// Metadata is a JSON object blob of arbitrary data that will be stored with
	// the job. Users should not overwrite or remove anything stored in this
	// field by River.
	Metadata []byte[](https://pkg.go.dev/builtin#byte)

	// Pending indicates that the job should be inserted in the `pending` state.
	// Pending jobs are not immediately available to be worked and are never
	// deleted, but they can be used to indicate work which should be performed in
	// the future once they are made available (or scheduled) by some external
	// update.
	Pending bool[](https://pkg.go.dev/builtin#bool)

	// Priority is the priority of the job, with 1 being the highest priority and
	// 4 being the lowest. When fetching available jobs to work, the highest
	// priority jobs will always be fetched before any lower priority jobs are
	// fetched. Note that if your workers are swamped with more high-priority jobs
	// then they can handle, lower priority jobs may not be fetched.
	//
	// Defaults to PriorityDefault.
	Priority int[](https://pkg.go.dev/builtin#int)

	// Queue is the name of the job queue in which to insert the job.
	//
	// Defaults to the job kind's default queue if set via
	// `JobArgsWithInsertOpts`, or QueueDefault if not.
	Queue string[](https://pkg.go.dev/builtin#string)

	// ScheduledAt is a time in future at which to schedule the job (i.e. in
	// cases where it shouldn't be run immediately). The job is guaranteed not
	// to run before this time, but may run slightly after depending on the
	// number of other scheduled jobs and how busy the queue is.
	//
	// Use of this option generally only makes sense when passing options into
	// Insert rather than when a job args struct is implementing
	// JobArgsWithInsertOpts, however, it will work in both cases.
	ScheduledAt time[](https://pkg.go.dev/time).Time[](https://pkg.go.dev/time#Time)

	// Tags are an arbitrary list of keywords to add to the job. They have no
	// functional behavior and are meant entirely as a user-specified construct
	// to help group and categorize jobs.
	//
	// Tags should conform to the regex `\A[\w][\w\-]+[\w]\z` and be a maximum
	// of 255 characters long. No special characters are allowed.
	//
	// If tags are specified from both a job args override and from options on
	// Insert, the latter takes precedence. Tags are not merged.
	Tags []string[](https://pkg.go.dev/builtin#string)

	// UniqueOpts returns options relating to job uniqueness. An empty struct
	// avoids setting any worker-level unique options.
	UniqueOpts UniqueOpts[](https://pkg.go.dev/github.com/riverqueue/river#UniqueOpts)
}
```

InsertOpts are optional settings for a new job which can be provided at job insertion time. These will override any default InsertOpts settings provided by JobArgsWithInsertOpts, as well as any global defaults. 
####  type [Job](https://github.com/riverqueue/river/blob/v0.39.0/job.go#L9) [¶](https://pkg.go.dev/github.com/riverqueue/river#Job "Go to Job")

```
type Job[T JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs)] struct {
	*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow)

	// Args are the arguments for the job.
	Args T
}
```

Job represents a single unit of work, holding both the arguments and information for a job with args of type T. 
####  func [JobCompleteTx](https://github.com/riverqueue/river/blob/v0.39.0/job_complete_tx.go#L28) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobCompleteTx "Go to JobCompleteTx")

```
func JobCompleteTx[TDriver riverdriver[](https://pkg.go.dev/github.com/riverqueue/river/riverdriver).Driver[](https://pkg.go.dev/github.com/riverqueue/river/riverdriver#Driver)[TTx], TTx any[](https://pkg.go.dev/builtin#any), TArgs JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs)](ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, job *Job[](https://pkg.go.dev/github.com/riverqueue/river#Job)[TArgs]) (*Job[](https://pkg.go.dev/github.com/riverqueue/river#Job)[TArgs], error[](https://pkg.go.dev/builtin#error))
```

JobCompleteTx marks the job as completed as part of transaction tx. If tx is rolled back, the completion will be as well. 
The function needs to know the type of the River database driver, which is the same as the one in use by Client, but the other generic parameters can be inferred. An invocation should generally look like: 

```
_, err := river.JobCompleteTx[*riverpgxv5.Driver](ctx, tx, job)
if err != nil {
	// handle error
}

```

Returns the updated, completed job. 
####  func [ResumableSetStepCursorTx](https://github.com/riverqueue/river/blob/v0.39.0/resumable_step_tx.go#L45) [¶](https://pkg.go.dev/github.com/riverqueue/river#ResumableSetStepCursorTx "Go to ResumableSetStepCursorTx") added in v0.37.0

```
func ResumableSetStepCursorTx[TDriver riverdriver[](https://pkg.go.dev/github.com/riverqueue/river/riverdriver).Driver[](https://pkg.go.dev/github.com/riverqueue/river/riverdriver#Driver)[TTx], TTx any[](https://pkg.go.dev/builtin#any), TArgs JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs), TCursor any[](https://pkg.go.dev/builtin#any)](ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, job *Job[](https://pkg.go.dev/github.com/riverqueue/river#Job)[TArgs], cursor TCursor) (*Job[](https://pkg.go.dev/github.com/riverqueue/river#Job)[TArgs], error[](https://pkg.go.dev/builtin#error))
```

ResumableSetStepCursorTx immediately persists the current resumable step and cursor as part of transaction tx. If tx is rolled back, the step and cursor update will be as well. 
Normally, a resumable job's step progress is recorded after it runs along with its result status. This is normally sufficient, but because it happens out-of-transaction, there's a chance that it doesn't happen in case of panic or other abrupt termination. This function is useful in cases where a resumable worker needs a guarantee of a checkpoint being recorded durably, at the cost of an extra database operation. 
Must be called from within a ResumableStepCursor callback. The current step name to persist is read from context. 
####  func [ResumableSetStepTx](https://github.com/riverqueue/river/blob/v0.39.0/resumable_step_tx.go#L28) [¶](https://pkg.go.dev/github.com/riverqueue/river#ResumableSetStepTx "Go to ResumableSetStepTx") added in v0.37.0

```
func ResumableSetStepTx[TDriver riverdriver[](https://pkg.go.dev/github.com/riverqueue/river/riverdriver).Driver[](https://pkg.go.dev/github.com/riverqueue/river/riverdriver#Driver)[TTx], TTx any[](https://pkg.go.dev/builtin#any), TArgs JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs)](ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), tx TTx, job *Job[](https://pkg.go.dev/github.com/riverqueue/river#Job)[TArgs]) (*Job[](https://pkg.go.dev/github.com/riverqueue/river#Job)[TArgs], error[](https://pkg.go.dev/builtin#error))
```

ResumableSetStepTx immediately persists the current resumable step as part of transaction tx. If tx is rolled back, the step update will be as well. 
Normally, a resumable job's step progress is recorded after it runs along with its result status. This is normally sufficient, but because it happens out-of-transaction, there's a chance that it doesn't happen in case of panic or other abrupt termination. This function is useful in cases where a resumable worker needs a guarantee of a checkpoint being recorded durably, at the cost of an extra database operation. 
Must be called from within a ResumableStep or ResumableStepCursor callback. The current step name to persist is read from context. 
####  type [JobArgs](https://github.com/riverqueue/river/blob/v0.39.0/job.go#L21) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobArgs "Go to JobArgs")

```
type JobArgs interface {
	// Kind is a string that uniquely identifies the type of job. This must be
	// provided on your job arguments struct. Jobs are identified by a string
	// instead of being based on type names so that previously inserted jobs
	// can be worked across deploys even if job/worker types are renamed.
	//
	// Kinds should be formatted without spaces like `my_custom_job`,
	// `mycustomjob`, or `my-custom-job`. Many special characters like colons,
	// dots, hyphens, and underscores are allowed, but those like spaces and
	// commas, which would interfere with UI functionality, are invalid.
	//
	// After initially deploying a job, it's generally not safe to rename its
	// kind (unless the database is completely empty) because River won't know
	// which worker should work the old kind. Job kinds can be renamed safely
	// over multiple deploys using the JobArgsWithKindAliases interface.
	Kind() string[](https://pkg.go.dev/builtin#string)
}
```

JobArgs is an interface that represents the arguments for a job of type T. These arguments are serialized into JSON and stored in the database. 
The struct is serialized using `encoding/json`. All exported fields are serialized, unless skipped with a struct field tag. 
####  type [JobArgsWithHooks](https://github.com/riverqueue/river/blob/v0.39.0/job.go#L75) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobArgsWithHooks "Go to JobArgsWithHooks") added in v0.19.0

```
type JobArgsWithHooks interface {
	// Hooks returns specific hooks to run for this job type. These will run
	// after the global hooks configured on the client.
	//
	// Warning: Hooks returned should be based on the job type only and be
	// invariant of the specific contents of a job. Hooks are extracted by
	// instantiating a generic instance of the job even when a specific instance
	// is available, so any conditional logic within will be ignored. This is
	// done because although specific job information may be available in some
	// hook contexts like on InsertBegin, it won't be in others like WorkBegin.
	Hooks() []rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).Hook[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#Hook)
}
```

JobArgsWithHooks is an interface that job args can implement to attach specific hooks (i.e. other than those globally installed to a client) to certain kinds of jobs. 
####  type [JobArgsWithInsertOpts](https://github.com/riverqueue/river/blob/v0.39.0/job.go#L90) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobArgsWithInsertOpts "Go to JobArgsWithInsertOpts")

```
type JobArgsWithInsertOpts interface {
	// InsertOpts returns options for all jobs of this job type, overriding any
	// system defaults. These can also be overridden at insertion time.
	InsertOpts() InsertOpts[](https://pkg.go.dev/github.com/riverqueue/river#InsertOpts)
}
```

JobArgsWithInsertOpts is an extra interface that a job may implement on top of JobArgs to provide insertion-time options for all jobs of this type. 
####  type [JobArgsWithKindAliases](https://github.com/riverqueue/river/blob/v0.39.0/job.go#L66) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobArgsWithKindAliases "Go to JobArgsWithKindAliases") added in v0.22.0

```
type JobArgsWithKindAliases interface {
	// KindAliases returns alias kinds that an associated job args worker will
	// respond to.
	KindAliases() []string[](https://pkg.go.dev/builtin#string)
}
```

JobArgsWithKindAliases is an interface that jobs args can implement to provide an alternate kind which a worker will be registered under in addition to the primary kind. This is useful for renaming a job kind in a safe manner so that any jobs already in the database aren't orphaned. 
Renaming a job is a three part process. To begin, a job args with its original name: 

```
type jobArgsBeingRenamed struct{}

func (a jobArgsBeingRenamed) Kind() string { return "old_name" }

```

Rename by putting the new name in Kind and moving the old name to KindAliases: 

```
type jobArgsBeingRenamed struct{}

func (a jobArgsBeingRenamed) Kind() string          { return "new_name" }
func (a jobArgsBeingRenamed) KindAliases() []string { return []string{"old_name"} }

```

After all jobs inserted under the original name have finished working (including all their possible retries, which notably might take up to three weeks on the default retry policy), remove KindAliases: 

```
type jobArgsBeingRenamed struct{}

func (a jobArgsBeingRenamed) Kind() string { return "new_name" }

```

####  type [JobCancelError](https://github.com/riverqueue/river/blob/v0.39.0/error.go#L16) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobCancelError "Go to JobCancelError") added in v0.14.0

```
type JobCancelError = rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobCancelError[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobCancelError)
```

JobCancelError is the error type returned by JobCancel. It should not be initialized directly, but is returned from the [JobCancel](https://pkg.go.dev/github.com/riverqueue/river#JobCancel) function and can be used for test assertions. 
####  type [JobDeleteManyParams](https://github.com/riverqueue/river/blob/v0.39.0/delete_many_params.go#L13) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams "Go to JobDeleteManyParams") added in v0.24.0

```
type JobDeleteManyParams struct {
	// contains filtered or unexported fields
}
```

JobDeleteManyParams specifies the parameters for a JobDeleteMany query. It must be initialized with NewJobDeleteManyParams. Params can be built by chaining methods on the JobDeleteManyParams object: 

```
params := NewJobDeleteManyParams().First(100).States(river.JobStateCompleted)

```

####  func [NewJobDeleteManyParams](https://github.com/riverqueue/river/blob/v0.39.0/delete_many_params.go#L26) [¶](https://pkg.go.dev/github.com/riverqueue/river#NewJobDeleteManyParams "Go to NewJobDeleteManyParams") added in v0.24.0

```
func NewJobDeleteManyParams() *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)
```

NewJobDeleteManyParams creates a new JobDeleteManyParams to delete jobs sorted by ID in ascending order, deleting 100 jobs at most. 
####  func (*JobDeleteManyParams) [First](https://github.com/riverqueue/river/blob/v0.39.0/delete_many_params.go#L70) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.First "Go to JobDeleteManyParams.First") added in v0.24.0

```
func (p *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)) First(count int[](https://pkg.go.dev/builtin#int)) *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)
```

First returns an updated filter set that will only delete the first count jobs. 
Count must be between 1 and 10_000, inclusive, or this will panic. 
####  func (*JobDeleteManyParams) [IDs](https://github.com/riverqueue/river/blob/v0.39.0/delete_many_params.go#L84) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.IDs "Go to JobDeleteManyParams.IDs") added in v0.24.0

```
func (p *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)) IDs(ids ...int64[](https://pkg.go.dev/builtin#int64)) *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)
```

IDs returns an updated filter set that will only delete jobs with the given IDs. 
####  func (*JobDeleteManyParams) [Kinds](https://github.com/riverqueue/river/blob/v0.39.0/delete_many_params.go#L93) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.Kinds "Go to JobDeleteManyParams.Kinds") added in v0.24.0

```
func (p *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)) Kinds(kinds ...string[](https://pkg.go.dev/builtin#string)) *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)
```

Kinds returns an updated filter set that will only delete jobs of the given kinds. 
####  func (*JobDeleteManyParams) [Priorities](https://github.com/riverqueue/river/blob/v0.39.0/delete_many_params.go#L102) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.Priorities "Go to JobDeleteManyParams.Priorities") added in v0.24.0

```
func (p *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)) Priorities(priorities ...int16[](https://pkg.go.dev/builtin#int16)) *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)
```

Priorities returns an updated filter set that will only delete jobs with the given priorities. 
####  func (*JobDeleteManyParams) [Queues](https://github.com/riverqueue/river/blob/v0.39.0/delete_many_params.go#L111) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.Queues "Go to JobDeleteManyParams.Queues") added in v0.24.0

```
func (p *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)) Queues(queues ...string[](https://pkg.go.dev/builtin#string)) *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)
```

Queues returns an updated filter set that will only delete jobs from the given queues. 
####  func (*JobDeleteManyParams) [States](https://github.com/riverqueue/river/blob/v0.39.0/delete_many_params.go#L120) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.States "Go to JobDeleteManyParams.States") added in v0.24.0

```
func (p *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)) States(states ...rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobState[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobState)) *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)
```

States returns an updated filter set that will only delete jobs in the given states. 
####  func (*JobDeleteManyParams) [UnsafeAll](https://github.com/riverqueue/river/blob/v0.39.0/delete_many_params.go#L142) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams.UnsafeAll "Go to JobDeleteManyParams.UnsafeAll") added in v0.25.0

```
func (p *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)) UnsafeAll() *JobDeleteManyParams[](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyParams)
```

UnsafeAll is a special directive that allows unbounded job deletion without any filters. Normally, filters like IDs or Kinds is required to scope down the deletion so that the caller doesn't accidentally delete all non-running jobs. Invoking UnsafeAll removes this safety guard so that all jobs can be removed arbitrarily. 
Example of use: 

```
deleteRes, err = client.JobDeleteMany(ctx, NewJobDeleteManyParams().UnsafeAll())
if err != nil {
	// handle error
}

```

It only makes sense to call this function if no filters have yet been applied on the parameters object. If some have already, calling it will panic. 
####  type [JobDeleteManyResult](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2315) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobDeleteManyResult "Go to JobDeleteManyResult") added in v0.24.0

```
type JobDeleteManyResult struct {
	// Jobs is a slice of job returned as part of the list operation.
	Jobs []*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow)
}
```

JobDeleteManyResult is the result of a job list operation. It contains a list of jobs and a cursor for fetching the next page of results. 
####  type [JobInsertMiddlewareDefaults](https://github.com/riverqueue/river/blob/v0.39.0/middleware_defaults.go#L22) deprecated added in v0.13.0

```
type JobInsertMiddlewareDefaults struct{ MiddlewareDefaults[](https://pkg.go.dev/github.com/riverqueue/river#MiddlewareDefaults) }
```

JobInsertMiddlewareDefaults is an embeddable struct that provides default implementations for the rivertype.JobInsertMiddleware. Use of this struct is recommended in case rivertype.JobInsertMiddleware is expanded in the future so that existing code isn't unexpectedly broken during an upgrade. 
Deprecated: Prefer embedding the more general MiddlewareDefaults instead. 
####  func (*JobInsertMiddlewareDefaults) [InsertMany](https://github.com/riverqueue/river/blob/v0.39.0/middleware_defaults.go#L24) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareDefaults.InsertMany "Go to JobInsertMiddlewareDefaults.InsertMany") added in v0.13.0

```
func (d *JobInsertMiddlewareDefaults[](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareDefaults)) InsertMany(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), manyParams []*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertParams[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertParams), doInner func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context)) ([]*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertResult[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertResult), error[](https://pkg.go.dev/builtin#error))) ([]*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertResult[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertResult), error[](https://pkg.go.dev/builtin#error))
```

####  type [JobInsertMiddlewareFunc](https://github.com/riverqueue/river/blob/v0.39.0/middleware_defaults.go#L30) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareFunc "Go to JobInsertMiddlewareFunc") added in v0.21.0

```
type JobInsertMiddlewareFunc func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), manyParams []*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertParams[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertParams), doInner func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context)) ([]*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertResult[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertResult), error[](https://pkg.go.dev/builtin#error))) ([]*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertResult[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertResult), error[](https://pkg.go.dev/builtin#error))
```

JobInsertMiddlewareFunc is a convenience helper for implementing rivertype.JobInsertMiddleware using a simple function instead of a struct. 
####  func (JobInsertMiddlewareFunc) [InsertMany](https://github.com/riverqueue/river/blob/v0.39.0/middleware_defaults.go#L32) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareFunc.InsertMany "Go to JobInsertMiddlewareFunc.InsertMany") added in v0.21.0

```
func (f JobInsertMiddlewareFunc[](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareFunc)) InsertMany(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), manyParams []*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertParams[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertParams), doInner func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context)) ([]*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertResult[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertResult), error[](https://pkg.go.dev/builtin#error))) ([]*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobInsertResult[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobInsertResult), error[](https://pkg.go.dev/builtin#error))
```

####  func (JobInsertMiddlewareFunc) [IsMiddleware](https://github.com/riverqueue/river/blob/v0.39.0/middleware_defaults.go#L36) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareFunc.IsMiddleware "Go to JobInsertMiddlewareFunc.IsMiddleware") added in v0.21.0

```
func (f JobInsertMiddlewareFunc[](https://pkg.go.dev/github.com/riverqueue/river#JobInsertMiddlewareFunc)) IsMiddleware() bool[](https://pkg.go.dev/builtin#bool)
```

####  type [JobListCursor](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L18) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor "Go to JobListCursor") added in v0.0.17

```
type JobListCursor struct {
	// contains filtered or unexported fields
}
```

JobListCursor is used to specify a starting point for a paginated job list query. 
####  func [JobListCursorFromJob](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L28) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListCursorFromJob "Go to JobListCursorFromJob") added in v0.0.17

```
func JobListCursorFromJob(job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow)) *JobListCursor[](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor)
```

JobListCursorFromJob creates a JobListCursor from a JobRow. 
####  func (JobListCursor) [MarshalText](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L91) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor.MarshalText "Go to JobListCursor.MarshalText") added in v0.0.17

```
func (c JobListCursor[](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor)) MarshalText() ([]byte[](https://pkg.go.dev/builtin#byte), error[](https://pkg.go.dev/builtin#error))
```

MarshalText implements encoding.TextMarshaler to encode the cursor as an opaque string. 
####  func (*JobListCursor) [UnmarshalText](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L67) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor.UnmarshalText "Go to JobListCursor.UnmarshalText") added in v0.0.17

```
func (c *JobListCursor[](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor)) UnmarshalText(text []byte[](https://pkg.go.dev/builtin#byte)) error[](https://pkg.go.dev/builtin#error)
```

UnmarshalText implements encoding.TextUnmarshaler to decode the cursor from a previously marshaled string. 
####  type [JobListOrderByField](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L132) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListOrderByField "Go to JobListOrderByField") added in v0.0.17

```
type JobListOrderByField string[](https://pkg.go.dev/builtin#string)
```

JobListOrderByField specifies the field to sort by. 

```
const (
	// JobListOrderByID specifies that the sort should be by job ID.
	JobListOrderByID JobListOrderByField[](https://pkg.go.dev/github.com/riverqueue/river#JobListOrderByField) = "id"

	// JobListOrderByFinalizedAt specifies that the sort should be by
	// `finalized_at`.
	//
	// This option must be used in conjunction with filtering by only finalized
	// job states.
	JobListOrderByFinalizedAt JobListOrderByField[](https://pkg.go.dev/github.com/riverqueue/river#JobListOrderByField) = "finalized_at"

	// JobListOrderByScheduledAt specifies that the sort should be by
	// `scheduled_at`.
	JobListOrderByScheduledAt JobListOrderByField[](https://pkg.go.dev/github.com/riverqueue/river#JobListOrderByField) = "scheduled_at"

	// JobListOrderByTime specifies that the sort should be by the "best fit"
	// time field based on listed state. The best fit is determined by looking
	// at the first value given to JobListParams.States. If multiple states are
	// specified, the ones after the first will be ignored.
	//
	// The specific time field used for sorting depends on requested state:
	//
	// * States `available`, `retryable`, or `scheduled` use `scheduled_at`.
	// * State `running` uses `attempted_at`.
	// * States `cancelled`, `completed`, or `discarded` use `finalized_at`.
	JobListOrderByTime JobListOrderByField[](https://pkg.go.dev/github.com/riverqueue/river#JobListOrderByField) = "time"
)
```

####  type [JobListParams](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L167) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListParams "Go to JobListParams") added in v0.0.17

```
type JobListParams struct {
	// contains filtered or unexported fields
}
```

JobListParams specifies the parameters for a JobList query. It must be initialized with NewJobListParams. Params can be built by chaining methods on the JobListParams object: 

```
params := NewJobListParams().OrderBy(JobListOrderByTime, SortOrderAsc).First(100)

```

####  func [NewJobListParams](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L185) [¶](https://pkg.go.dev/github.com/riverqueue/river#NewJobListParams "Go to NewJobListParams") added in v0.0.17

```
func NewJobListParams() *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)
```

NewJobListParams creates a new JobListParams to return available jobs sorted by time in ascending order, returning 100 jobs at most. 
####  func (*JobListParams) [After](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L299) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.After "Go to JobListParams.After") added in v0.0.17

```
func (p *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)) After(cursor *JobListCursor[](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor)) *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)
```

After returns an updated filter set that will only return jobs after the given cursor. 
####  func (*JobListParams) [First](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L313) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.First "Go to JobListParams.First") added in v0.0.17

```
func (p *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)) First(count int[](https://pkg.go.dev/builtin#int)) *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)
```

First returns an updated filter set that will only return the first count jobs. 
Count must be between 1 and 10_000, inclusive, or this will panic. 
####  func (*JobListParams) [IDs](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L327) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.IDs "Go to JobListParams.IDs") added in v0.21.0

```
func (p *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)) IDs(ids ...int64[](https://pkg.go.dev/builtin#int64)) *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)
```

IDs returns an updated filter set that will only return jobs with the given IDs. 
####  func (*JobListParams) [Kinds](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L336) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Kinds "Go to JobListParams.Kinds") added in v0.0.23

```
func (p *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)) Kinds(kinds ...string[](https://pkg.go.dev/builtin#string)) *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)
```

Kinds returns an updated filter set that will only return jobs of the given kinds. 
####  func (*JobListParams) [Metadata](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L353) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Metadata "Go to JobListParams.Metadata") added in v0.0.17

```
func (p *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)) Metadata(json string[](https://pkg.go.dev/builtin#string)) *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)
```

Metadata returns an updated filter set that will return only jobs that has metadata which contains the given JSON fragment at its top level. This is equivalent to the `@>` operator in Postgres: 
<https://www.postgresql.org/docs/current/functions-json.html>
This function isn't supported in SQLite due to SQLite not having an equivalent operator to use, so there's no efficient way to implement it. We recommend the use of Where using a condition with a comparison on the `->>` operator instead. 
####  func (*JobListParams) [OrderBy](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L368) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.OrderBy "Go to JobListParams.OrderBy") added in v0.0.17

```
func (p *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)) OrderBy(field JobListOrderByField[](https://pkg.go.dev/github.com/riverqueue/river#JobListOrderByField), direction SortOrder[](https://pkg.go.dev/github.com/riverqueue/river#SortOrder)) *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)
```

OrderBy returns an updated filter set that will sort the results using the specified field and direction. 
If ordering by FinalizedAt, the States filter will be set to only include finalized job states unless it has already been overridden. 
####  func (*JobListParams) [Priorities](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L392) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Priorities "Go to JobListParams.Priorities") added in v0.21.0

```
func (p *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)) Priorities(priorities ...int16[](https://pkg.go.dev/builtin#int16)) *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)
```

Priorities returns an updated filter set that will only return jobs with the given priorities. 
####  func (*JobListParams) [Queues](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L401) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Queues "Go to JobListParams.Queues") added in v0.0.17

```
func (p *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)) Queues(queues ...string[](https://pkg.go.dev/builtin#string)) *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)
```

Queues returns an updated filter set that will only return jobs from the given queues. 
####  func (*JobListParams) [States](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L410) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.States "Go to JobListParams.States") added in v0.4.0

```
func (p *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)) States(states ...rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobState[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobState)) *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)
```

States returns an updated filter set that will only return jobs in the given states. 
####  func (*JobListParams) [Where](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L451) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListParams.Where "Go to JobListParams.Where") added in v0.23.0

```
func (p *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)) Where(sql string[](https://pkg.go.dev/builtin#string), namedArgsMany ...NamedArgs[](https://pkg.go.dev/github.com/riverqueue/river#NamedArgs)) *JobListParams[](https://pkg.go.dev/github.com/riverqueue/river#JobListParams)
```

Where is an all-encompassing query escape hatch that adds an arbitrary predicate after a list query's `WHERE ...` clause. Use of other JobListParams filters should be preferred where possible because they're safer and their compatibility between drivers is better guaranteed, but in case none is suitable, Where can be used as a last resort. 
For example, using Where to query with `jsonb_path_query_first(...)` using a JSON path, a function that's specific to Postgres: 

```
listParams = listParams.Where("jsonb_path_query_first(metadata, @json_path) = @json_val", NamedArgs{"json_path": "$.foo", "json_val": `"bar"`})

```

A JSON path can be used in a query in SQLite as well, but there the `->` or `->>` operators must be used instead: 

```
listParams = listParams.Where("metadata ->> @json_path = @json_val", NamedArgs{"json_path": "$.foo", "json_val": "bar"})

```

Arguments beyond the first are interpreted as named parameters. Each one should be present in the query SQL prefixed with a `@` symbol. Multiple sets of named parameters will be merged together, with values in later sets overwriting those in earlier ones. 
Calling Where multiple times will add multiple conditions separate by `AND`. Use `OR` instead by stuffing all conditions into a single Where invocation. 
Consider use of this function possibly hazardous! Any time raw SQL is in play, an application is opening itself up to SQL injection attacks. Never mix unsanitized user input into a SQL string, and use named parameters to curb the likelihood of injection. 
####  type [JobListResult](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2373) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobListResult "Go to JobListResult") added in v0.4.0

```
type JobListResult struct {
	// Jobs is a slice of job returned as part of the list operation.
	Jobs []*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow)

	// LastCursor is a cursor that can be used to list the next page of jobs.
	LastCursor *JobListCursor[](https://pkg.go.dev/github.com/riverqueue/river#JobListCursor)
}
```

JobListResult is the result of a job list operation. It contains a list of jobs and a cursor for fetching the next page of results. 
####  type [JobSnoozeError](https://github.com/riverqueue/river/blob/v0.39.0/error.go#L28) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobSnoozeError "Go to JobSnoozeError") added in v0.14.0

```
type JobSnoozeError = rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobSnoozeError[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobSnoozeError)
```

JobSnoozeError is the error type returned by JobSnooze. It should not be initialized directly, but is returned from the [JobSnooze](https://pkg.go.dev/github.com/riverqueue/river#JobSnooze) function and can be used for test assertions. 
####  type [JobStatistics](https://github.com/riverqueue/river/blob/v0.39.0/event.go#L67) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobStatistics "Go to JobStatistics")

```
type JobStatistics struct {
	CompleteDuration  time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration) // Time it took to set the job completed, discarded, or errored.
	QueueWaitDuration time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration) // Time the job spent waiting in available state before starting execution.
	RunDuration       time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration) // Time job spent running (measured around job worker.)
}
```

JobStatistics contains information about a single execution of a job. 
####  type [JobUpdateParams](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1575) [¶](https://pkg.go.dev/github.com/riverqueue/river#JobUpdateParams "Go to JobUpdateParams") added in v0.29.0

```
type JobUpdateParams struct {
	// Output is a new output value for a job.
	//
	// If not set, and a job is updated from inside a work function, the job's
	// output is set based on output recorded so far using RecordOutput.
	Output any[](https://pkg.go.dev/builtin#any)
}
```

JobUpdateParams contains parameters for Client.JobUpdate and Client.JobUpdateTx. 
####  type [MiddlewareDefaults](https://github.com/riverqueue/river/blob/v0.39.0/middleware_defaults.go#L12) [¶](https://pkg.go.dev/github.com/riverqueue/river#MiddlewareDefaults "Go to MiddlewareDefaults") added in v0.19.0

```
type MiddlewareDefaults struct{}
```

MiddlewareDefaults should be embedded on any middleware implementation. It helps identify a struct as middleware, and guarantees forward compatibility in case additions are necessary to the rivertype.Middleware interface. 
####  func (*MiddlewareDefaults) [IsMiddleware](https://github.com/riverqueue/river/blob/v0.39.0/middleware_defaults.go#L14) [¶](https://pkg.go.dev/github.com/riverqueue/river#MiddlewareDefaults.IsMiddleware "Go to MiddlewareDefaults.IsMiddleware") added in v0.19.0

```
func (d *MiddlewareDefaults[](https://pkg.go.dev/github.com/riverqueue/river#MiddlewareDefaults)) IsMiddleware() bool[](https://pkg.go.dev/builtin#bool)
```

####  type [NamedArgs](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L421) [¶](https://pkg.go.dev/github.com/riverqueue/river#NamedArgs "Go to NamedArgs") added in v0.23.0

```
type NamedArgs map[string[](https://pkg.go.dev/builtin#string)]any[](https://pkg.go.dev/builtin#any)
```

NamedArgs are named arguments for use with JobListParams.Where. Keys should look like "my_param", and map to parameters like "@my_param" in SQL queries. "@" are present in the SQL, but not in the keys of this map. 
####  type [PeriodicJob](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L29) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJob "Go to PeriodicJob")

```
type PeriodicJob struct {
	// contains filtered or unexported fields
}
```

PeriodicJob is a configuration for a periodic job. 
####  func [NewPeriodicJob](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L74) [¶](https://pkg.go.dev/github.com/riverqueue/river#NewPeriodicJob "Go to NewPeriodicJob")

```
func NewPeriodicJob(scheduleFunc PeriodicSchedule[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicSchedule), constructorFunc PeriodicJobConstructor[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobConstructor), opts *PeriodicJobOpts[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobOpts)) *PeriodicJob[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJob)
```

NewPeriodicJob returns a new PeriodicJob given a schedule and a constructor function. 
The schedule returns a time until the next time the periodic job should run. The helper PeriodicInterval is available for jobs that should run on simple, fixed intervals (e.g. every 15 minutes), and a custom schedule or third party cron package can be used for more complex scheduling (see the cron example). The constructor function is invoked each time a periodic job's schedule elapses, returning job arguments to insert along with optional insertion options. 
The periodic job scheduler is approximate and doesn't guarantee strong durability. It's started by the elected leader in a River cluster, and each periodic job is assigned an initial run time when that occurs. New run times are scheduled each time a job's target run time is reached and a new job inserted. However, each scheduler only retains in-memory state, so anytime a process quits or a new leader is elected, the whole process starts over without regard for the state of the last scheduler. The RunOnStart option can be used as a hedge to make sure that jobs with long run durations are guaranteed to occasionally run. 
####  type [PeriodicJobBundle](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L111) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle "Go to PeriodicJobBundle") added in v0.2.0

```
type PeriodicJobBundle struct {
	// contains filtered or unexported fields
}
```

PeriodicJobBundle is a bundle of currently configured periodic jobs. It's made accessible through Client, where new periodic jobs can be configured, and old ones removed. 
####  func (*PeriodicJobBundle) [Add](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L133) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.Add "Go to PeriodicJobBundle.Add") added in v0.2.0

```
func (b *PeriodicJobBundle[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle)) Add(periodicJob *PeriodicJob[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJob)) rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).PeriodicJobHandle[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#PeriodicJobHandle)
```

Add adds a new periodic job to the client. The job is queued immediately if RunOnStart is enabled, and then scheduled normally. 
Returns a periodic job handle which can be used to subsequently remove the job if desired. 
Adding or removing periodic jobs has no effect unless this client is elected leader because only the leader enqueues periodic jobs. To make sure that a new periodic job is fully enabled or disabled, it should be added or removed from _every_ active River client across all processes. 
####  func (*PeriodicJobBundle) [AddMany](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L157) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.AddMany "Go to PeriodicJobBundle.AddMany") added in v0.2.0

```
func (b *PeriodicJobBundle[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle)) AddMany(periodicJobs []*PeriodicJob[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJob)) []rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).PeriodicJobHandle[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#PeriodicJobHandle)
```

AddMany adds many new periodic jobs to the client. The jobs are queued immediately if their RunOnStart is enabled, and then scheduled normally. 
Returns a periodic job handle which can be used to subsequently remove the job if desired. 
Adding or removing periodic jobs has no effect unless this client is elected leader because only the leader enqueues periodic jobs. To make sure that a new periodic job is fully enabled or disabled, it should be added or removed from _every_ active River client across all processes. 
####  func (*PeriodicJobBundle) [AddManySafely](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L167) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.AddManySafely "Go to PeriodicJobBundle.AddManySafely") added in v0.23.0

```
func (b *PeriodicJobBundle[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle)) AddManySafely(periodicJobs []*PeriodicJob[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJob)) ([]rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).PeriodicJobHandle[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#PeriodicJobHandle), error[](https://pkg.go.dev/builtin#error))
```

AddManySafely is the same as AddMany, but it returns an error in the case of a validation problem or duplicate ID instead of panicking. 
####  func (*PeriodicJobBundle) [AddSafely](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L143) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.AddSafely "Go to PeriodicJobBundle.AddSafely") added in v0.23.0

```
func (b *PeriodicJobBundle[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle)) AddSafely(periodicJob *PeriodicJob[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJob)) (rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).PeriodicJobHandle[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#PeriodicJobHandle), error[](https://pkg.go.dev/builtin#error))
```

AddSafely is the same as Add, but it returns an error in the case of a validation problem or duplicate ID instead of panicking. 
####  func (*PeriodicJobBundle) [Clear](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L177) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.Clear "Go to PeriodicJobBundle.Clear") added in v0.2.0

```
func (b *PeriodicJobBundle[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle)) Clear()
```

Clear clears all periodic jobs, cancelling all scheduled runs. 
Adding or removing periodic jobs has no effect unless this client is elected leader because only the leader enqueues periodic jobs. To make sure that a new periodic job is fully enabled or disabled, it should be added or removed from _every_ active River client across all processes. 
####  func (*PeriodicJobBundle) [Remove](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L190) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.Remove "Go to PeriodicJobBundle.Remove") added in v0.2.0

```
func (b *PeriodicJobBundle[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle)) Remove(periodicJobHandle rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).PeriodicJobHandle[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#PeriodicJobHandle))
```

Remove removes a periodic job, cancelling all scheduled runs. 
Requires the use of the periodic job handle that was returned when the job was added. 
Adding or removing periodic jobs has no effect unless this client is elected leader because only the leader enqueues periodic jobs. To make sure that a new periodic job is fully enabled or disabled, it should be added or removed from _every_ active River client across all processes. 
####  func (*PeriodicJobBundle) [RemoveByID](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L205) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.RemoveByID "Go to PeriodicJobBundle.RemoveByID") added in v0.27.0

```
func (b *PeriodicJobBundle[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle)) RemoveByID(id string[](https://pkg.go.dev/builtin#string)) bool[](https://pkg.go.dev/builtin#bool)
```

RemoveByID removes a periodic job by ID, cancelling all scheduled runs. 
Adding or removing periodic jobs has no effect unless this client is elected leader because only the leader enqueues periodic jobs. To make sure that a new periodic job is fully enabled or disabled, it should be added or removed from _every_ active River client across all processes. 
Has no effect if no jobs with the given ID is configured. 
Returns true if a job with the given ID existed (and was removed), and false otherwise. 
####  func (*PeriodicJobBundle) [RemoveMany](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L218) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.RemoveMany "Go to PeriodicJobBundle.RemoveMany") added in v0.2.0

```
func (b *PeriodicJobBundle[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle)) RemoveMany(periodicJobHandles []rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).PeriodicJobHandle[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#PeriodicJobHandle))
```

RemoveMany removes many periodic jobs, cancelling all scheduled runs. 
Requires the use of the periodic job handles that were returned when the jobs were added. 
Adding or removing periodic jobs has no effect unless this client is elected leader because only the leader enqueues periodic jobs. To make sure that a new periodic job is fully enabled or disabled, it should be added or removed from _every_ active River client across all processes. 
####  func (*PeriodicJobBundle) [RemoveManyByID](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L231) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle.RemoveManyByID "Go to PeriodicJobBundle.RemoveManyByID") added in v0.27.0

```
func (b *PeriodicJobBundle[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobBundle)) RemoveManyByID(ids []string[](https://pkg.go.dev/builtin#string))
```

RemoveManyByID removes many periodic jobs by ID, cancelling all scheduled runs. 
Adding or removing periodic jobs has no effect unless this client is elected leader because only the leader enqueues periodic jobs. To make sure that a new periodic job is fully enabled or disabled, it should be added or removed from _every_ active River client across all processes. 
Has no effect if no jobs with the given IDs are configured. 
####  type [PeriodicJobConstructor](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L26) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobConstructor "Go to PeriodicJobConstructor")

```
type PeriodicJobConstructor func() (JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs), *InsertOpts[](https://pkg.go.dev/github.com/riverqueue/river#InsertOpts))
```

PeriodicJobConstructor is a function that gets called each time the paired PeriodicSchedule is triggered. 
A constructor must never block. It may return nil to indicate that no job should be inserted. 
####  type [PeriodicJobOpts](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L36) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicJobOpts "Go to PeriodicJobOpts")

```
type PeriodicJobOpts struct {
	// ID is an optional identifier for the job. Identifiers must be unique
	// between all periodic jobs and adding a periodic job will error if they're
	// not.
	ID string[](https://pkg.go.dev/builtin#string)

	// RunOnStart can be used to indicate that a periodic job should insert an
	// initial job as a new scheduler is started. This can be used as a hedge
	// for jobs with longer scheduled durations that may not get to expiry
	// before a new scheduler is elected.
	//
	// RunOnStart also applies when a new periodic job is added dynamically with
	// `PeriodicJobs().Add` or `PeriodicJobs().AddMany`. Jobs added this way
	// with RunOnStart set to true are inserted once, then continue with their
	// normal run schedule.
	RunOnStart bool[](https://pkg.go.dev/builtin#bool)
}
```

PeriodicJobOpts are options for a periodic job. 
####  type [PeriodicSchedule](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L15) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicSchedule "Go to PeriodicSchedule")

```
type PeriodicSchedule interface {
	// Next returns the next time at which the job should be run given the
	// current time.
	Next(current time[](https://pkg.go.dev/time).Time[](https://pkg.go.dev/time#Time)) time[](https://pkg.go.dev/time).Time[](https://pkg.go.dev/time#Time)
}
```

PeriodicSchedule is a schedule for a periodic job. Periodic jobs should generally have an interval of at least 1 minute, and never less than one second. 
####  func [NeverSchedule](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L90) [¶](https://pkg.go.dev/github.com/riverqueue/river#NeverSchedule "Go to NeverSchedule") added in v0.16.0

```
func NeverSchedule() PeriodicSchedule[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicSchedule)
```

NeverSchedule returns a PeriodicSchedule that never runs. 
####  func [PeriodicInterval](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go#L100) [¶](https://pkg.go.dev/github.com/riverqueue/river#PeriodicInterval "Go to PeriodicInterval")

```
func PeriodicInterval(interval time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)) PeriodicSchedule[](https://pkg.go.dev/github.com/riverqueue/river#PeriodicSchedule)
```

PeriodicInterval returns a simple PeriodicSchedule that runs at the given interval. 
####  type [QueueAlreadyAddedError](https://github.com/riverqueue/river/blob/v0.39.0/error.go#L49) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueAlreadyAddedError "Go to QueueAlreadyAddedError") added in v0.23.0

```
type QueueAlreadyAddedError struct {
	Name string[](https://pkg.go.dev/builtin#string)
}
```

QueueAlreadyAddedError is returned when attempting to add a queue that has already been added to the Client. 
####  func (*QueueAlreadyAddedError) [Error](https://github.com/riverqueue/river/blob/v0.39.0/error.go#L53) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueAlreadyAddedError.Error "Go to QueueAlreadyAddedError.Error") added in v0.23.0

```
func (e *QueueAlreadyAddedError[](https://pkg.go.dev/github.com/riverqueue/river#QueueAlreadyAddedError)) Error() string[](https://pkg.go.dev/builtin#string)
```

####  func (*QueueAlreadyAddedError) [Is](https://github.com/riverqueue/river/blob/v0.39.0/error.go#L57) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueAlreadyAddedError.Is "Go to QueueAlreadyAddedError.Is") added in v0.23.0

```
func (e *QueueAlreadyAddedError[](https://pkg.go.dev/github.com/riverqueue/river#QueueAlreadyAddedError)) Is(target error[](https://pkg.go.dev/builtin#error)) bool[](https://pkg.go.dev/builtin#bool)
```

####  type [QueueBundle](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2884) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueBundle "Go to QueueBundle") added in v0.10.0

```
type QueueBundle struct {
	// contains filtered or unexported fields
}
```

QueueBundle is a bundle for adding additional queues. It's made accessible through Client.Queues. 
####  func (*QueueBundle) [Add](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2905) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueBundle.Add "Go to QueueBundle.Add") added in v0.10.0

```
func (b *QueueBundle[](https://pkg.go.dev/github.com/riverqueue/river#QueueBundle)) Add(queueName string[](https://pkg.go.dev/builtin#string), queueConfig QueueConfig[](https://pkg.go.dev/github.com/riverqueue/river#QueueConfig)) error[](https://pkg.go.dev/builtin#error)
```

Add adds a new queue to the client. If the client is already started, a producer for the queue is started. Context is inherited from the one given to Client.Start. 
####  func (*QueueBundle) [Remove](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2944) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueBundle.Remove "Go to QueueBundle.Remove") added in v0.36.0

```
func (b *QueueBundle[](https://pkg.go.dev/github.com/riverqueue/river#QueueBundle)) Remove(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), queueName string[](https://pkg.go.dev/builtin#string)) error[](https://pkg.go.dev/builtin#error)
```

Remove removes a queue from the client, stopping the producer if the client is running. It waits for any jobs currently being worked in the queue to complete before returning. 
If the provided context is done before the producer has fully stopped, Remove returns the context's error and does not fully remove the queue, though the queue's producer may have started stopping, leaving it in a stopped state where it doesn't work new jobs. Call Remove again with a new context to remove it completely. 
Returns an error if the client is not configured to execute jobs or if the specified queue does not exist. 
####  type [QueueConfig](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L588) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueConfig "Go to QueueConfig")

```
type QueueConfig struct {
	// FetchCooldown is the minimum amount of time to wait between fetches of new
	// jobs. Jobs will only be fetched *at most* this often, but if no new jobs
	// are coming in via LISTEN/NOTIFY then fetches may be delayed as long as
	// FetchPollInterval.
	//
	// Throughput is limited by this value.
	//
	// If non-zero, this overrides the FetchCooldown setting in the Client's
	// Config.
	FetchCooldown time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// FetchPollInterval is the amount of time between periodic fetches for new
	// jobs. Typically new jobs will be picked up ~immediately after insert via
	// LISTEN/NOTIFY, but this provides a fallback.
	//
	// If non-zero, this overrides the FetchCooldown setting in the Client's
	// Config.
	FetchPollInterval time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// MaxWorkers is the maximum number of workers to run for the queue, or put
	// otherwise, the maximum parallelism to run.
	//
	// This is the maximum number of workers within this particular client
	// instance, but note that it doesn't control the total number of workers
	// across parallel processes. Installations will want to calculate their
	// total number by multiplying this number by the number of parallel nodes
	// running River clients configured to the same database and queue.
	//
	// Requires a minimum of 1, and a maximum of 10,000.
	MaxWorkers int[](https://pkg.go.dev/builtin#int)
}
```

QueueConfig contains queue-specific configuration. 
####  type [QueueListParams](https://github.com/riverqueue/river/blob/v0.39.0/queue_list_params.go#L8) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueListParams "Go to QueueListParams") added in v0.5.0

```
type QueueListParams struct {
	// contains filtered or unexported fields
}
```

QueueListParams specifies the parameters for a QueueList query. It must be initialized with NewQueueListParams. Params can be built by chaining methods on the QueueListParams object: 

```
params := NewQueueListParams().First(100)

```

####  func [NewQueueListParams](https://github.com/riverqueue/river/blob/v0.39.0/queue_list_params.go#L14) [¶](https://pkg.go.dev/github.com/riverqueue/river#NewQueueListParams "Go to NewQueueListParams") added in v0.5.0

```
func NewQueueListParams() *QueueListParams[](https://pkg.go.dev/github.com/riverqueue/river#QueueListParams)
```

NewQueueListParams creates a new QueueListParams to return available queues sorted by time in ascending order, returning 100 jobs at most. 
####  func (*QueueListParams) [First](https://github.com/riverqueue/river/blob/v0.39.0/queue_list_params.go#L30) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueListParams.First "Go to QueueListParams.First") added in v0.5.0

```
func (p *QueueListParams[](https://pkg.go.dev/github.com/riverqueue/river#QueueListParams)) First(count int[](https://pkg.go.dev/builtin#int)) *QueueListParams[](https://pkg.go.dev/github.com/riverqueue/river#QueueListParams)
```

First returns an updated filter set that will only return the first count queues. 
Count must be between 1 and 10000, inclusive, or this will panic. 
####  type [QueueListResult](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2575) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueListResult "Go to QueueListResult") added in v0.5.0

```
type QueueListResult struct {
	// Queues is a slice of queues returned as part of the list operation.
	Queues []*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).Queue[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#Queue)
}
```

QueueListResult is the result of a job list operation. It contains a list of jobs and leaves room for future cursor functionality. 
####  type [QueueNotFoundError](https://github.com/riverqueue/river/blob/v0.39.0/error.go#L64) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueNotFoundError "Go to QueueNotFoundError") added in v0.36.0

```
type QueueNotFoundError struct {
	Name string[](https://pkg.go.dev/builtin#string)
}
```

QueueNotFoundError is returned when attempting to remove a queue that does not exist on the Client. 
####  func (*QueueNotFoundError) [Error](https://github.com/riverqueue/river/blob/v0.39.0/error.go#L68) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueNotFoundError.Error "Go to QueueNotFoundError.Error") added in v0.36.0

```
func (e *QueueNotFoundError[](https://pkg.go.dev/github.com/riverqueue/river#QueueNotFoundError)) Error() string[](https://pkg.go.dev/builtin#string)
```

####  func (*QueueNotFoundError) [Is](https://github.com/riverqueue/river/blob/v0.39.0/error.go#L72) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueNotFoundError.Is "Go to QueueNotFoundError.Is") added in v0.36.0

```
func (e *QueueNotFoundError[](https://pkg.go.dev/github.com/riverqueue/river#QueueNotFoundError)) Is(target error[](https://pkg.go.dev/builtin#error)) bool[](https://pkg.go.dev/builtin#bool)
```

####  type [QueuePauseOpts](https://github.com/riverqueue/river/blob/v0.39.0/queue_pause_opts.go#L4) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueuePauseOpts "Go to QueuePauseOpts") added in v0.5.0

```
type QueuePauseOpts struct{}
```

QueuePauseOpts are optional settings for pausing or resuming a queue. 
####  type [QueueUpdateParams](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L2775) [¶](https://pkg.go.dev/github.com/riverqueue/river#QueueUpdateParams "Go to QueueUpdateParams") added in v0.20.0

```
type QueueUpdateParams struct {
	// Metadata is the new metadata for the queue. If nil or empty, the metadata
	// will not be changed.
	Metadata []byte[](https://pkg.go.dev/builtin#byte)
}
```

QueueUpdateParams are the parameters for a QueueUpdate operation. 
####  type [SortOrder](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go#L121) [¶](https://pkg.go.dev/github.com/riverqueue/river#SortOrder "Go to SortOrder") added in v0.0.17

```
type SortOrder int[](https://pkg.go.dev/builtin#int)
```

SortOrder specifies the direction of a sort. 

```
const (
	// SortOrderAsc specifies that the sort should in ascending order.
	SortOrderAsc SortOrder[](https://pkg.go.dev/github.com/riverqueue/river#SortOrder) = iota[](https://pkg.go.dev/builtin#iota)

	// SortOrderDesc specifies that the sort should in descending order.
	SortOrderDesc
)
```

####  type [StepOpts](https://github.com/riverqueue/river/blob/v0.39.0/resumable.go#L44) [¶](https://pkg.go.dev/github.com/riverqueue/river#StepOpts "Go to StepOpts") added in v0.37.0

```
type StepOpts struct{}
```

StepOpts are options for ResumableStep and ResumableStepCursor. There are currently no available options, but this space is reserved for future use. 
####  type [SubscribeConfig](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L1326) [¶](https://pkg.go.dev/github.com/riverqueue/river#SubscribeConfig "Go to SubscribeConfig") added in v0.1.0

```
type SubscribeConfig struct {
	// ChanSize is the size of the buffered channel that will be created for the
	// subscription. Incoming events that overall this number because a listener
	// isn't reading from the channel in a timely manner will be dropped.
	//
	// Defaults to 1000.
	ChanSize int[](https://pkg.go.dev/builtin#int)

	// Kinds are the kinds of events that the subscription will receive.
	// Requiring that kinds are specified explicitly allows for forward
	// compatibility in case new kinds of events are added in future versions.
	// If new event kinds are added, callers will have to explicitly add them to
	// their requested list and ensure they can be handled correctly.
	Kinds []EventKind[](https://pkg.go.dev/github.com/riverqueue/river#EventKind)
}
```

SubscribeConfig is more thorough subscription configuration used for Client.SubscribeConfig. 
####  type [TestConfig](https://github.com/riverqueue/river/blob/v0.39.0/client.go#L71) [¶](https://pkg.go.dev/github.com/riverqueue/river#TestConfig "Go to TestConfig") added in v0.17.0

```
type TestConfig struct {
	// DisableUniqueEnforcement disables the application of unique job
	// constraints. This is useful for testing scenarios when testing a worker
	// that typically uses uniqueness, but where enforcing uniqueness would cause
	// conflicts with parallel test execution.
	//
	// The [rivertest.Worker] type automatically disables uniqueness enforcement
	// when creating jobs.
	DisableUniqueEnforcement bool[](https://pkg.go.dev/builtin#bool)

	// Time is a time generator to make time stubbable in tests.
	Time rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).TimeGenerator[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#TimeGenerator)
}
```

TestConfig contains configuration specific to test environments. 
####  type [UniqueOpts](https://github.com/riverqueue/river/blob/v0.39.0/insert_opts.go#L102) [¶](https://pkg.go.dev/github.com/riverqueue/river#UniqueOpts "Go to UniqueOpts")

```
type UniqueOpts struct {
	// ByArgs indicates that uniqueness should be enforced for any specific
	// instance of encoded args for a job.
	//
	// Default is false, meaning that as long as any other unique property is
	// enabled, uniqueness will be enforced for a kind regardless of input args.
	//
	// When set to true, the entire encoded args field will be included in the
	// uniqueness hash, which requires care to ensure that no irrelevant args are
	// factored into the uniqueness check. It is also possible to use a subset of
	// the args by indicating on the `JobArgs` struct which fields should be
	// included in the uniqueness check using struct tags:
	//
	// 	type MyJobArgs struct {
	// 		CustomerID string `json:"customer_id" river:"unique"`
	// 		TraceID    string `json:"trace_id"
	// 	}
	//
	// In this example, only the encoded `customer_id` key will be included in the
	// uniqueness check and the `trace_id` key will be ignored.
	//
	// All keys are sorted alphabetically before hashing to ensure consistent
	// results.
	//
	// River recurses into embedded structs and fields with struct values and
	// looks for `river:"unique"` annotations on them as well:
	//
	// 	type MyJobArgs struct {
	// 		Customer *Customer `json:"customer"`
	// 		TraceID  string    `json:"trace_id"
	// 	}
	//
	// 	type Customer struct {
	// 		ID string `json:"id" river:"unique"`
	// 	}
	//
	// In this example, the `id` value inside a `customer` subboject is used in
	// the uniqueness check. It'd be the same story if Customer was embedded on
	// MyJobArgs instead:
	//
	// 	type MyJobArgs struct {
	// 		Customer
	// 		TraceID string `json:"trace_id"
	// 	}
	//
	// If the struct field itself has a `river:"unique"` annotation, but none on
	// any fields in the substruct, then the entire JSON encoded value of the
	// struct is used as a unique value:
	//
	// 	type MyJobArgs struct {
	// 		Customer *Customer `json:"customer" river:"unique"`
	// 		TraceID  string    `json:"trace_id"
	// 	}
	//
	// 	type Customer struct {
	// 		ID string `json:"id"`
	// 	}
	ByArgs bool[](https://pkg.go.dev/builtin#bool)

	// ByPeriod defines uniqueness within a given period. On an insert time is
	// rounded down to the nearest multiple of the given period, and a job is
	// only inserted if there isn't an existing job that will run between then
	// and the next multiple of the period.
	//
	// Default is no unique period, meaning that as long as any other unique
	// property is enabled, uniqueness will be enforced across all jobs of the
	// kind in the database, regardless of when they were scheduled.
	ByPeriod time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// ByQueue indicates that uniqueness should be enforced within each queue.
	//
	// Default is false, meaning that as long as any other unique property is
	// enabled, uniqueness will be enforced for a kind across all queues.
	ByQueue bool[](https://pkg.go.dev/builtin#bool)

	// ByState indicates that uniqueness should be enforced across any of the
	// states in the given set. Unlike other unique options, ByState gets a
	// default when it's not set for user convenience. The default is equivalent
	// to:
	//
	// 	ByState: []rivertype.JobState{rivertype.JobStateAvailable, rivertype.JobStateCompleted, rivertype.JobStatePending, rivertype.JobStateRunning, rivertype.JobStateRetryable, rivertype.JobStateScheduled}
	//
	// Or more succinctly:
	//
	// 	ByState: rivertype.UniqueOptsByStateDefault()
	//
	// With this setting, any jobs of the same kind that have been completed or
	// discarded, but not yet cleaned out by the system, will still prevent a
	// duplicate unique job from being inserted. For example, with the default
	// states, if a unique job is actively `running`, a duplicate cannot be
	// inserted. Likewise, if a unique job has `completed`, you still can't
	// insert a duplicate, at least not until the job cleaner maintenance process
	// eventually removes the completed job from the `river_job` table.
	//
	// The list may be safely customized to _add_ additional states (`cancelled`
	// or `discarded`), though only `retryable` may be safely _removed_ from the
	// list.
	//
	// The following states must be included in ByState if set:
	//
	//   - rivertype.JobStateAvailable
	//   - rivertype.JobStatePending
	//   - rivertype.JobStateRunning
	//   - rivertype.JobStateScheduled
	//
	// These states being required is an implementation detail, but not
	// requiring them would put River in a difficult position when moving jobs
	// between common parts of the state machine and finding a unique conflict
	// already there. Resolving this isn't completely intractable, but'll
	// require some in-depth thinking and designing around every possible edge.
	ByState []rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobState[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobState)

	// ExcludeKind indicates that the job kind should not be included in the
	// uniqueness check. This is useful when you want to enforce uniqueness
	// across all jobs regardless of kind.
	ExcludeKind bool[](https://pkg.go.dev/builtin#bool)
}
```

UniqueOpts contains parameters for uniqueness for a job. 
When the options struct is uninitialized (its zero value) no uniqueness at is enforced. As each property is initialized, it's added as a dimension on the uniqueness matrix. When any property has a non-zero value specified, the job's kind automatically counts toward uniqueness, but can be excluded by setting ExcludeKind to true. 
So for example, if only ByQueue is on, then for the given job kind, only a single instance is allowed in any given queue, regardless of other properties on the job. If both ByArgs and ByQueue are on, then for the given job kind, a single instance is allowed for each combination of args and queues. If either args or queue is changed on a new job, it's allowed to be inserted as a new job. 
Uniqueness relies on a hash of the job kind and any unique properties along with a database unique constraint. See the note on ByState for more details including about the fallback to a deprecated advisory lock method. 
####  type [UnknownJobKindError](https://github.com/riverqueue/river/blob/v0.39.0/error.go#L79) [¶](https://pkg.go.dev/github.com/riverqueue/river#UnknownJobKindError "Go to UnknownJobKindError")

```
type UnknownJobKindError = rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).UnknownJobKindError[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#UnknownJobKindError)
```

UnknownJobKindError is returned when a Client fetches and attempts to work a job that has not been registered on the Client's Workers bundle (using AddWorker). 
####  type [Worker](https://github.com/riverqueue/river/blob/v0.39.0/worker.go#L39) [¶](https://pkg.go.dev/github.com/riverqueue/river#Worker "Go to Worker")

```
type Worker[T JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs)] interface {
	// Middleware returns the type-specific middleware for this job.
	Middleware(job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow)) []rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).WorkerMiddleware[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#WorkerMiddleware)

	// NextRetry calculates when the next retry for a failed job should take
	// place given when it was last attempted and its number of attempts, or any
	// other of the job's properties a user-configured retry policy might want
	// to consider.
	//
	// Note that this method on a worker overrides any client-level retry policy.
	// To use the client-level retry policy, return an empty `time.Time{}` or
	// include WorkerDefaults to do this for you.
	NextRetry(job *Job[](https://pkg.go.dev/github.com/riverqueue/river#Job)[T]) time[](https://pkg.go.dev/time).Time[](https://pkg.go.dev/time#Time)

	// Timeout is the maximum amount of time the job is allowed to run before
	// its context is cancelled. A timeout of zero (the default) means the job
	// will inherit the Client-level timeout. A timeout of -1 means the job's
	// context will never time out.
	Timeout(job *Job[](https://pkg.go.dev/github.com/riverqueue/river#Job)[T]) time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)

	// Work performs the job and returns an error if the job failed. The context
	// will be configured with a timeout according to the worker settings and may
	// be cancelled for other reasons.
	//
	// If no error is returned, the job is assumed to have succeeded and will be
	// marked completed.
	//
	// It is important for any worker to respect context cancellation to enable
	// the client to respond to shutdown requests. In particular, workers that
	// wait on channels, timers, or network operations should prefer a `select`
	// that also watches `ctx.Done()`. There is no way to cancel a running job
	// that does not respect context cancellation, other than terminating the
	// process.
	//
	// A worker that ignores cancellation may continue running even after the
	// client has timed out the job or the job rescuer has moved it out of
	// running.
	Work(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), job *Job[](https://pkg.go.dev/github.com/riverqueue/river#Job)[T]) error[](https://pkg.go.dev/builtin#error)
}
```

Worker is an interface that can perform a job with args of type T. A typical implementation will be a JSON-serializable `JobArgs` struct that implements `Kind()`, along with a Worker that embeds WorkerDefaults and implements `Work()`. Workers may optionally override other methods to provide job-specific configuration for all jobs of that type: 

```
type SleepArgs struct {
	Duration time.Duration `json:"duration"`
}

func (SleepArgs) Kind() string { return "sleep" }

type SleepWorker struct {
	WorkerDefaults[SleepArgs]
}

func (w *SleepWorker) Work(ctx context.Context, job *Job[SleepArgs]) error {
	select {
	case <-ctx.Done():
		return ctx.Err()
	case <-time.After(job.Args.Duration):
		return nil
	}
}

```

In addition to fulfilling the Worker interface, workers must be registered with the client using the AddWorker function. 
####  func [WorkFunc](https://github.com/riverqueue/river/blob/v0.39.0/worker.go#L221) [¶](https://pkg.go.dev/github.com/riverqueue/river#WorkFunc "Go to WorkFunc")

```
func WorkFunc[T JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs)](f func(context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), *Job[](https://pkg.go.dev/github.com/riverqueue/river#Job)[T]) error[](https://pkg.go.dev/builtin#error)) Worker[](https://pkg.go.dev/github.com/riverqueue/river#Worker)[T]
```

WorkFunc wraps a function to implement the Worker interface. A job args struct implementing JobArgs will still be required to specify a Kind. 
For example: 

```
river.AddWorker(workers, river.WorkFunc(func(ctx context.Context, job *river.Job[WorkFuncArgs]) error {
	fmt.Printf("Message: %s", job.Args.Message)
	return nil
}))

```

####  type [WorkerDefaults](https://github.com/riverqueue/river/blob/v0.39.0/worker.go#L81) [¶](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults "Go to WorkerDefaults")

```
type WorkerDefaults[T JobArgs[](https://pkg.go.dev/github.com/riverqueue/river#JobArgs)] struct{}
```

WorkerDefaults is an empty struct that can be embedded in your worker struct to make it fulfill the Worker interface with default values. 
####  func (WorkerDefaults[T]) [Middleware](https://github.com/riverqueue/river/blob/v0.39.0/worker.go#L83) [¶](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults.Middleware "Go to WorkerDefaults.Middleware") added in v0.13.0

```
func (w WorkerDefaults[](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults)[T]) Middleware(*rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow)) []rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).WorkerMiddleware[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#WorkerMiddleware)
```

####  func (WorkerDefaults[T]) [NextRetry](https://github.com/riverqueue/river/blob/v0.39.0/worker.go#L88) [¶](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults.NextRetry "Go to WorkerDefaults.NextRetry")

```
func (w WorkerDefaults[](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults)[T]) NextRetry(*Job[](https://pkg.go.dev/github.com/riverqueue/river#Job)[T]) time[](https://pkg.go.dev/time).Time[](https://pkg.go.dev/time#Time)
```

NextRetry returns an empty time.Time{} to avoid setting any job or Worker-specific overrides on the next retry time. This means that the Client-level retry policy schedule will be used instead. 
####  func (WorkerDefaults[T]) [Timeout](https://github.com/riverqueue/river/blob/v0.39.0/worker.go#L92) [¶](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults.Timeout "Go to WorkerDefaults.Timeout")

```
func (w WorkerDefaults[](https://pkg.go.dev/github.com/riverqueue/river#WorkerDefaults)[T]) Timeout(*Job[](https://pkg.go.dev/github.com/riverqueue/river#Job)[T]) time[](https://pkg.go.dev/time).Duration[](https://pkg.go.dev/time#Duration)
```

Timeout returns the job-specific timeout. Override this method to set a job-specific timeout, otherwise the Client-level timeout will be applied. 
####  type [WorkerMiddlewareDefaults](https://github.com/riverqueue/river/blob/v0.39.0/middleware_defaults.go#L44) deprecated added in v0.13.0

```
type WorkerMiddlewareDefaults struct{ MiddlewareDefaults[](https://pkg.go.dev/github.com/riverqueue/river#MiddlewareDefaults) }
```

WorkerInsertMiddlewareDefaults is an embeddable struct that provides default implementations for the rivertype.WorkerMiddleware. Use of this struct is recommended in case rivertype.WorkerMiddleware is expanded in the future so that existing code isn't unexpectedly broken during an upgrade. 
Deprecated: Prefer embedding the more general MiddlewareDefaults instead. 
####  func (*WorkerMiddlewareDefaults) [Work](https://github.com/riverqueue/river/blob/v0.39.0/middleware_defaults.go#L46) [¶](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareDefaults.Work "Go to WorkerMiddlewareDefaults.Work") added in v0.13.0

```
func (d *WorkerMiddlewareDefaults[](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareDefaults)) Work(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), doInner func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context)) error[](https://pkg.go.dev/builtin#error)) error[](https://pkg.go.dev/builtin#error)
```

####  type [WorkerMiddlewareFunc](https://github.com/riverqueue/river/blob/v0.39.0/middleware_defaults.go#L52) [¶](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareFunc "Go to WorkerMiddlewareFunc") added in v0.21.0

```
type WorkerMiddlewareFunc func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), doInner func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context)) error[](https://pkg.go.dev/builtin#error)) error[](https://pkg.go.dev/builtin#error)
```

WorkerMiddlewareFunc is a convenience helper for implementing rivertype.WorkerMiddleware using a simple function instead of a struct. 
####  func (WorkerMiddlewareFunc) [IsMiddleware](https://github.com/riverqueue/river/blob/v0.39.0/middleware_defaults.go#L58) [¶](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareFunc.IsMiddleware "Go to WorkerMiddlewareFunc.IsMiddleware") added in v0.21.0

```
func (f WorkerMiddlewareFunc[](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareFunc)) IsMiddleware() bool[](https://pkg.go.dev/builtin#bool)
```

####  func (WorkerMiddlewareFunc) [Work](https://github.com/riverqueue/river/blob/v0.39.0/middleware_defaults.go#L54) [¶](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareFunc.Work "Go to WorkerMiddlewareFunc.Work") added in v0.21.0

```
func (f WorkerMiddlewareFunc[](https://pkg.go.dev/github.com/riverqueue/river#WorkerMiddlewareFunc)) Work(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context), job *rivertype[](https://pkg.go.dev/github.com/riverqueue/river/rivertype).JobRow[](https://pkg.go.dev/github.com/riverqueue/river/rivertype#JobRow), doInner func(ctx context[](https://pkg.go.dev/context).Context[](https://pkg.go.dev/context#Context)) error[](https://pkg.go.dev/builtin#error)) error[](https://pkg.go.dev/builtin#error)
```

####  type [Workers](https://github.com/riverqueue/river/blob/v0.39.0/worker.go#L143) [¶](https://pkg.go.dev/github.com/riverqueue/river#Workers "Go to Workers")

```
type Workers struct {
	// contains filtered or unexported fields
}
```

Workers is a list of available job workers. A Worker must be registered for each type of Job to be handled. 
Use the top-level AddWorker function combined with a Workers to register a worker. 
####  func [NewWorkers](https://github.com/riverqueue/river/blob/v0.39.0/worker.go#L158) [¶](https://pkg.go.dev/github.com/riverqueue/river#NewWorkers "Go to NewWorkers")

```
func NewWorkers() *Workers[](https://pkg.go.dev/github.com/riverqueue/river#Workers)
```

NewWorkers initializes a new registry of available job workers. 
Use the top-level AddWorker function combined with a Workers registry to register each available worker. 
##  ![](https://pkg.go.dev/static/shared/icon/insert_drive_file_gm_grey_24dp.svg) Source Files [¶](https://pkg.go.dev/github.com/riverqueue/river#section-sourcefiles "Go to Source Files")
[View all Source files](https://github.com/riverqueue/river/tree/v0.39.0)
  * [client.go](https://github.com/riverqueue/river/blob/v0.39.0/client.go "client.go")
  * [client_context.go](https://github.com/riverqueue/river/blob/v0.39.0/client_context.go "client_context.go")
  * [delete_many_params.go](https://github.com/riverqueue/river/blob/v0.39.0/delete_many_params.go "delete_many_params.go")
  * [doc.go](https://github.com/riverqueue/river/blob/v0.39.0/doc.go "doc.go")
  * [error.go](https://github.com/riverqueue/river/blob/v0.39.0/error.go "error.go")
  * [error_handler.go](https://github.com/riverqueue/river/blob/v0.39.0/error_handler.go "error_handler.go")
  * [event.go](https://github.com/riverqueue/river/blob/v0.39.0/event.go "event.go")
  * [hook_defaults_funcs.go](https://github.com/riverqueue/river/blob/v0.39.0/hook_defaults_funcs.go "hook_defaults_funcs.go")
  * [insert_opts.go](https://github.com/riverqueue/river/blob/v0.39.0/insert_opts.go "insert_opts.go")
  * [job.go](https://github.com/riverqueue/river/blob/v0.39.0/job.go "job.go")
  * [job_complete_tx.go](https://github.com/riverqueue/river/blob/v0.39.0/job_complete_tx.go "job_complete_tx.go")
  * [job_list_params.go](https://github.com/riverqueue/river/blob/v0.39.0/job_list_params.go "job_list_params.go")
  * [metadata.go](https://github.com/riverqueue/river/blob/v0.39.0/metadata.go "metadata.go")
  * [middleware_defaults.go](https://github.com/riverqueue/river/blob/v0.39.0/middleware_defaults.go "middleware_defaults.go")
  * [periodic_job.go](https://github.com/riverqueue/river/blob/v0.39.0/periodic_job.go "periodic_job.go")
  * [plugin.go](https://github.com/riverqueue/river/blob/v0.39.0/plugin.go "plugin.go")
  * [producer.go](https://github.com/riverqueue/river/blob/v0.39.0/producer.go "producer.go")
  * [queue_list_params.go](https://github.com/riverqueue/river/blob/v0.39.0/queue_list_params.go "queue_list_params.go")
  * [queue_pause_opts.go](https://github.com/riverqueue/river/blob/v0.39.0/queue_pause_opts.go "queue_pause_opts.go")
  * [recorded_output.go](https://github.com/riverqueue/river/blob/v0.39.0/recorded_output.go "recorded_output.go")
  * [resumable.go](https://github.com/riverqueue/river/blob/v0.39.0/resumable.go "resumable.go")
  * [resumable_step_tx.go](https://github.com/riverqueue/river/blob/v0.39.0/resumable_step_tx.go "resumable_step_tx.go")
  * [retry_policy.go](https://github.com/riverqueue/river/blob/v0.39.0/retry_policy.go "retry_policy.go")
  * [subscription_manager.go](https://github.com/riverqueue/river/blob/v0.39.0/subscription_manager.go "subscription_manager.go")
  * [work_unit_wrapper.go](https://github.com/riverqueue/river/blob/v0.39.0/work_unit_wrapper.go "work_unit_wrapper.go")
  * [worker.go](https://github.com/riverqueue/river/blob/v0.39.0/worker.go "worker.go")


##  ![](https://pkg.go.dev/static/shared/icon/folder_gm_grey_24dp.svg) Directories [¶](https://pkg.go.dev/github.com/riverqueue/river#section-directories "Go to Directories")
Show internal  Expand all  
| Path  | Synopsis  |  
| --- | --- |  
|  ![](https://pkg.go.dev/static/shared/icon/arrow_right_gm_grey_24dp.svg) cmd  |   |  
|  [river](https://pkg.go.dev/github.com/riverqueue/river/cmd/river) module  |   |  
|  ![](https://pkg.go.dev/static/shared/icon/arrow_right_gm_grey_24dp.svg) internal  |   |  
|  [dblist](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/dblist)  |   |  
|  [dbunique](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/dbunique)  |   |  
|  [execution](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/execution)  |   |  
|  [hooklookup](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/hooklookup)  |   |  
|  [jobcompleter](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/jobcompleter)  |   |  
|  [jobexecutor](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/jobexecutor)  |   |  
|  [jobstats](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/jobstats)  |   |  
|  [leadership](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/leadership) Package leadership implements leader election for River clients sharing a database schema.  | Package leadership implements leader election for River clients sharing a database schema.  |  
|  [maintenance](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/maintenance)  |   |  
|  [middlewarelookup](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/middlewarelookup)  |   |  
|  [notifier](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/notifier)  |   |  
|  [notifylimiter](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/notifylimiter)  |   |  
|  [rivercommon](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/rivercommon)  |   |  
|  [riverinternaltest](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/riverinternaltest) Package riverinternaltest contains shared testing utilities for tests throughout the rest of the project.  | Package riverinternaltest contains shared testing utilities for tests throughout the rest of the project.  |  
|  [riverinternaltest/retrypolicytest](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/riverinternaltest/retrypolicytest)  |   |  
|  [riverinternaltest/sharedtx](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/riverinternaltest/sharedtx)  |   |  
|  [rivermiddleware](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/rivermiddleware)  |   |  
|  [util/chanutil](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/util/chanutil)  |   |  
|  [workunit](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/internal/workunit)  |   |  
|  [riverdbtest](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/riverdbtest) Package riverdbtest contains testing infrastructure for the River project itself that creates isolated schemas suitable for use within a single case.  | Package riverdbtest contains testing infrastructure for the River project itself that creates isolated schemas suitable for use within a single case.  |  
|  ![](https://pkg.go.dev/static/shared/icon/arrow_right_gm_grey_24dp.svg) [riverdriver](https://pkg.go.dev/github.com/riverqueue/river/riverdriver) module  |   |  
|  [riverdatabasesql](https://pkg.go.dev/github.com/riverqueue/river/riverdriver/riverdatabasesql) module  |   |  
|  [riverdrivertest](https://pkg.go.dev/github.com/riverqueue/river/riverdriver/riverdrivertest) module  |   |  
|  [riverpgxv5](https://pkg.go.dev/github.com/riverqueue/river/riverdriver/riverpgxv5) module  |   |  
|  [riversqlite](https://pkg.go.dev/github.com/riverqueue/river/riverdriver/riversqlite) module  |   |  
|  [riverlog](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/riverlog) Package riverlog provides a context logging middleware for workers that collates output and stores it to job records.  | Package riverlog provides a context logging middleware for workers that collates output and stores it to job records.  |  
|  [rivermigrate](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/rivermigrate) Package rivermigrate provides a Go API for running migrations as alternative to migrating via the bundled CLI.  | Package rivermigrate provides a Go API for running migrations as alternative to migrating via the bundled CLI.  |  
|  [rivershared](https://pkg.go.dev/github.com/riverqueue/river/rivershared) module  |   |  
|  [rivertest](https://pkg.go.dev/github.com/riverqueue/river@v0.39.0/rivertest) Package rivertest contains test assertions that can be used in a project's tests to verify that certain actions occurred from the main river package.  | Package rivertest contains test assertions that can be used in a project's tests to verify that certain actions occurred from the main river package.  |  
|  [rivertype](https://pkg.go.dev/github.com/riverqueue/river/rivertype) module  |   |  
Click to show internal directories. 
Click to hide internal directories. 
[ Why Go ](https://go.dev/solutions) [ Use Cases ](https://go.dev/solutions#use-cases) [ Case Studies ](https://go.dev/solutions#case-studies)
[ Get Started ](https://learn.go.dev/) [ Playground ](https://play.golang.org) [ Tour ](https://tour.golang.org) [ Stack Overflow ](https://stackoverflow.com/questions/tagged/go?tab=Newest) [ API ](https://pkg.go.dev/api) [ Help ](https://go.dev/help)
[ Packages ](https://pkg.go.dev) [ Standard Library ](https://pkg.go.dev/std) [ Sub-repositories ](https://pkg.go.dev/golang.org/x) [ About Go Packages ](https://pkg.go.dev/about)
[ About ](https://go.dev/project) [Download](https://go.dev/dl/) [Blog](https://go.dev/blog) [ Issue Tracker ](https://github.com/golang/go/issues) [ Release Notes ](https://go.dev/doc/devel/release.html) [ Brand Guidelines ](https://go.dev/brand) [ Code of Conduct ](https://go.dev/conduct)
[ Connect ](https://www.twitter.com/golang) [ Twitter ](https://www.twitter.com/golang) [GitHub](https://github.com/golang) [ Slack ](https://invite.slack.golangbridge.org/) [ r/golang ](https://reddit.com/r/golang) [ Meetup ](https://www.meetup.com/pro/go) [ Golang Weekly ](https://golangweekly.com/)
![Gopher in flight goggles](https://pkg.go.dev/static/shared/gopher/pilot-bust-1431x901.svg)
  * [Copyright](https://go.dev/copyright)
  * [Terms of Service](https://go.dev/tos)
  * [ Privacy Policy ](http://www.google.com/intl/en/policies/privacy/)
  * [ Report an Issue ](https://go.dev/s/pkgsite-feedback)
  * ![System theme](https://pkg.go.dev/static/shared/icon/brightness_6_gm_grey_24dp.svg) ![Dark theme](https://pkg.go.dev/static/shared/icon/brightness_2_gm_grey_24dp.svg) ![Light theme](https://pkg.go.dev/static/shared/icon/light_mode_gm_grey_24dp.svg)
Theme Toggle 
  * ![](https://pkg.go.dev/static/shared/icon/keyboard_grey_24dp.svg)
Shortcuts Modal 

[ ![Google logo](https://pkg.go.dev/static/shared/logo/google-white.svg) ](https://google.com)
## Jump to
![](https://pkg.go.dev/static/shared/icon/close_gm_grey_24dp.svg)
Close
## Keyboard shortcuts
![](https://pkg.go.dev/static/shared/icon/close_gm_grey_24dp.svg)  
|  **?**  |  : This menu  |  
| --- | --- |  
|  **/**  |  : Search site  |  
|  **f** or **F**  |  : Jump to  |  
|  **y** or **Y**  |  : Canonical URL  |  
Close
go.dev uses cookies from Google to deliver and enhance the quality of its services and to analyze traffic. [Learn more.](https://policies.google.com/technologies/cookies)
Okay

