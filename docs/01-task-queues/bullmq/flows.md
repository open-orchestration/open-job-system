[![](https://docs.bullmq.io/~gitbook/image?url=https%3A%2F%2F1340146492-files.gitbook.io%2F%7E%2Ffiles%2Fv0%2Fb%2Fgitbook-x-prod.appspot.com%2Fo%2Fspaces%252F-LUuDmt_xXMfG66Rn1GA%252Ficon%252FHOq80FSJicAlE4bVptC9%252Fbull.png%3Falt%3Dmedia%26token%3D10a2ba71-db1f-4d5c-8787-3dbedc8dd3ce&width=32&dpr=3&quality=100&sign=929d1d52&sv=2)![](https://docs.bullmq.io/~gitbook/image?url=https%3A%2F%2F1340146492-files.gitbook.io%2F%7E%2Ffiles%2Fv0%2Fb%2Fgitbook-x-prod.appspot.com%2Fo%2Fspaces%252F-LUuDmt_xXMfG66Rn1GA%252Ficon%252FHOq80FSJicAlE4bVptC9%252Fbull.png%3Falt%3Dmedia%26token%3D10a2ba71-db1f-4d5c-8787-3dbedc8dd3ce&width=32&dpr=3&quality=100&sign=929d1d52&sv=2)\\
BullMQ](https://docs.bullmq.io/)

`⌘Ctrl`  `k`

[![](https://docs.bullmq.io/~gitbook/image?url=https%3A%2F%2F1340146492-files.gitbook.io%2F%7E%2Ffiles%2Fv0%2Fb%2Fgitbook-x-prod.appspot.com%2Fo%2Fspaces%252F-LUuDmt_xXMfG66Rn1GA%252Ficon%252FHOq80FSJicAlE4bVptC9%252Fbull.png%3Falt%3Dmedia%26token%3D10a2ba71-db1f-4d5c-8787-3dbedc8dd3ce&width=32&dpr=3&quality=100&sign=929d1d52&sv=2)![](https://docs.bullmq.io/~gitbook/image?url=https%3A%2F%2F1340146492-files.gitbook.io%2F%7E%2Ffiles%2Fv0%2Fb%2Fgitbook-x-prod.appspot.com%2Fo%2Fspaces%252F-LUuDmt_xXMfG66Rn1GA%252Ficon%252FHOq80FSJicAlE4bVptC9%252Fbull.png%3Falt%3Dmedia%26token%3D10a2ba71-db1f-4d5c-8787-3dbedc8dd3ce&width=32&dpr=3&quality=100&sign=929d1d52&sv=2)\\
BullMQ](https://docs.bullmq.io/)

- [What is BullMQ](https://docs.bullmq.io/)
- [Quick Start](https://docs.bullmq.io/readme-1)
- [API Reference](https://api.docs.bullmq.io/)
- [Changelogs](https://docs.bullmq.io/changelog)
- Guide





  - [Introduction](https://docs.bullmq.io/guide/introduction)
  - [Connections](https://docs.bullmq.io/guide/connections)
  - [Queues](https://docs.bullmq.io/guide/queues)
  - [Workers](https://docs.bullmq.io/guide/workers)
  - [Jobs](https://docs.bullmq.io/guide/jobs)
  - [Job Schedulers](https://docs.bullmq.io/guide/job-schedulers)
  - [Flows](https://docs.bullmq.io/guide/flows)


    - [Adding flows in bulk](https://docs.bullmq.io/guide/flows/adding-bulks)
    - [Get Flow Tree](https://docs.bullmq.io/guide/flows/get-flow-tree)
    - [Fail Parent](https://docs.bullmq.io/guide/flows/fail-parent)
    - [Continue Parent](https://docs.bullmq.io/guide/flows/continue-parent)
    - [Remove Dependency](https://docs.bullmq.io/guide/flows/remove-dependency)
    - [Ignore Dependency](https://docs.bullmq.io/guide/flows/ignore-dependency)
    - [Remove Child Dependency](https://docs.bullmq.io/guide/flows/remove-child-dependency)

  - [Metrics](https://docs.bullmq.io/guide/metrics)
  - [Rate limiting](https://docs.bullmq.io/guide/rate-limiting)
  - [Parallelism and Concurrency](https://docs.bullmq.io/guide/parallelism-and-concurrency)
  - [Retrying failing jobs](https://docs.bullmq.io/guide/retrying-failing-jobs)
  - [Returning job data](https://docs.bullmq.io/guide/returning-job-data)
  - [Events](https://docs.bullmq.io/guide/events)
  - [Telemetry](https://docs.bullmq.io/guide/telemetry)
  - [QueueScheduler](https://docs.bullmq.io/guide/queuescheduler)
  - [Redis™ Compatibility](https://docs.bullmq.io/guide/redis-tm-compatibility)
  - [Redis™ hosting](https://docs.bullmq.io/guide/redis-tm-hosting)
  - [Architecture](https://docs.bullmq.io/guide/architecture)
  - [NestJs](https://docs.bullmq.io/guide/nestjs)
  - [Going to production](https://docs.bullmq.io/guide/going-to-production)
  - [Migrations](https://docs.bullmq.io/guide/migration-to-newer-versions)
  - [Troubleshooting](https://docs.bullmq.io/guide/troubleshooting)

- Patterns





  - [Adding jobs in bulk across different queues](https://docs.bullmq.io/patterns/adding-bulks)
  - [Deduplication](https://docs.bullmq.io/patterns/deduplication)
  - [Manually processing jobs](https://docs.bullmq.io/patterns/manually-fetching-jobs)
  - [Named Processor](https://docs.bullmq.io/patterns/named-processor)
  - [Flows](https://docs.bullmq.io/patterns/flows)
  - [Idempotent jobs](https://docs.bullmq.io/patterns/idempotent-jobs)
  - [Throttle jobs](https://docs.bullmq.io/patterns/throttle-jobs)
  - [Manual retrying](https://docs.bullmq.io/patterns/manual-retrying)
  - [Process Step Jobs](https://docs.bullmq.io/patterns/process-step-jobs)
  - [Failing fast when Redis is down](https://docs.bullmq.io/patterns/failing-fast-when-redis-is-down)
  - [Stop retrying jobs](https://docs.bullmq.io/patterns/stop-retrying-jobs)
  - [Timeout jobs](https://docs.bullmq.io/patterns/timeout-jobs)
  - [Timeout for Sandboxed processors](https://docs.bullmq.io/patterns/timeout-for-sandboxed-processors)
  - [Redis Cluster](https://docs.bullmq.io/patterns/redis-cluster)

- BullMQ Pro





  - [Introduction](https://docs.bullmq.io/bullmq-pro/introduction)
  - [Install](https://docs.bullmq.io/bullmq-pro/install)
  - [Observables](https://docs.bullmq.io/bullmq-pro/observables)
  - [Groups](https://docs.bullmq.io/bullmq-pro/groups)
  - [Telemetry](https://docs.bullmq.io/bullmq-pro/telemetry)
  - [Batches](https://docs.bullmq.io/bullmq-pro/batches)
  - [NestJs](https://docs.bullmq.io/bullmq-pro/nestjs)
  - [API Reference](https://api.bullmq.pro/)
  - [Changelogs](https://docs.bullmq.io/bullmq-pro/changelog)
  - [New Releases](https://docs.bullmq.io/bullmq-pro/new-releases)
  - [Support](https://docs.bullmq.io/bullmq-pro/support)

- Bull





  - [Introduction](https://docs.bullmq.io/bull/introduction)
  - [Install](https://docs.bullmq.io/bull/install)
  - [Quick Guide](https://docs.bullmq.io/bull/quick-guide)
  - [Important Notes](https://docs.bullmq.io/bull/important-notes)
  - [Reference](https://github.com/OptimalBits/bull/blob/develop/REFERENCE.md)
  - [Patterns](https://docs.bullmq.io/bull/patterns)

- Python





  - [Introduction](https://docs.bullmq.io/python/introduction)
  - [Changelogs](https://docs.bullmq.io/python/changelog)

- Rust





  - [Introduction](https://docs.bullmq.io/rust/introduction)
  - [API Reference](https://docs.rs/bullmq)
  - [Changelogs](https://docs.bullmq.io/rust/changelog)

- Elixir





  - [Introduction](https://docs.bullmq.io/elixir/introduction)
  - [API Reference](https://hexdocs.pm/bullmq)
  - [Changelogs](https://docs.bullmq.io/elixir/changelog)

- PHP





  - [Introduction](https://docs.bullmq.io/php/introduction)
  - [Changelogs](https://docs.bullmq.io/php/changelog)

[Powered by GitBook](https://www.gitbook.com/?utm_source=content&utm_medium=trademark&utm_campaign=-LUuDmt_xXMfG66Rn1GA&utm_content=site_Wqm72) [Powered by GitBook](https://www.gitbook.com/?utm_source=content&utm_medium=trademark&utm_campaign=-LUuDmt_xXMfG66Rn1GA&utm_content=site_Wqm72)

On this page

- [Getters](https://docs.bullmq.io/guide/flows#getters)
- [Get Dependencies Count](https://docs.bullmq.io/guide/flows#get-dependencies-count)
- [Get Children Values](https://docs.bullmq.io/guide/flows#get-children-values)
- [Provide options](https://docs.bullmq.io/guide/flows#provide-options)
- [Jobs removal](https://docs.bullmq.io/guide/flows#jobs-removal)
- [Read more:](https://docs.bullmq.io/guide/flows#read-more)

Was this helpful?

For the complete documentation index, see [llms.txt](https://docs.bullmq.io/llms.txt). This page is also available as [Markdown](https://docs.bullmq.io/guide/flows.md).

Copy

On this page

1. [Guide](https://docs.bullmq.io/guide)

# Flows

BullMQ supports parent - child relationships between jobs. The basic idea is that a parent job will not be moved to the wait status (i.e. where it could be picked up by a worker) until all its children jobs have been processed successfully. Apart from that, a parent or a child job are no different from regular jobs.

This functionality enables the creation of flows where jobs are the node of trees of arbitrary depth.

Flows are added to a queue using the `FlowProducer` class.

In order to create "flows" you must use the [`FlowProducer`](https://api.docs.bullmq.io/classes/v5.FlowProducer.html) class. The [`add`](https://api.docs.bullmq.io/classes/v5.FlowProducer.html#add) method accepts an object with the following interface:

Copy

```
interface FlowJobBase<T> {
  name: string;
  queueName: string;
  data?: any;
  prefix?: string;
  opts?: Omit<T, 'debounce' | 'deduplication' | 'repeat'>;
  children?: FlowChildJob[];
}

type FlowChildJob = FlowJobBase<
  Omit<JobsOptions, 'debounce' | 'deduplication' | 'parent' | 'repeat'>
>;

type FlowJob = FlowJobBase<JobsOptions>;
```

So we can add a flow like this one:

TypeScript

Python

Copy

```
import { FlowProducer } from 'bullmq';

// A FlowProducer constructor takes an optional "connection"
// object otherwise it connects to a local redis instance.
const flowProducer = new FlowProducer();

const flow = await flowProducer.add({
  name: 'renovate-interior',
  queueName: 'renovate',
  children: [\
    { name: 'paint', data: { place: 'ceiling' }, queueName: 'steps' },\
    { name: 'paint', data: { place: 'walls' }, queueName: 'steps' },\
    { name: 'fix', data: { place: 'floor' }, queueName: 'steps' },\
  ],
});
```

Copy

```
from bullmq import FlowProducer

# A FlowProducer constructor takes an optional "connection"
# object otherwise it connects to a local redis instance.
flowProducer = FlowProducer()

flow = await flowProducer.add({
  "name": "renovate-interior",
  "queueName": "renovate",
  "children": [\
    { "name": "paint", "data": { "place": "ceiling" }, "queueName": "steps" },\
    { "name": "paint", "data": { "place": "walls" }, "queueName": "steps" },\
    { "name": "fix", "data": { "place": "floor" }, "queueName": "steps" },\
  ],
})
```

The above code will atomically add 4 jobs: one to the "renovate" queue, and 3 to the "steps" queue. When the 3 jobs in the "steps" queue are completed, the parent job in the "renovate" queue will be processed as a regular job.

The above call will return instances for all the jobs added to the queue.

Note that the parent queue does not need to be the same queue as the one used for the children.

If a jobId option is provided, make sure that it does not contain a colon **:** as this is considered a separator.

When the parent job is processed it is possible to access the results generated by its child jobs. For example, lets assume the following worker for the child jobs:

TypeScript

Python

Copy

```
import { Worker } from 'bullmq';

const stepsWorker = new Worker('steps', async job => {
  await performStep(job.data);

  if (job.name === 'paint') {
    return 2500;
  } else if (job.name === 'fix') {
    return 1750;
  }
});
```

Copy

```
from bullmq import Worker

async def process(job: Job, token: str):
  await performStep(job.data)
  if job.name == 'paint':
    return 2500
  elif job.name == 'fix':
    return 1750

stepsWorker = Worker("steps", process, {"connection": connection})
```

We can implement a parent worker that sums the costs of the children's jobs using the `getChildrenValues` method. This method returns an object with _job keys_ as keys and the _result of that given job_ as a value:

Copy

```
import { Worker } from 'bullmq';

const renovateWorker = new Worker('renovate', async job => {
  const childrenValues = await job.getChildrenValues();

  const totalCosts = Object.values(childrenValues).reduce(
    (prev, cur) => prev + cur,
    0,
  );

  await sendInvoice(totalCosts);
});
```

It is possible to add as deep job hierarchies as needed. See the following example where jobs are depending on each other, allowing serial execution of jobs:

TypeScript

Python

Copy

```
import { FlowProducer } from 'bullmq';
const flowProducer = new FlowProducer();

const queueName = 'assembly-line';
const chain = await flowProducer.add({
  name: 'car',
  data: { step: 'engine' },
  queueName,
  children: [\
    {\
      name: 'car',\
      data: { step: 'wheels' },\
      queueName,\
      children: [{ name: 'car', data: { step: 'chassis' }, queueName }],\
    },\
  ],
});
```

Copy

```
from bullmq import FlowProducer

flowProducer = FlowProducer()

queueName = 'assembly-line'
chain = await flowProducer.add({
  "name": "car",
  "data": { "step": "engine" },
  "queueName": queueName,
  "children": [\
    {\
      "name": "car",\
      "data": { "step": "wheels" },\
      "queueName": queueName,\
      "children": [{ "name": "car", "data": { "step": "chassis" }, "queueName": queueName }],\
    },\
  ],
})
```

In this case one job will be processed after the previous one has been completed.

The order of processing would be: `chassis`, `wheels` and finally `engine`.

## [Direct link to heading](https://docs.bullmq.io/guide/flows\#getters)    Getters

There are some special getters that can be used in order to get jobs related to a flow. First, we have a method in the `Job` class to get all the dependencies for a given job:

Copy

```
const dependencies = await job.getDependencies();
```

it will return all the **direct** **dependencies** (i.e. the children of a given job).

Or if you want to get specific types of children:

Copy

```
// cursors are used in pagination
const { processed, nextProcessedCursor } = await job.getDependencies({
  processed: {
    count: 5,
    cursor: 0,
  },
});

const { unprocessed, nextUnprocessedCursor } = await job.getDependencies({
  unprocessed: {
    count: 5,
    cursor: 0,
  },
});

const { failed, nextFailedCursor } = await job.getDependencies({
  failed: {
    count: 5,
    cursor: 0,
  },
});

const { ignored, nextIgnoredCursor } = await job.getDependencies({
  ignored: {
    count: 5,
    cursor: 0,
  },
});
```

The `Job` class also provides some other methods that we presented above

## [Direct link to heading](https://docs.bullmq.io/guide/flows\#get-dependencies-count)    Get Dependencies Count

To get all the different counts of children by type:

Copy

```
const { failed, ignored, processed, unprocessed } =
  await job.getDependenciesCount();
```

Or if you want to be specific:

Copy

```
const { failed } = await job.getDependenciesCount({
  failed: true,
});

const { ignored, processed } = await job.getDependenciesCount({
  ignored: true,
  processed: true,
});

const { unprocessed } = await job.getDependenciesCount({
  unprocessed: true,
});
```

## [Direct link to heading](https://docs.bullmq.io/guide/flows\#get-children-values)    Get Children Values

To get all the values produced by the children of a given job:

Copy

```
const values = await job.getChildrenValues();
```

Also, a new property is available in the `Job` class, `parentKey` _**,**_ with a fully qualified key for the job parent.

Finally, there is also a new state which a job can be in, "waiting-children", for parent jobs that have not yet had their children completed:

Copy

```
const state = await job.getState();
// state will be "waiting-children"
```

## [Direct link to heading](https://docs.bullmq.io/guide/flows\#provide-options)    Provide options

When adding a flow it is also possible to provide an extra object `queueOptions` object, in which you can add your specific options for every queue that is used in the flow. These options would affect each one of the jobs that are added to the flow using the `FlowProducer`.

Copy

```
import { FlowProducer } from 'bullmq';
const flowProducer = new FlowProducer();

const queueName = 'assembly-line';
const chain = await flowProducer.add(
  {
    name: 'car',
    data: { step: 'engine' },
    queueName,
    children: [\
      {\
        name: 'car',\
        data: { step: 'wheels' },\
        queueName,\
      },\
    ],
  },
  {
    queuesOptions: {
      [queueName]: {
        defaultJobOptions: {
          removeOnComplete: true,
        },
      },
    },
  },
);
```

Queue options are defined in the context of their instances. You should provide your configurations in the second parameter in order to avoid unexpected behaviors.

## [Direct link to heading](https://docs.bullmq.io/guide/flows\#jobs-removal)    Jobs removal

BullMQ also provides seamless removal functionality for jobs that are part of a flow.

When removing a job that is part of the flow there are several important considerations:

1. If a parent job is removed, all its children will also be removed.

2. If a child job is removed, its parent dependency to said child is also removed, and if the child was the last child in the dependencies list, the parent job will be completed.

3. Since a job can be both a parent and a child in a large flow, both 1 and 2 will occur if removing such a job.

4. If any of the jobs that would be removed happen to be locked, none of the jobs will be removed, and an exception will be thrown.


Apart from the considerations above, removing a job can simply be done by either using the `Job` or the `Queue` class:

Copy

```
await job.remove();
// or
await queue.remove(job.id);
```

## [Direct link to heading](https://docs.bullmq.io/guide/flows\#read-more)    Read more:

- 📋 [Divide large jobs using flows](https://blog.taskforce.sh/splitting-heavy-jobs-using-bullmq-flows/)

- 💡 [FlowProducer API Reference](https://api.docs.bullmq.io/classes/v5.FlowProducer.html)

- 💡 [Job API Reference](https://api.docs.bullmq.io/classes/v5.Job.html)

- 💡 [Get Children Values API Reference](https://api.docs.bullmq.io/classes/v5.Job.html#getchildrenvalues)

- 💡 [Get Dependencies API Reference](https://api.docs.bullmq.io/classes/v5.Job.html#getdependencies)

- 💡 [Get Dependencies Count API Reference](https://api.docs.bullmq.io/classes/v5.Job.html#getdependenciescount)


[PreviousManage Job Schedulers](https://docs.bullmq.io/guide/job-schedulers/manage-job-schedulers) [NextAdding flows in bulk](https://docs.bullmq.io/guide/flows/adding-bulks)

Last updated 5 months ago

Was this helpful?

Copyright (c) Taskforce.sh Inc.