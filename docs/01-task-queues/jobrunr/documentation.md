[![JobRunr](https://www.jobrunr.io/images/logo.svg)](https://www.jobrunr.io/en/)

Menu OpenMenu Close

- [Home](https://www.jobrunr.io/en/)
- [JobRunr Pro](https://www.jobrunr.io/en/pro/)
- [Blog](https://www.jobrunr.io/en/blog/)
- [Documentation](https://www.jobrunr.io/en/documentation/)
- [Guides](https://www.jobrunr.io/en/guides/)
- [Use Cases](https://www.jobrunr.io/en/use-case/)

[Source code](https://github.com/jobrunr/jobrunr)

![Background Pattern](https://www.jobrunr.io/images/header-bg_hu_e981b315d3911de0.webp)

[Introduction](https://www.jobrunr.io/en/documentation/)

[5 minute intro](https://www.jobrunr.io/en/documentation/5-minute-intro/)

[Installation](https://www.jobrunr.io/en/documentation/installation/)

- [Storage](https://www.jobrunr.io/en/documentation/installation/storage/)

[Configuration](https://www.jobrunr.io/en/documentation/configuration/)

- [Fluent API](https://www.jobrunr.io/en/documentation/configuration/fluent/)
- [Spring Boot Starter](https://www.jobrunr.io/en/documentation/configuration/spring/)
- [Micronaut Integration](https://www.jobrunr.io/en/documentation/configuration/micronaut/)
- [Quarkus Extension](https://www.jobrunr.io/en/documentation/configuration/quarkus/)
- [Carbon Aware Processing](https://www.jobrunr.io/en/documentation/configuration/carbon-aware/)
- [Virtual Threads](https://www.jobrunr.io/en/documentation/configuration/virtual-threads/)
- [Metrics](https://www.jobrunr.io/en/documentation/configuration/metrics/)

[Serialization](https://www.jobrunr.io/en/documentation/serialization/)

- [Gson](https://www.jobrunr.io/en/documentation/serialization/gson/)
- [Jackson 2](https://www.jobrunr.io/en/documentation/serialization/jackson2/)
- [Jackson 3](https://www.jobrunr.io/en/documentation/serialization/jackson3/)
- [JSON-B](https://www.jobrunr.io/en/documentation/serialization/jsonb/)
- [Kotlin Serialization](https://www.jobrunr.io/en/documentation/serialization/kotlinx-serialization/)

[Background methods](https://www.jobrunr.io/en/documentation/background-methods/)

- [Dashboard](https://www.jobrunr.io/en/documentation/background-methods/dashboard/)
- [Enqueueing jobs](https://www.jobrunr.io/en/documentation/background-methods/enqueueing-jobs/)
- [Scheduling jobs](https://www.jobrunr.io/en/documentation/background-methods/scheduling-jobs/)
- [Recurring jobs](https://www.jobrunr.io/en/documentation/background-methods/recurring-jobs/)
- [Carbon Aware Jobs](https://www.jobrunr.io/en/documentation/background-methods/carbon-aware-jobs/)
- [Deleting jobs](https://www.jobrunr.io/en/documentation/background-methods/deleting-jobs/)
- [Passing arguments](https://www.jobrunr.io/en/documentation/background-methods/passing-arguments/)
- [Background job dependencies](https://www.jobrunr.io/en/documentation/background-methods/background-jobs-dependencies/)
- [Dealing with exceptions](https://www.jobrunr.io/en/documentation/background-methods/dealing-with-exceptions/)
- [Logging & job progress](https://www.jobrunr.io/en/documentation/background-methods/logging-progress/)
- [Best practices](https://www.jobrunr.io/en/documentation/background-methods/best-practices/)

[JobRunr Pro](https://www.jobrunr.io/en/documentation/pro/)

- [Installation](https://www.jobrunr.io/en/documentation/pro/installation/)
- [JobRunr Pro Dashboard](https://www.jobrunr.io/en/documentation/pro/jobrunr-pro-dashboard/)
- [Multi-Cluster Dashboard](https://www.jobrunr.io/en/documentation/pro/jobrunr-pro-multi-dashboard/)
- [Observability](https://www.jobrunr.io/en/documentation/pro/observability/)
- [Transaction plugin](https://www.jobrunr.io/en/documentation/pro/transactions/)
- [Instant Job Processing](https://www.jobrunr.io/en/documentation/pro/instant-job-processing/)
- [Real-time scheduling and enqueueing](https://www.jobrunr.io/en/documentation/pro/real-time-scheduling/)
- [Dynamic Queues](https://www.jobrunr.io/en/documentation/pro/dynamic-queues/)
- [Priority Queues](https://www.jobrunr.io/en/documentation/pro/priority-queues/)
- [Workflows using Job Chains](https://www.jobrunr.io/en/documentation/pro/job-chaining/)
- [Batches](https://www.jobrunr.io/en/documentation/pro/batches/)
- [Advanced CRON expressions](https://www.jobrunr.io/en/documentation/pro/advanced-cron-expressions/)
- [Server Tags](https://www.jobrunr.io/en/documentation/pro/server-tags/)
- [Rate Limiters](https://www.jobrunr.io/en/documentation/pro/rate-limiters/)
- [Mutexes](https://www.jobrunr.io/en/documentation/pro/mutexes/)
- [Job Result](https://www.jobrunr.io/en/documentation/pro/results/)
- [Replacing jobs](https://www.jobrunr.io/en/documentation/pro/replace-jobs/)
- [Job Filters](https://www.jobrunr.io/en/documentation/pro/job-filters/)
- [Job time-outs](https://www.jobrunr.io/en/documentation/pro/job-timeout/)
- [Custom delete policy](https://www.jobrunr.io/en/documentation/pro/custom-delete-policy/)
- [Database Fault Tolerance](https://www.jobrunr.io/en/documentation/pro/database-fault-tolerance/)
- [CI / CD & Job Migrations](https://www.jobrunr.io/en/documentation/pro/migrations/)
- [Database Migrations](https://www.jobrunr.io/en/documentation/pro/database-migrations/)
- [Issue Tracking Integration](https://www.jobrunr.io/en/documentation/pro/issuetracking-integration/)
- [Single Sign On](https://www.jobrunr.io/en/documentation/pro/sso-authentication/)

[References](https://www.jobrunr.io/en/documentation/references/)

[FAQ](https://www.jobrunr.io/en/documentation/faq/)

[JobRunr compared](https://www.jobrunr.io/en/documentation/alternatives/)

[Credits](https://www.jobrunr.io/en/documentation/credits/)

[Edit this page on GitHub](https://github.com/jobrunr/website/edit/master/content/english/documentation/_index.md)

documentation

# Introduction

Ask AI

Ask JobRunr AssistantGet better accuracy with an LLM trained on our documentation

Copy markdownCopy page content for LLM use[Open in ChatGPTAsk ChatGPT about this page](https://chatgpt.com/?q=Read+the+content+at+https%3A%2F%2Fwww.jobrunr.io%2Fen%2Fdocumentation%2F.+I+want+to+ask+questions+about+it.) [Open in ClaudeAsk Claude about this page](https://claude.ai/new?q=Read+the+content+at+https%3A%2F%2Fwww.jobrunr.io%2Fen%2Fdocumentation%2F.+I+want+to+ask+questions+about+it.)

Run background jobs on the JVM with ease with persistence, automatic retries, and monitoring.

On this page

1. [What is JobRunr?](https://www.jobrunr.io/en/documentation/#what-is-jobrunr)
2. [Why JobRunr?](https://www.jobrunr.io/en/documentation/#why-jobrunr)
3. [How JobRunr works](https://www.jobrunr.io/en/documentation/#how-jobrunr-works)
4. [Where to go next](https://www.jobrunr.io/en/documentation/#where-to-go-next)
5. [Core concepts](https://www.jobrunr.io/en/documentation/#core-concepts)
1. [Scheduling Jobs & Recurring Jobs](https://www.jobrunr.io/en/documentation/#scheduling-jobs--recurring-jobs)
      1. [Creating jobs](https://www.jobrunr.io/en/documentation/#creating-jobs)
      2. [Configuring jobs](https://www.jobrunr.io/en/documentation/#configuring-jobs)
2. [Persisting Jobs with a StorageProvider](https://www.jobrunr.io/en/documentation/#persisting-jobs-with-a-storageprovider)
3. [Processing Jobs with a BackgroundJobServer](https://www.jobrunr.io/en/documentation/#processing-jobs-with-a-backgroundjobserver)
      1. [Accessing your application IoC container](https://www.jobrunr.io/en/documentation/#accessing-your-application-ioc-container)
      2. [Handling failures](https://www.jobrunr.io/en/documentation/#handling-failures)
      3. [Extending JobRunr with JobFilters](https://www.jobrunr.io/en/documentation/#extending-jobrunr-with-jobfilters)
4. [Monitoring Jobs via the Dashboard](https://www.jobrunr.io/en/documentation/#monitoring-jobs-via-the-dashboard)

**JobRunr DocsGPT is here to help**

Trained on all our documentation, it's your fastest way to get unstuck.

Open the chatbot now →

## [What is JobRunr?](https://www.jobrunr.io/en/documentation/\#what-is-jobrunr)

JobRunr is an easy way to perform fire-and-forget, delayed and recurring asynchronous tasks on the JVM, using only Java 8 lambdas, with distributed execution across multiple instances. Jobs are persisted in your existing database so nothing gets lost across restarts or failures. Automatic retries handle the unexpected, while a built-in dashboard gives you real-time visibility into every job.

```java
BackgroundJob.enqueue(() -> emailService.sendWelcomeEmail(userEmail));
```

That one line is a durable, distributed background job. JobRunr serializes the lambda to JSON, stores it in your existing database, and returns to the caller immediately — a `BackgroundJobServer` picks it up and executes it, whether on the same JVM or across a cluster.

## [Why JobRunr?](https://www.jobrunr.io/en/documentation/\#why-jobrunr)

**Developer-friendly API:** Simple and flexible, designed to fit any software architecture with minimal dependencies and few changes to your existing codebase.

**Persistent storage:** JobRunr stores jobs in the same SQL or NoSQL database your application already uses (PostgreSQL, MySQL, MongoDB, and more). No extra infrastructure to operate.

**Fault-tolerant:** By default, every job is retried automatically up to 10 times with exponential back-off if it fails. JobRunr handles transient failures (e.g., network blips, downstream outages, application restarts) so you don’t have to.

**Built-in observability:** A built-in Web UI provides a centralized dashboard for monitoring jobs in real time: inspect any job, diagnose failures, and requeue or delete jobs with a single click.

**No separate deployment needed:** JobRunr is a library, not a service. Include it in your application and you won’t need a separate deployment. You can also choose to deploy it as a microservice.

**Distributed processing:** Scale out by running more instances of your application, each one automatically joins the processing cluster and picks up jobs, with guaranteed single execution.

## [How JobRunr works](https://www.jobrunr.io/en/documentation/\#how-jobrunr-works)

```
BackgroundJobServer Cluster

Persist Job & return immediately

Claim & perform Job

Claim & perform Job

Claim & perform Job

Update Job state

Update Job state

Update Job state

Monitor Jobs

JobScheduler

Server 1

Server 2

... Server N

Storage Provider

Dashboard
```

1. Your code calls `BackgroundJob.enqueue()` (or `schedule()`, or `scheduleRecurrently()`).
2. The [JobScheduler](https://www.jobrunr.io/en/documentation/#scheduling-jobs--recurring-jobs) inspects the lambda using ASM, serializes the type, method, and arguments to JSON, and writes a `Job` record to the [StorageProvider](https://www.jobrunr.io/en/documentation/#persisting-jobs-with-a-storageprovider). Control returns to the caller right after this.
3. One or more [BackgroundJobServers](https://www.jobrunr.io/en/documentation/#processing-jobs-with-a-backgroundjobserver) poll the StorageProvider for new enqueued jobs and process them. Additionally, the longest running server is automatically elected as master, it handles housekeeping (enqueue scheduled jobs, schedule recurring jobs, delete jobs, etc.).
4. On success the job is marked `SUCCEEDED`. On failure the [RetryFilter](https://www.jobrunr.io/en/documentation/#handling-failures) reschedules it with exponential back-off.
5. A Dashboard allows to follow to monitor your job scheduling system.

## [Where to go next](https://www.jobrunr.io/en/documentation/\#where-to-go-next)

- **Learning the basics of JobRunr?** Keep on reading this page.
- **Just getting started?** Follow the [5-minute intro](https://www.jobrunr.io/en/documentation/5-minute-intro/) or the [quickstart guide](https://www.jobrunr.io/en/guides/intro/5-minute-quickstart/).
- **Adding JobRunr to your project?** See [Installation](https://www.jobrunr.io/en/documentation/installation/) and pick a [StorageProvider](https://www.jobrunr.io/en/documentation/installation/storage/).
- **Integrating with your framework?** Choose your setup: [Spring Boot](https://www.jobrunr.io/en/documentation/configuration/spring/), [Micronaut](https://www.jobrunr.io/en/documentation/configuration/micronaut/), [Quarkus](https://www.jobrunr.io/en/documentation/configuration/quarkus/), or the [Fluent API](https://www.jobrunr.io/en/documentation/configuration/fluent/).
- **Need enterprise features?** [JobRunr Pro](https://www.jobrunr.io/en/documentation/pro/) adds batches, job chaining (aka workflows), priority queues, rate limiting, SSO, and more.

[Try JobRunr Pro!](https://www.jobrunr.io/en/try-jobrunr-pro/)

* * *

## [Core concepts](https://www.jobrunr.io/en/documentation/\#core-concepts)

### [Scheduling Jobs & Recurring Jobs](https://www.jobrunr.io/en/documentation/\#scheduling-jobs--recurring-jobs)

At the core of JobRunr, we have the `Job` entity - it contains the name, the signature, the `JobDetails` (the type, the method to execute and all arguments) and the history - including all states - of the background job itself. A `Job` is a unit of work that should be performed outside of the current execution context, e.g. in a background thread, other process, or even on different server – all is possible with JobRunr, without any additional configuration.

A `RecurringJob` is in essence a `Job` with a `cron` expression or a fixed interval attached. The master node checks for due recurring jobs on each poll cycle and and schedules them. More technically, a `RecurringJob` creates multiple `Jobs` during its lifetime.

#### [Creating jobs](https://www.jobrunr.io/en/documentation/\#creating-jobs)

You may create jobs in two ways, either using a `JobLambda` or using a `JobRequest`.

**Via a Java 8 lambda** — pass a lambda referencing the method to execute. JobRunr uses ASM to inspect it and extract the type, method name, and arguments.

```java
BackgroundJob.enqueue(() -> emailService.sendWelcomeEmail(userEmail));
```

`BackgroundJob` is a convenience class with static helper methods; it delegates to the `JobScheduler` under the hood. You can also inject and use `JobScheduler` directly, which is preferable for testability.

Delayed and recurring jobs follow the same pattern:

```java
// Run once, 5 days from now
BackgroundJob.schedule(Instant.now().plus(5, DAYS), () -> emailService.sendFollowUp(userEmail));

// Run every day
BackgroundJob.scheduleRecurrently("daily-report", Cron.daily(), () -> reportService.sendDailyReport());
```

When using Spring, Micronaut, or Quarkus, you can also declare recurring jobs declaratively with the `@Recurring` annotation — JobRunr will register them automatically on startup.

The lambda approach is the least invasive option — no new classes to create. It works with both Java and Kotlin lambdas.

**Via a `JobRequest`** — implement the `JobRequest` interface to carry the job’s data, and pair it with a `JobRequestHandler` that performs the work. This follows the command/handler pattern and is a good fit when you want a clear separation between job data and job logic.

```java
// The request carries the data
public class SendEmailRequest implements JobRequest {
    private final String userId;
    // ...
    @Override
    public Class<SendEmailRequestHandler> getJobRequestHandler() {
        return SendEmailRequestHandler.class;
    }
}

// The handler performs the work
public class SendEmailRequestHandler implements JobRequestHandler<SendEmailRequest> {
    @Override
    public void run(SendEmailRequest request) {
        // do the work
    }
}
```

To create a `Job` using this pattern you have to use `BackgroundJobRequest` or `JobRequestScheduler`. For instance, registering the above `JobRequest` can be done as follows:

```java
BackgroundJobRequest.enqueue(new SendEmailRequest(userId));
```

`BackgroundJobRequest` and `JobRequestScheduler` offer the same APIs as `BackgroundJob` and `JobScheduler`.

#### [Configuring jobs](https://www.jobrunr.io/en/documentation/\#configuring-jobs)

Two mechanisms let you provide additional information to a `Job`: its name, number of retries, labels, and more.

**`@Job` annotation** — place it on the method being executed.

```java
@Job(name = "Send welcome email", retries = 3)
public void sendWelcomeEmail(String userEmail) { ... }
```

> Tip
>
> `@Job` supports parameter substitution using `%0`, `%1`, etc. to reference method arguments by position — e.g. `"Send welcome email to %0"` resolves to `"Send welcome email to email@example.com"` at runtime.

**`JobBuilder`** — use it when you need greater flexibility, e.g., a computed job name. It wraps either a lambda or a `JobRequest`.

```java
BackgroundJob.create(JobBuilder.aJob()
    .withName("Send welcome email to " + userEmail)
    .withAmountOfRetries(3)
    .withJobLambda(() -> emailService.sendWelcomeEmail(userEmail)));
```

### [Persisting Jobs with a StorageProvider](https://www.jobrunr.io/en/documentation/\#persisting-jobs-with-a-storageprovider)

A `StorageProvider` is a place where JobRunr keeps all the information related to background job processing. All the details like types, method names, arguments, etc. are serialized to JSON and placed into storage, no data is kept in a process’ memory. The `StorageProvider` is abstracted in JobRunr well enough to be implemented for [both RDBMS and NoSQL](https://www.jobrunr.io/en/documentation/%28https://www.jobrunr.io/en/documentation/installation/storage/%29) solutions.

JobRunr supports Jackson, Gson, JSON-B, and Kotlin Serialization. See [Serialization](https://www.jobrunr.io/en/documentation/serialization/) for setup instructions.

> Important
>
> This is the main decision you must make, and the only configuration required before you start using the framework.

### [Processing Jobs with a BackgroundJobServer](https://www.jobrunr.io/en/documentation/\#processing-jobs-with-a-backgroundjobserver)

The `BackgroundJobServer` runs inside your application process and is responsible for executing jobs. It polls the `StorageProvider` for work, claims jobs atomically so the same job is never processed twice, and invokes the target method. Because all state lives in the storage provider, jobs are never lost — even if a server is terminated mid-processing, another server will pick up and retry the job after restart.

Run more instances of your application to scale out — each one starts a `BackgroundJobServer` and automatically joins the cluster. One server is elected **master**: it handles recurring job scheduling and housekeeping tasks. The rest act as workers.

> Important
>
> By default, the `BackgroundJobServer` is disabled. You need to explicitly enable it.

> Caution
>
> Never start more than one `BackgroundJobServer` within the same JVM instance.

#### [Accessing your application IoC container](https://www.jobrunr.io/en/documentation/\#accessing-your-application-ioc-container)

When executing a job, JobRunr needs to resolve an instance of the class that holds the job method. By default it instantiates the class directly, but most applications use an [IoC container](https://en.wikipedia.org/wiki/Inversion_of_control) like Spring, Micronaut, or Quarkus. The `JobActivator` interface lets you plug in your container: given a class, it returns a fully initialized instance. When using a framework integration, JobRunr configures this automatically.

#### [Handling failures](https://www.jobrunr.io/en/documentation/\#handling-failures)

When a job fails, the built-in `RetryFilter` automatically reschedules it with exponential back-off — 10 attempts by default. After all retries are exhausted the job moves to `FAILED`, where it stays visible in the dashboard until you act on it. It is never silently dropped.

You can customize retry behaviour with the `@Job` annotation or a `JobBuilder`, or implement your own `RetryFilter` or custom `RetryPolicy` in JobRunr Pro.

#### [Extending JobRunr with JobFilters](https://www.jobrunr.io/en/documentation/\#extending-jobrunr-with-jobfilters)

JobRunr exposes `JobFilter` hooks at every stage of the job lifecycle: before and after job creation (`JobClientFilter`), before and after processing (`JobServerFilter`), and on state transitions (`ElectStateFilter`, `ApplyStateFilter`). This lets you add custom logic — auditing, notifications, … — without touching your job methods.

### [Monitoring Jobs via the Dashboard](https://www.jobrunr.io/en/documentation/\#monitoring-jobs-via-the-dashboard)

The `JobRunrDashboardWebServer` is a built-in web UI available at `http://localhost:8000` by default. It gives you a real-time view of all jobs and servers — monitor job states, inspect failures with full stack traces, requeue or delete jobs, and keep an eye on your processing cluster.

> Important
>
> By default, the dashboard is disabled. You need to explicitly enable it.

> Pro
>
> Need advanced search, authentication, a custom context path, or embedding within your framework’s server? See [JobRunr Pro Dashboard](https://www.jobrunr.io/en/documentation/pro/jobrunr-pro-dashboard/).

#### On this page

1. [What is JobRunr?](https://www.jobrunr.io/en/documentation/#what-is-jobrunr)
2. [Why JobRunr?](https://www.jobrunr.io/en/documentation/#why-jobrunr)
3. [How JobRunr works](https://www.jobrunr.io/en/documentation/#how-jobrunr-works)
4. [Where to go next](https://www.jobrunr.io/en/documentation/#where-to-go-next)
5. [Core concepts](https://www.jobrunr.io/en/documentation/#core-concepts)
1. [Scheduling Jobs & Recurring Jobs](https://www.jobrunr.io/en/documentation/#scheduling-jobs--recurring-jobs)
      1. [Creating jobs](https://www.jobrunr.io/en/documentation/#creating-jobs)
      2. [Configuring jobs](https://www.jobrunr.io/en/documentation/#configuring-jobs)
2. [Persisting Jobs with a StorageProvider](https://www.jobrunr.io/en/documentation/#persisting-jobs-with-a-storageprovider)
3. [Processing Jobs with a BackgroundJobServer](https://www.jobrunr.io/en/documentation/#processing-jobs-with-a-backgroundjobserver)
      1. [Accessing your application IoC container](https://www.jobrunr.io/en/documentation/#accessing-your-application-ioc-container)
      2. [Handling failures](https://www.jobrunr.io/en/documentation/#handling-failures)
      3. [Extending JobRunr with JobFilters](https://www.jobrunr.io/en/documentation/#extending-jobrunr-with-jobfilters)
4. [Monitoring Jobs via the Dashboard](https://www.jobrunr.io/en/documentation/#monitoring-jobs-via-the-dashboard)

Clear

Close

[![JobRunr](https://www.jobrunr.io/images/logo.svg)](https://www.jobrunr.io/en/)

The ultimate library for background processing in Java. Distributed, persistent, and reliable job scheduling.

- [github](https://github.com/jobrunr/jobrunr)
- [linkedin](https://www.linkedin.com/company/jobrunr/)
- [youtube](https://www.youtube.com/@JobRunr)

[![](https://status.jobrunr.io/api/badge/4/status) Carbon Intensity API](https://status.jobrunr.io/status/jobrunr) [![](https://status.jobrunr.io/api/badge/2/status) JobRunr Pro Maven Repo](https://status.jobrunr.io/status/jobrunr)

### Resources

- [Cron Generator](https://www.jobrunr.io/en/tools/cron-expression-generator/)
- [JobRunr for Finance](https://www.jobrunr.io/en/finance/)
- [JobRunr for Insurance](https://www.jobrunr.io/en/insurance/)
- [JobRunr for AI](https://www.jobrunr.io/en/ai/)
- [Our License Model](https://www.jobrunr.io/en/understanding-your-license/)
- [Contact](https://www.jobrunr.io/en/contact/)
- [Live Interactive Demo](https://finance.demo.jobrunr.io/storyline)

### Quick Info

- [Terms](https://www.jobrunr.io/en/licensing/info/)
- [Privacy Policy](https://www.jobrunr.io/en/privacy)
- [About JobRunr](https://www.jobrunr.io/en/about/)
- [Jobs at JobRunr](https://www.jobrunr.io/en/jobs/)

### Subscribe to Our Newsletter!

Stay up-to-date with new JobRunr releases, features, and technical articles.

Subscribe

✕

Can our JobRunr DocsGPT help you out?

![JobRunr Documentation Helper](https://backend.chatbase.co/storage/v1/object/public/chat-icons/411127bc-04c1-403d-ab1f-5294f1181fcc/QK5ol9aVPhWxSgAmhYXq4.webp)